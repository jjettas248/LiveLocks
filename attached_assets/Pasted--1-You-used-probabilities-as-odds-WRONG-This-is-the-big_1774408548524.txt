🚨 1. You used probabilities as odds (WRONG)

This is the biggest issue.

You wrote:

odds: { 
  overOdds: o.calibratedProbabilityOver, 
  underOdds: o.calibratedProbabilityUnder 
}
❌ Why this fails validation
hasRealOdds() is supposed to validate sportsbook odds
You passed probabilities (2–98) instead

That means:

your odds validation is fake
your signal gating is compromised
you’re violating: “no fabricated data”
✅ FIX (REQUIRED)

Use actual book odds only

odds: {
  overOdds: o.overOddsAmerican,
  underOdds: o.underOddsAmerican
}

If your structure is:

o.bookOdds?.over
o.bookOdds?.under

Then use that.

🚨 2. Your “freshness fix” is only half correct

You said:

recompute signalLocked at serve-time

That’s correct direction.

But your implementation still has a flaw:

❌ Problem

You’re relying on:

hasRealOdds(...)

But your client freshness logic must ALSO be applied at render-time, not just server.

Otherwise:

cached signalLocked can still leak stale signals
✅ REQUIRED FIX
Server (routes.ts)

Recompute:

const isFresh =
  isFreshTimestamp(o.oddsUpdatedAt) &&
  isFreshTimestamp(o.projectionUpdatedAt);

const signalLocked =
  isFresh &&
  canShowSignal(market) &&
  Math.abs(o.edge) >= 5;
Client (ALSO REQUIRED)

You MUST already have this (verify):

if (!isFresh(market.oddsUpdatedAt)) return false;
if (!isFresh(market.projectionUpdatedAt)) return false;

inside:

hasRealOdds
canShowSignal

If not → still broken.

⚠️ 3. You mutated projection in-place (risky)

You wrote:

projResult.projection = clampProjection(projResult.projection);
❌ Why this is risky
silent mutation
can corrupt upstream calculations
violates deterministic pipeline
✅ FIX (SAFE PATTERN)
const safeProjection = clampProjection(projResult.projection);

Then return:

projection: safeProjection

Never mutate shared objects.

✅ 4. Clamp coverage — sanity check

You said you applied clamps in:

calculateHitsEdge ✅
buildOutput ✅
calibrated outputs ✅
You’re correct IF:

Every one of these is true:

no rawProbability is returned anywhere
no projection leaves a function unclamped
calibration step does not bypass clamp
🔍 FINAL PASS CHECK (RUN THIS BEFORE COMMIT)

Search your codebase:

1. No raw probability leaks
rawProbability

→ must never appear in final outputs

2. All projections clamped
projection:

→ must always come from clampProjection(...)

3. Odds are REAL

Search:

calibratedProbability

→ must NEVER be used inside:

hasRealOdds
canShowSignal
odds object
4. Freshness enforced BOTH layers

Must exist in:

server (routes or orchestrator output)
client (render gating)
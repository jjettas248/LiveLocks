#11 - Landing Page Screenshot Swap

What & Why
Replace the coded mockup components on the landing page with real product screenshots. The user has provided two images — a dashboard screenshot and an SMS alerts screenshot — that should be used instead of the simulated UI to improve credibility and conversion.

Done looks like
client/public/dashboard-preview.png and client/public/sms-alerts.png exist (copied from attached assets)

DashboardPreview.tsx renders only the dashboard screenshot image inside a rounded bordered container, with no simulated tab/card/prop UI code remaining

The SMS alerts section in landing.tsx shows the sms-alerts.png image in the left column, replacing whatever phone mockup or simulated message UI was there before

Both images render responsively (max-width constrained, centered)

All other sections (routing, auth, pricing, feature grid, CTA, footer, Tailwind classes) are completely untouched

Out of scope
Any routing, backend, or auth changes
Pricing tiers, feature grid, CTA banner, or footer modifications
Any Tailwind styling system changes

Tasks

Copy images to public
Copy:

attached_assets/sms-alerts.png_1773358227254.png  
→ client/public/sms-alerts.png

attached_assets/dashboard-preview.png_1773358233339.png  
→ client/public/dashboard-preview.png

Replace DashboardPreview component

Rewrite:

client/src/components/DashboardPreview.tsx

Component should only render the screenshot image inside a styled container.

Example implementation:

export default function DashboardPreview() {
  return (
    <div className="mx-auto max-w-5xl rounded-xl border border-neutral-800 shadow-2xl overflow-hidden">
      <img
        src="/dashboard-preview.png"
        alt="LiveLocks live NBA prop detection dashboard"
        className="w-full h-auto"
        loading="lazy"
      />
    </div>
  )
}

Remove all:
• mock data
• tabs
• prop card components
• simulated UI code

Update SMS alerts section

Open:

client/src/pages/landing.tsx

Locate the SMS alerts section.

Replace the existing phone mockup / simulated message UI with:

<img
  src="/sms-alerts.png"
  alt="LiveLocks SMS halftime prop alerts"
  className="w-full max-w-md mx-auto rounded-xl border border-neutral-800 shadow-xl"
  loading="lazy"
/>

Place the image in the existing left column.

Do NOT modify the two-column layout or surrounding content.

Relevant files
client/src/components/DashboardPreview.tsx
client/src/pages/landing.tsx
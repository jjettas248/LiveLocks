#72 - NCAAB Full Recovery (All APIs + Fallbacks + Top Plays Restore)

## SYSTEM
Core Engine / Service Layer / UX Layer

## OBJECTIVE
Fully restore NCAAB so that:
- Top Plays is correctly populated from canonical markets
- the selected game detail card shows the same market availability as Top Plays
- Full Game / 1st Half / 2nd Half markets hydrate from all available APIs
- safe fallbacks are used only when primary sources fail
- false "Unavailable / No data" states are eliminated when upstream data exists

## CONTEXT
Current symptoms:
- Top Plays may appear while detail card shows Unavailable
- Full Game totals/spreads can be null during live tournament games
- H1/H2 lines and odds are inconsistent across sources
- ESPN summary data already exists but is not fully used as fallback
- SGO provides some H1/H2 lines without odds
- Odds API may miss live tournament totals or fail H1/H2 coverage

## REQUIREMENTS

### Phase A — Read-only reconciliation first
For one live affected game, compare:
- Top Plays source object
- Today’s Games / live tile source object
- selected detail card play object

Print:
- gameId
- engineOutput.markets for all 6 keys
- market key used by Top Plays
- detail card market object for the same key
- whether duplicate play objects exist for the same gameId

Do not change code until this is reported.

### Phase B — Hydration from all APIs
Use all currently available sources in this priority order:

Full Game:
1. Odds API live markets
2. ESPN summary fallback:
   - overUnder for total
   - spreadDetails for spread
3. If line exists but odds are null, apply -110 fallback

H1/H2:
1. SGO
2. Odds API if available
3. If line exists but odds are null, apply -110 fallback
4. No synthetic lines

Do not exclude any existing API.
Do not weaken Guardrail G7.

### Phase C — Multi-source merge hardening
In ncaabService.ts:
- remove premature loop exits
- allow multi-book composition
- do not overwrite non-null values with null
- totals/spreads should be first-valid-wins
- add tolerant H2 key matching similar to H1
- support outcome parsing by:
  - over / under
  - home / away
  - team names
- support both `price` and `odds`

### Phase D — Canonical market contract
Ensure the selected detail card reads only from:
- full_total / full_spread
- h1_total / h1_spread
- h2_total / h2_spread

No fallback to:
- play.total
- play.spread
- ?? 50
- derived UI-only estimates

If Top Plays can read a valid market for a game, the detail card must show that same market on the correct tab.

### Phase E — Restore Top Plays card
Top Plays must be built only from canonical available markets:
- available === true
- edge !== null

If a market is valid enough for Top Plays, the same game’s detail card must show:
- line
- model probability
- book implied probability
- edge

### Phase F — Logging / diagnostics
Add production-safe logs for:
- Odds API miss
- SGO miss
- ESPN fallback activation
- line present but odds missing
- duplicate play objects for same gameId
- Top Plays market key vs detail card market key mismatch

### Phase G — Runtime verification
For one real live game, prove:
- Top Plays market key
- engineOutput.markets[key]
- detail card tab for same key
- same line
- same model %
- same book implied %
- same edge

## DO NOT
- Do not redesign UI
- Do not add new external APIs
- Do not fabricate synthetic H1/H2 lines
- Do not modify NBA/MLB
- Do not weaken G7
- Do not stop after ESPN fallback only

## SUCCESS CRITERIA
- Full Game markets hydrate when Odds API or ESPN has data
- H1/H2 markets hydrate when SGO or Odds API has data
- odds fallback fills only when line exists and odds are missing
- Top Plays and detail card agree on the same canonical market
- no false Unavailable state when upstream data exists
- Top Plays card is restored and trustworthy

## OUTPUT
1. Root cause report first
2. Exact code changes
3. File-level diffs
4. Runtime proof for one affected live game
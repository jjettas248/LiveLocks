# ADDENDUM: ACTIVE PATH ENFORCEMENT + SINGLE SOURCE OF TRUTH

## PURPOSE:
Ensure all fixes are applied ONLY to the ACTIVE pipeline used by the UI and prevent false-positive restoration.

---

## RULE 1: ACTIVE ROUTE VALIDATION (MANDATORY BEFORE ANY PATCH)

Before modifying ANY logic:

1. Log every incoming request to halftime endpoint:

```ts
console.log("[HT_ENDPOINT_HIT]", {
  path: req.path,
  query: req.query,
  timestamp: Date.now()
});
From the client, confirm this endpoint is actually being hit during UI load.

If NOT:
→ STOP
→ Find correct endpoint
→ DO NOT patch current route

RULE 2: RESPONSE COUNT ASSERTION (SERVER SIDE)

At the FINAL response point of the ACTIVE route:

console.log("[HT_RESPONSE_ASSERT]", {
  totalGames: games.length,
  halftimeGames: games.filter(g => g.isHalftime).length,
  totalMarkets: parsedMarkets.length,
  secondHalfMarkets: secondHalfMarkets.length,
  playsGenerated: plays.length
});

This is the ground truth checkpoint.

RULE 3: CLIENT ↔ SERVER CONSISTENCY CHECK

On the client (dashboard.tsx):

Immediately after fetch:

console.log("[HT_CLIENT_ASSERT]", {
  received: data?.length ?? 0
});

If:

server shows plays > 0
client shows 0

→ This is NOT a pipeline issue
→ This is a client transform / endpoint mismatch

RULE 4: NO PATCHING WITHOUT EVIDENCE

For EACH fix:

You must tie it to a log:

Halftime detection fix → justified by [GAME_STATE_AUDIT]
2H parsing fix → justified by [SECOND_HALF_MARKET_AUDIT]
Quick View fix → justified by [QUICK_VIEW_SOURCE_AUDIT]

If no log proves it:
→ DO NOT CHANGE IT

RULE 5: SINGLE ACTIVE PIPELINE GUARANTEE

Search and confirm:

only ONE /api/halftime-plays exists
only ONE odds ingestion path feeds it
client uses ONLY that endpoint

If multiple:
→ log them
→ identify active one
→ ignore others

RULE 6: HARD SUCCESS GATE (MUST PASS ALL)

The task is NOT complete unless ALL are true:

[HT_ENDPOINT_HIT] confirms correct route is used
[HALFTIME_DETECTION_RESULT] shows ≥1 halftime game when expected
[SECOND_HALF_MARKET_RESULT] shows either:
valid 2H markets OR
confirmed absence from source (not silent failure)
[HT_RESPONSE_ASSERT].playsGenerated > 0
[HT_CLIENT_ASSERT].received > 0
Quick View renders visible data

If ANY fail → task is incomplete

RULE 7: EXPLICIT FINAL STATE

At the end of execution, log ONE of:

console.log("STATUS: HALFTIME PIPELINE RESTORED — TASK #50 UNBLOCKED");

OR

console.log("STATUS: HALFTIME PIPELINE STILL BLOCKED — REASON:", reason);
DO NOT:
Assume the endpoint is correct
Patch multiple layers simultaneously
“Fix” detection without confirming feed shape
Return success without client + server reconciliation

---

# 🧠 Why this addendum matters

Your system is now in a **failure-before-logic state**.

Without enforcement, Replit will:
- patch detection  
- patch odds  
- patch UI  

…but possibly on:
- wrong route  
- wrong pipeline  
- dead code  

And you’ll still see:
> “0 Games at Halftime”

---

# 🎯 What this guarantees

With this addendum, you force:

### 1. Correct path
No more guessing endpoints

### 2. Observable truth
Server vs client reconciliation

### 3. Single fix point
No shotgun debugging

### 4. Clean handoff to Task #50
Once data flows again

---

# Bottom line

- ✅ Task #93 = **correct and approved**
- ⚠️ Addendum = **required for execution integrity**

---

Once this runs, you’ll be back to:

- real halftime games  
- real 2H data  
- functioning Quick View  

Then — and only then — Task #50 becomes relevant again.
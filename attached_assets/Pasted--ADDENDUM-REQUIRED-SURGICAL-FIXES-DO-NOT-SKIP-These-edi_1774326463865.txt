# ADDENDUM — REQUIRED SURGICAL FIXES (DO NOT SKIP)

These edits must be applied alongside Task #88 to prevent secondary regressions.

---

## 1. FIX NO_SIGNAL LEAK INTO signals[] (CRITICAL)

Problem:
Current plan maps NO_SIGNAL → "over", which creates fake signals and corrupts counts.

MANDATORY FIX:

In server/routes.ts where allSignals.push occurs:

REPLACE:
betDirection: (result.recommendedSide === "UNDER" ? "under" : "over"),

WITH:

if (result.recommendedSide === "NO_SIGNAL") {
  return; // skip this entry entirely
}

allSignals.push({
  playerName: dbPlayer.name,
  playerId: dbPlayer.id,
  statType,
  probability: result.displayConfidence ?? result.probability,
  betDirection: result.recommendedSide === "UNDER" ? "under" : "over",
  edge,
  line: liveLine,
  currentStat,
});

RULE:
- NO_SIGNAL entries must NEVER enter signals[]
- signals[] must only contain playable OVER or UNDER signals

---

## 2. HARDEN TEAM DEDUP (PREVENT CROSS-TEAM BLEED)

Problem:
Current dedup uses playerId ?? playerName, which is unsafe:
- null playerId collisions
- same-name collisions
- cross-game contamination

MANDATORY FIX:

In client/src/components/live-boxscore.tsx:

REPLACE dedup block with:

const teamStats = Array.from(
  new Map(
    playedStats
      .filter(
        (s) =>
          s.teamAbbr === team &&
          s.gameId === currentGameId
      )
      .map((s) => [
        `${s.gameId}:${s.teamAbbr}:${s.playerId ?? s.playerName}`,
        s,
      ])
  ).values()
);

RULES:
- MUST include gameId in key
- MUST include teamAbbr in key
- MUST NOT dedup using playerName alone
- MUST NOT allow cross-game or cross-team merging

If gameId is not currently available in this component:
→ pass it down explicitly from parent
→ DO NOT fallback to weaker dedup keys

---

## 3. LOCK PROBABILITY SEMANTICS (PREVENT FUTURE COLLAPSE)

Problem:
"probability" variable is ambiguous and caused original bug.

MANDATORY FIX:

In server/storage.ts inside calculateProbability():

REPLACE:

const overConfidence: number = probability;
const underConfidence: number = 100 - probability;

WITH:

const overLeanProbability = probability; // raw directional value (0–100, OVER likelihood)

const overConfidence = overLeanProbability;
const underConfidence = 100 - overLeanProbability;

const recommendedSide =
  overLeanProbability > 50
    ? "OVER"
    : overLeanProbability < 50
      ? "UNDER"
      : "NO_SIGNAL";

const displayConfidence =
  recommendedSide === "NO_SIGNAL"
    ? null
    : recommendedSide === "OVER"
      ? overConfidence
      : underConfidence;

RULES:
- "probability" must NEVER be treated as display confidence
- All UI must use displayConfidence only
- All directional logic must derive from overLeanProbability
- Prevents reintroduction of semantic ambiguity

---

## SUCCESS CRITERIA FOR ADDENDUM:

- No fake OVER signals created from NO_SIGNAL
- No players appearing under wrong team
- No probability inversion bugs reappearing later
- Directional confidence remains stable across engine, routes, and UI

---

## DO NOT:
- Skip any of these edits
- Reintroduce fallback direction logic for NO_SIGNAL
- Use playerName-only deduplication
- Use "probability" directly in UI without directional mapping
# ADDENDUM — Stripe Plan Mapping + Naming Consistency Fix

## OBJECTIVE
Eliminate plan-name ambiguity ("all"/"elite") and enforce a stable mapping:
- "pro" → STRIPE_PRO_PRICE_ID
- "all" → STRIPE_ALL_SPORTS_PRICE_ID

Ensure frontend, backend, and admin configs all use the same canonical keys.

---

## STEP 1 — CANONICAL PLAN KEYS

Define canonical plan keys everywhere:
- "pro" (Pro $40/mo)
- "all" (All Sports $65/mo)

Deprecate:
- "elite" (replace with "all")

---

## STEP 2 — BACKEND (server/stripeService.ts)

Replace PLAN_META with:

const PLAN_META = {
  pro: {
    priceId:
      process.env.STRIPE_PRO_PRICE_ID ||
      "price_1T6hh82ceUNmv10tdIMnFF5N",
  },
  all: {
    priceId:
      process.env.STRIPE_ALL_SPORTS_PRICE_ID ||
      "price_1T6hh92ceUNmv10tShQlLUYt",
  },
};

Add validation:

function assertPriceIds() {
  if (!process.env.STRIPE_PRO_PRICE_ID) {
    console.warn("[stripe] Missing STRIPE_PRO_PRICE_ID (using fallback)");
  }
  if (!process.env.STRIPE_ALL_SPORTS_PRICE_ID) {
    console.warn("[stripe] Missing STRIPE_ALL_SPORTS_PRICE_ID (using fallback)");
  }
}

Call assertPriceIds() on startup.

---

## STEP 3 — BACKEND (server/routes.ts)

Update ADMIN_TIER_PRICES:

const ADMIN_TIER_PRICES = {
  pro: {
    stripePriceId:
      process.env.STRIPE_PRO_PRICE_ID ||
      "price_1T6hh82ceUNmv10tdIMnFF5N",
  },
  all: {
    stripePriceId:
      process.env.STRIPE_ALL_SPORTS_PRICE_ID ||
      "price_1T6hh92ceUNmv10tShQlLUYt",
  },
};

Update any references:
- Replace "elite" → "all"

---

## STEP 4 — REQUEST NORMALIZATION (CRITICAL)

In checkout route, normalize incoming plan:

const rawPlan = req.body.plan;

const plan =
  rawPlan === "elite" ? "all" : rawPlan; // backward compatibility

if (!["pro", "all"].includes(plan)) {
  return res.status(400).json({ error: "Invalid plan" });
}

---

## STEP 5 — FRONTEND ALIGNMENT

Ensure upgrade calls:

handleUpgrade("pro")
handleUpgrade("all")

Search and replace:
- "elite" → "all"

---

## STEP 6 — REMOVE ALL LEGACY PRICE IDS

Search entire repo for:
price_1T6fl12cW8Vmrgt3B6ffBIuw
price_1T6fly2cW8Vmrgt3WU9uHL7L

Delete/replace ALL occurrences.

---

## STEP 7 — STARTUP LOGGING

Ensure logs show:

[stripe] Pro price ID: <value>
[stripe] All Sports price ID: <value>

---

## STEP 8 — DONE CRITERIA

✔ Only "pro" and "all" used across codebase  
✔ No "elite" references remain (except normalization)  
✔ No invalid price IDs remain  
✔ Checkout works for both plans  
✔ No 500 errors from Stripe  

---

## NON-GOALS

- Do NOT modify stripeClient.ts
- Do NOT change subscription logic
- Do NOT change frontend UI design beyond plan names
- Do NOT alter pricing amounts

---

## FINAL NOTE

This enforces a single source of truth:
plan key → env price ID → Stripe product

Prevents future checkout failures when adding tiers.
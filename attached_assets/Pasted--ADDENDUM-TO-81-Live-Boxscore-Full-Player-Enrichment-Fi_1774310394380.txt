# ADDENDUM TO #81 - Live Boxscore: Full-Player Enrichment Fix (Required Tightening)

## PURPOSE:
Resolve remaining ambiguity in #81 so the implementation fully fixes the boxscore and does not introduce inconsistent highlight behavior.

## REQUIRED UPDATES:

1. UNIFY PLAYER LOOKUPS ON PLAYER ID
- Do NOT keep any name-keyed lookup for live boxscore enrichment or highlight state.
- Replace ALL name-based signal/highlight matching in the live boxscore with playerId-based lookup.
- If the existing signals[] array does not include playerId, add it on the backend and preserve it through the response.
- Boxscore component must use playerId as the single canonical identifier for:
  - probability badge enrichment
  - row highlight state
  - selected player row state
  - row click / detail lookup logic

2. REMOVE HIGHLIGHT THRESHOLD CONFLICT
- Do NOT introduce a new row highlight rule based on edge >= 55 unless that threshold already exists in production and is intentionally desired.
- Preserve the CURRENT production highlight behavior and legend logic exactly.
- Probability badge visibility and row highlight visibility are separate concerns:
  - badge = render for every eligible player with valid engine output
  - highlight = only for players that meet the existing strong-signal production threshold
- Do not change the current color legend thresholds unless explicitly required elsewhere.

3. DEFINE ONE CANONICAL BADGE ELIGIBILITY RULE
- Use exactly one rule for when a player should show a probability badge.
- Canonical rule:
  - render badge only when player has a valid engineOutput entry for watchedStatType
  - and player minutes >= 3
- Do NOT also require “all players with stats” if they do not meet the minutes threshold.
- Success criteria must evaluate against this exact rule only.

4. HARDEN ENGINE OUTPUT RESPONSE SHAPE
- Backend response must return:
  {
    signals: Signal[],
    engineOutput: Record<number, Record<string, EngineEntry>>
  }
- EngineEntry must contain:
  {
    probability: number,
    betDirection: "OVER" | "UNDER",
    edge: number,
    line: number | null,
    statType: string
  }
- Ensure every player included in liveStats can be looked up via engineOutput[playerId] without name normalization.
- Do not filter engineOutput by edge threshold.
- Keep signals[] filtered/sorted as currently required for the halftime plays panel.

5. FRONTEND RENDER GUARANTEE
- live-boxscore.tsx must iterate the full player list with .map()
- No .find() on the players list for enrichment
- No early return that exits the entire player iteration
- Missing engineOutput for one player must not block rendering of the rest
- Badge lookup must be:
  engineOutput?.[player.playerId]?.[watchedStatType]
  or the exact equivalent based on the actual row object shape

6. ADD TEMP VALIDATION LOGGING
Add temporary logs during implementation:
- total liveStats player count
- total distinct playerIds in engineOutput
- total badge-rendered players for current watchedStatType
- total highlighted players
This is to verify:
- enrichment coverage works across the full card
- highlight count remains consistent with existing strong-signal behavior

## UPDATED SUCCESS CRITERIA:
- Every player with minutes >= 3 and a valid engineOutput entry for the selected watchedStatType shows a probability badge
- No player ID / name mismatch causes missing enrichment
- Strong-signal row highlighting matches existing production highlight behavior exactly
- Badge rendering and highlight rendering are decoupled correctly
- live-boxscore.tsx fully replaces the duplicated inline dashboard boxscore JSX with no behavior regression
- No regression to row click, selected-player state, sort, filter, or signal legend behavior
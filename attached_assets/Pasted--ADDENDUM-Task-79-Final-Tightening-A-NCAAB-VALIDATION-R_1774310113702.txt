# ADDENDUM — Task #79 (Final Tightening)

## A. NCAAB VALIDATION — REQUIRE MARKET TYPE + SAFE ODDS SELECTION

### Add explicit marketType source
Ensure `marketType` is explicitly defined (do NOT infer from fields):

- Allowed values: "total" | "spread"
- If unknown → skip with reason

```ts
if (!marketType || (marketType !== "total" && marketType !== "spread")) {
  console.warn(`[NCAAB SKIP][${gameId}][unknown] invalid marketType`)
  continue
}
Use safe odds selection (no null coalescing across types)
let line, odds

if (marketType === "total") {
  line = engineInput.liveTotalLine
  odds = engineInput.overOddsAmerican
} else {
  line = engineInput.liveSpread
  odds = engineInput.spreadOdds
}

Do NOT use ?? across types (prevents mixing totals/spreads).

B. VALIDATION — INCLUDE BOTH SIDES WHEN AVAILABLE

If both sides exist, prefer validating both:

if (marketType === "total") {
  if (engineInput.overOddsAmerican == null && engineInput.underOddsAmerican == null) {
    console.warn(`[NCAAB SKIP][${gameId}][total] missing both sides`)
    continue
  }
}

(Keep primary validation as defined; this is an additional guard.)

C. ANALYTICS VERIFY ENDPOINT — ACCESS + SHAPE GUARANTEES
Access control

Must use existing requireAdmin guard

Must not be exposed publicly

Response contract (strict)
{
  "totalRecords": number,
  "bySport": {
    "nba": number,
    "ncaab": number,
    "mlb": number
  },
  "unresolvedAlerts": number
}
Add sanity guards
if (!Number.isFinite(totalRecords)) totalRecords = 0
if (!bySport || typeof bySport !== "object") bySport = { nba: 0, ncaab: 0, mlb: 0 }
if (!Number.isFinite(unresolvedAlerts)) unresolvedAlerts = 0
D. NO REGRESSION CHECKS

Confirm:

No ESPN-derived values enter NCAAB engine input

No isDegraded introduced in NCAAB

No changes to /api/analytics/summary or /api/analytics/alerts response shapes

OUTPUT (UNCHANGED):

Return:

Files modified

File-level diffs

Example NCAAB skip logs (include marketType)

/api/analytics/verify sample response

Do not summarize.


---

## Why this is enough

- You now have **true per-market validation parity** with MLB
- You’ve preserved **clean API contracts**
- You’ve added **verifiable analytics without side effects**

---

## Bottom line

Run #79 + this addendum.

After that, your system is:
👉 **data-clean, contract-safe, and production-ready**
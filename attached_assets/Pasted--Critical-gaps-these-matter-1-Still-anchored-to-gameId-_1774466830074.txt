⚠️ Critical gaps (these matter)
1. ❌ Still anchored to :gameId

You confirmed:

sim:<gameId>:<scenario>

This is still wrong.

Why this breaks your system

Simulation should be:

stateless and deterministic

Right now it is:

dependent on whichever gameId is hit

That introduces:

inconsistent results across requests
hidden coupling to real schedule
unpredictable cache behavior
✅ Fix (must do)

Inside simulation branch:

const simGameId = `SIM_${scenario}`;
const cacheKey = `sim:${simGameId}`;
Also:

Return a synthetic response:

res.json({
  gameId: simGameId,
  isSimulation: true,
  scenario,
  results
});
2. ❌ You are not simulating a board (this is the biggest flaw)

Your system is designed to:

evaluate multiple players
rank edges
return top plays

But your simulation:

runs a single player through the engine

Why this is a problem

You are NOT testing:

edge distribution
ranking logic
OVER vs UNDER balance
sorting behavior

You are only testing:

“does projection change?”

That’s insufficient.

✅ Fix (high priority)

Update getMockData(scenario):

return {
  players: [
    {
      name: "Player A",
      minutes: 18,
      points: 12,
      fgPct: 0.42,
      line: 20.5
    },
    {
      name: "Player B",
      minutes: 20,
      points: 16,
      fgPct: 0.50,
      line: 24.5
    },
    {
      name: "Player C",
      minutes: 15,
      points: 8,
      fgPct: 0.36,
      line: 14.5
    },
    {
      name: "Player D",
      minutes: 22,
      points: 14,
      fgPct: 0.48,
      line: 18.5
    }
  ],
  gameState: {...}
};

Then:

const results = players.map(p =>
  calculateProbability({...p, gameState})
);

Then:

sort
filter
return top plays
3. ❌ No baseline vs post-fix comparison

Right now:

you run simulation
you “look at output”

That is not enough.

✅ Fix (simple, high ROI)

Add structured logs:

console.log("[SIM_BEFORE]", {
  projection,
  line,
  edge,
  direction
});

After change:

console.log("[SIM_AFTER]", {
  projection,
  line,
  edge,
  direction
});
Why this matters

You need to answer:

“Did this change actually improve the model?”

Not:

“Does this feel better?”

🧪 What you should do immediately
Step 1 — Fix cache + gameId
remove real gameId dependency
Step 2 — Expand to multi-player simulation
minimum: 3–5 players
run full pipeline
Step 3 — Capture baseline

Run:

neutral scenario
cold scenario

Record:

projections
directions
OVER/UNDER count
Step 4 — Apply shooting-weight fix
const halftimeShootingWeight = 0.7;
shootingModifier = 1 + (shootingModifier - 1) * halftimeShootingWeight;
Step 5 — Re-run and compare

You should see:

Before
mostly UNDER
projections slightly below line
After
more balanced mix
some OVER signals appear
projections slightly lifted
🎯 What success actually looks like

Not:

forced 50/50

But:

natural distribution
no systematic UNDER clustering
varied top plays
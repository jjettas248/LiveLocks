# Fix Replit "MERGING" / Apply Failure (Internal SSH Proxy)

SYSTEM
Replit Apply Recovery — NO CODE CHANGES

OBJECTIVE
Unblock the Replit apply/merge pipeline that is stuck due to internal SSH proxy authentication (git@ssh.worf.replit.dev). The code is already committed and correct. We must re-trigger the platform’s internal sync without modifying application logic or git history.

CRITICAL CONTEXT
- Repo is NOT GitHub-backed (no HTTPS fallback exists)
- main-repl is an internal Replit filesystem remote
- SSH push fails due to missing platform credentials
- Code changes (Task #67) are already committed and valid
- Working tree is clean
- This is a platform sync issue, NOT a code issue

DO NOT
- Do NOT modify any code files
- Do NOT reset, rebase, or force push
- Do NOT change remotes
- Do NOT attempt SSH fixes
- Do NOT run build/test loops

---

## STEP 1 — Create a no-op commit to trigger apply

Run:

git commit --allow-empty -m "replit: retrigger apply pipeline"

---

## STEP 2 — Soft refresh Replit environment

Execute:

kill -9 1 || true

(This forces the Replit container to restart and reinitialize the apply process)

---

## STEP 3 — Re-open project UI

After restart:
- Reload the Replit workspace
- Navigate to Version Control panel

---

## STEP 4 — Manually trigger apply

In the Replit UI:
- Click **“Apply changes”** OR **“Retry merge”**
- If visible, click **“Sync”**

This uses Replit’s internal credentials (not available to the agent)

---

## STEP 5 — If still stuck, force UI diff refresh

Run:

touch .replit

Then reload the UI again

---

## EXPECTED RESULT

- Replit exits "MERGING" state
- Internal SSH sync completes
- Task #67 is applied to main environment
- NCAAB engine behavior updates immediately:
  - H1/H2 lines populate
  - Book implied % appears
  - No more "Unavailable" from missing odds
  - 2H panel properly gated

---

## ESCALATION (only if all above fails)

If UI still stuck:
- Click **“Fork repl”**
- Open fork → Version Control → Apply
- Or contact Replit support with:
  "Internal SSH proxy (worf) apply failure — clean commit exists but cannot sync"

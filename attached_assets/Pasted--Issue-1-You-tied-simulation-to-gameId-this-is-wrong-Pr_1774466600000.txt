⚠️ Issue #1 — You tied simulation to :gameId (this is wrong)
Problem

Your route is:

/api/live-signals/:gameId

And simulation uses:

sim:<gameId>:<scenario>
Why this is flawed

Simulation should be:

independent of real game IDs

Right now:

you are anchoring fake data to real game IDs
your test surface is constrained by live schedule
Fix (important)
Replace dependency on gameId

Inside simulation branch:

const simGameId = `SIM_${scenario}`;

Then:

const cacheKey = `sim:${simGameId}`;

Do NOT use real gameId at all.

Why this matters

Without this fix:

simulation behavior varies depending on which game you click
debugging becomes inconsistent
edge cases get masked
⚠️ Issue #2 — You are simulating ONE player, not a slate
Problem

Your current setup:

injects one mock player
runs engine once
But your real system:
ranks multiple players
sorts edges
selects top plays
Consequence

You are not testing:

distribution
ranking logic
board composition

You are only testing:

“does projection move?”

That’s incomplete.

Fix (high value)
Change mock data to return MULTIPLE players
function getMockData(scenario) {
  return {
    players: [
      { name: "Player A", minutes: 18, points: 12, fgPct: 0.45, line: 20.5 },
      { name: "Player B", minutes: 20, points: 16, fgPct: 0.52, line: 24.5 },
      { name: "Player C", minutes: 15, points: 8, fgPct: 0.38, line: 14.5 }
    ],
    gameState: {...}
  };
}

Then run your full pipeline loop, not a single call.

Why this matters

You need to validate:

OVER/UNDER mix
edge ranking
top plays selection
⚠️ Issue #3 — Missing baseline comparison (this is critical)

Right now you:

run simulation
see output

But you are NOT:

comparing before vs after changes

Fix (simple + powerful)

Add:

console.log("[SIM_BASELINE]", {
  projection,
  line,
  edge,
  direction,
});

Then after changes:

console.log("[SIM_AFTER_FIX]", {
  projection,
  line,
  edge,
  direction,
});
Why this matters

Without this:

you won’t know if a fix improved anything
you’ll rely on feel instead of data
🧪 What you should do next (exact sequence)
Step 1 — Fix gameId dependency
remove real gameId usage
use SIM_<scenario>
Step 2 — Expand to 3–5 players
simulate a mini slate
run full ranking pipeline
Step 3 — Run baseline

With current engine:

log 5 players
record direction mix
Step 4 — Apply shooting-weight fix
const halftimeShootingWeight = 0.7;
shootingModifier = 1 + (shootingModifier - 1) * halftimeShootingWeight;
Step 5 — Re-run simulation

Compare:

projections
edges
OVER vs UNDER count
🎯 What success looks like

Before:

80–90% UNDER
projections just below line

After:

40–60% mix
projections slightly lifted
some OVER edges appear naturally
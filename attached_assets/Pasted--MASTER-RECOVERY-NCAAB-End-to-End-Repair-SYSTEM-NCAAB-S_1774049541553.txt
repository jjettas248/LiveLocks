# MASTER RECOVERY — NCAAB End-to-End Repair

## SYSTEM
NCAAB Service Layer / Engine / UI Rendering

## OBJECTIVE
Fully restore the NCAAB product so that H1, H2, and Full Game markets hydrate correctly, reach the engine correctly, and render correctly in the UI.

This is an end-to-end recovery task.
Do not treat this as a single ingestion bug.

---

## CONTEXT

Current symptoms:
- H1 and H2 markets often show "Unavailable"
- Book implied probability shows "—%"
- Edge is missing
- H1/H2 lines may not be reaching engineInput
- The wrong market block may render in the wrong tab
- The experience was working previously, so this likely indicates a regression in:
  - extraction
  - market routing
  - or canonical market rendering

We already know:
- Odds fallback logic exists for H1/H2 (-110 when line exists and odds are null)
- Live2HPanel should only render in H2 tab
- Canonical markets architecture exists
- Guardrail G7 should remain intact

---

## REQUIREMENTS

### PHASE A — READ-ONLY ROOT CAUSE REPORT FIRST
Do NOT change code yet.

For one live H1 game and one halftime/2H game, trace the full pipeline and return a root cause report.

For each game, report:

1. Raw sportsbook data
- all bookmaker keys
- all H1/H2 market keys detected
- all H1/H2 outcome names detected
- all prices/odds detected

2. Extracted service-layer values
- h1TotalLine
- h1SpreadLine
- h2TotalLine
- h2SpreadLine
- h1OverOddsAmerican
- h1SpreadOddsAmerican
- h2OverOddsAmerican
- h2SpreadOddsAmerican

3. Engine input
- exact values passed into runNCAABEngine

4. Engine output
- engineOutput.markets.full_total
- engineOutput.markets.full_spread
- engineOutput.markets.h1_total
- engineOutput.markets.h1_spread
- engineOutput.markets.h2_total
- engineOutput.markets.h2_spread

5. UI render path
- which market object Full Game tab uses
- which market object H1 tab uses
- which market object H2 tab uses
- whether any projected/fallback blocks are rendered outside their correct tab

Return the exact root cause(s) before any fixes.

---

### PHASE B — SERVICE LAYER HYDRATION FIX
Only after root cause is identified.

Fix all hydration issues so:
- all bookmakers are scanned
- no premature break exits the loop
- H1 keys are matched tolerantly
- H2 keys are matched tolerantly
- outcome parsing does not assume fixed ordering
- team-name spread outcomes are supported
- odds extraction supports both `price` and `odds`

Rules:
- use normalizeMarketKey()
- use isH1TotalKey / isH1SpreadKey
- add isH2TotalKey / isH2SpreadKey
- totals must be classified before spreads
- do not overwrite valid values with null
- allow multi-book composition:
  - book A may provide H1 total
  - book B may provide H1 spread
  - book C may provide H2 markets

Preserve:
- existing -110 fallback
- no synthetic lines
- no new APIs

---

### PHASE C — ENGINE INPUT / MARKET VALIDATION
Validate that the service layer is actually feeding the engine usable values.

Before runNCAABEngine, add non-production debug logging showing:

- h1TotalLine
- h1SpreadLine
- h2TotalLine
- h2SpreadLine
- h1OverOddsAmerican
- h1SpreadOddsAmerican
- h2OverOddsAmerican
- h2SpreadOddsAmerican

If a line exists and odds are null, confirm the -110 fallback is applied before engine call.

Do NOT weaken Guardrail G7.

---

### PHASE D — UI TAB / MARKET RENDER FIX
Fix any render-layer leakage or wrong binding.

Requirements:
- Full Game tab renders only full_total / full_spread
- H1 tab renders only h1_total / h1_spread
- H2 tab renders only h2_total / h2_spread
- Live2HPanel only renders when:
  marketTab === "h2"
  AND halftime/2H state is true

Do NOT allow:
- 2H projected panel in Full Game tab
- 2H projected panel in H1 tab
- H1 tab reading full game market fallback
- H2 tab reading full game market fallback

If canonical market is unavailable:
- render canonical unavailable state only
- no synthetic line
- no ?? 50
- no fallback to play.total / play.spread

---

### PHASE E — POST-FIX VERIFICATION
After changes, verify with a live or mocked game:

1. H1 tab:
- line present when sportsbook provides it
- book implied % present when odds exist or fallback is applied
- edge present

2. H2 tab:
- line present when sportsbook provides it
- book implied % present
- edge present

3. Full Game tab:
- no H2 projected panel leakage

4. Logs:
- confirm engineInput receives non-null H1/H2 values
- confirm engineOutput.markets for H1/H2 are available when upstream data exists

---

## DO NOT

- Do not redesign the UI
- Do not reopen old canonical market rebuild logic unless directly necessary
- Do not fabricate synthetic H1/H2 lines
- Do not weaken Guardrail G7
- Do not add new data providers
- Do not modify NBA or MLB
- Do not run long build loops automatically

---

## SUCCESS CRITERIA

The NCAAB product is considered repaired only when all of the following are true:

- H1 markets hydrate and render when sportsbook data exists
- H2 markets hydrate and render when sportsbook data exists
- book implied probability is populated
- edge is populated
- wrong tab content no longer leaks
- no false "Unavailable" state when upstream data exists

---

## OUTPUT

1. Root cause report first
2. Then exact code changes
3. Then file-level diffs
4. Then runtime verification summary

No placeholders.
No vague summaries.
Show exact fixes.
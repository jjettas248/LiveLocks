🎯 OBJECTIVE TODAY

Get MLB to:

✅ Fully wired (data → engine → UI)
✅ Admin-visible (no locks)
✅ Markets generating consistently
✅ UI clean (no broken cards, no placeholders)
✅ Debuggable (you can trust what you see)
🔥 PRIORITY STACK (DO IN ORDER)
1. VERIFY SERVER TRUTH (5–10 min)

Hit:

GET /api/mlb/live-games
GET /api/mlb/debug
You are checking ONLY this:
{
  "awayTeam": "Atlanta Braves",
  "homeTeam": "San Francisco Giants",
  "pitcherAway": "Max Fried",
  "pitcherHome": "Logan Webb",
  "market": {
    "line": 6.5,
    "odds": {
      "overOdds": -110,
      "underOdds": -110
    },
    "projection": 7.2,
    "probability": 61,
    "edge": 8.3
  }
}
If ANY of these are null:
teams → ESPN mapping broken
odds → OddsAPI wiring broken
projection → engine not firing
edge/probability → market generation failing

👉 Do NOT move forward until this is clean

2. FIX ADMIN GATING (CRITICAL)

Right now you cannot trust anything you see.

Must be true:
const isAdmin = user?.role === "admin";
const hasProAccess = isAdmin || user?.tier === "pro" || user?.tier === "pro_all";
const signalLocked = !hasProAccess;
Verify in UI:
No “Probability locked”
No “Edge locked”
You see real numbers

👉 If this is not fixed, stop everything else

3. FIX PROJECTED OPPORTUNITIES CARD (YOUR CURRENT BREAK)

Your screenshot shows:

Max Fried
@

This means:

card is NOT using canonical fields
OR it’s using stale/old field names
Find this component:
“Projected Opportunities”
or similar card renderer
Replace ALL identity fields with:
game.awayTeam
game.homeTeam
game.pitcherAway
game.pitcherHome
Add hard guard:
if (!game.awayTeam || !game.homeTeam) return null;

👉 This alone will eliminate broken cards

4. VERIFY MARKET GENERATION (REAL EDGE OUTPUT)

Check logs:

[MLB MARKET OUTPUT]

You should see:

{
  projection: number,
  probability: number,
  edge: number,
  line: number
}
If you see mostly:
[MLB MARKET SKIP]

Then diagnose:

Reason	Meaning
no book line	OddsAPI issue
no real odds	stale or incomplete odds
guard error	validation too strict
engine exception	calculation bug

👉 Fix based on dominant skip reason

5. VERIFY RENDER STATES (UI LOGIC)

Each game must fall into ONE state:

State	Condition
INVALID	missing teams
PREVIEW	no odds
NO_SIGNAL	odds but no edge
SIGNAL	valid edge
Quick visual check:
No teams → card should NOT exist
No odds → “Awaiting live lines”
No edge → “No strong edge”
Edge → full signal
6. REMOVE ALL UI FALLBACK GARBAGE

Search and delete:

"N/A"
"vs"
"0"
"||"
"@"

Replace with:

if (!data) return null;
7. FINAL SYSTEM CHECK (10 min)

Run through:

A. One game with:
teams
pitchers
no odds → PREVIEW
B. One game with:
odds
no edge → NO_SIGNAL
C. One game with:
odds + edge → SIGNAL
🚨 WHAT YOU SHOULD NOT TOUCH TODAY

Do NOT:

tweak model logic
adjust probability scaling
optimize Savant inputs
change BallparkPal weighting

👉 Today is wiring + integrity only

🧠 SUCCESS CRITERIA

You are done when:

You (admin) see full signals
No cards render broken
At least 1 valid MLB signal appears when odds exist
Debug endpoint matches UI exactly

Rewriting halftime engine math unless the active response path proves it is still biased
Running another full forensic instrumentation pass (Task #50 prior work already exists)
Touching MLB or NCAAB
Artificial OVER/UNDER balancing
Patching dead or alternate code paths not in the active client→server route
Tasks
Identify the active client endpoint — Search the frontend halftime UI component(s) for the exact API endpoint being called (fetch, useQuery, axios, or equivalent). Document the exact client file, endpoint URL, and hook/function used.

Trace the endpoint to the server handler — Locate the exact server route handler that responds to the halftime UI's fetch. Confirm whether this is the same route targeted by previous Task #50 work. Add a [HT_RESPONSE_FINAL] console log at the final return/res.json of that handler, capturing play count, OVER count, UNDER count, and a 10-play sample with playerName, market, direction, probability, edge, projection, and line.

Instrument the client fetch and render — In the actual halftime UI component, add [HT_CLIENT_FETCHED] immediately after the data resolves, and [HT_CLIENT_RENDERED] immediately before render after all local transforms. Between them, log counts after each intermediate transform stage (filter, sort, slice, dedupe, map) — input and output count plus OVER/UNDER split per stage.

Audit downstream suppression — Search the client halftime path for every .filter(), .sort(), .slice(), dedup call, probability re-check, or "top plays" selection logic. Document each in order with its input/output counts and directional impact.

Verify engine contract usage and remove re-derivation — Confirm the halftime card render layer uses direction, probability, and edge directly from the engine response. Search for and remove any client-side re-derivation such as probability < 50 ? "UNDER" : "OVER" or 100 - probability.

Check top-play sort distortion — Add a [HT_SORTED_TOP20] log in the active server response path that sorts plays by edge descending and logs the top 20. This determines whether the engine distribution is healthy overall but the top-ranked plays happen to be under-heavy.

Classify root cause and apply minimal fix — Using all log evidence, classify the issue as A/B/C/D/E and apply exactly one targeted change: repoint the UI if path mismatch (B), remove the suppressive transform (C), delete client re-derivation (D), adjust the UI ranking/selection contract if sort skew (E), or patch only the proven active engine path if engine output is still biased (A).

Relevant files
server/routes.ts:2094-2565
server/storage.ts:313-803
# TASK #50 — FINAL FIX: PROJECTION SUPPRESSION NORMALIZATION

## SYSTEM:
NBA Halftime Engine (server-side projection pipeline)

## OBJECTIVE:
Eliminate structural UNDER bias caused by compounded suppression from stacked multipliers (pace, defense, regression).

This is the confirmed root cause after Task #50 audit.

Apply ONE fix only:
- normalize/clamp suppression multipliers
- slightly adjust regression factor

DO NOT modify:
- probability model
- edge calculation
- sorting
- filtering
- UI
- MLB or NCAAB systems

---

## ROOT CAUSE (CONFIRMED):

Projection pipeline applies multiple <1 multipliers:

projection = base * pace * defense * regression

This compounds into systemic suppression:

Example:
0.95 * 0.97 * 0.92 ≈ 0.85

This forces:
projection << line → UNDER bias across all plays

---

## IMPLEMENTATION:

### STEP 1 — Adjust halftime regression factor

Locate:
halftimeRegressionFactor

Replace:

```ts
const halftimeRegressionFactor = 0.92;

With:

const halftimeRegressionFactor = 0.96;
STEP 2 — Clamp multiplier stack

Locate projection pipeline where multipliers are combined:

const suppression = paceMultiplier * defenseMultiplier * regressionMultiplier;

Replace with:

const suppressionRaw =
  paceMultiplier * defenseMultiplier * regressionMultiplier;

// Clamp to prevent systemic under-bias
const suppression = Math.max(0.92, Math.min(1.08, suppressionRaw));

Then ensure projection uses:

projection = baseProjection * suppression;
STEP 3 — Add trace logging (REQUIRED for validation)

Immediately before play is pushed:

console.log("[HT_SUPPRESSION_TRACE]", {
  player: playerName,
  market,
  baseProjection,
  paceMultiplier,
  defenseMultiplier,
  regressionMultiplier,
  suppressionRaw,
  suppressionClamped: suppression,
  finalProjection: projection,
  line,
  direction,
});
DO NOT:
Do NOT rebalance OVER/UNDER manually
Do NOT modify probability scaling
Do NOT adjust edge calculation
Do NOT introduce randomness
Do NOT stack additional fixes
SUCCESS CRITERIA:

After deployment:

[HT_DISTRIBUTION] shows natural mix:
~40–60% UNDER
~40–60% OVER
[HT_SUPPRESSION_TRACE] shows:
suppressionRaw occasionally < 0.92
suppressionClamped never < 0.92
Top plays include BOTH directions (not all UNDER)
Projection vs line relationship is no longer systematically negative
OUTPUT:

Return:

exact file and function modified
before/after code diff
3 sample [HT_SUPPRESSION_TRACE] logs
updated [HT_DISTRIBUTION] log
NOTES:

This is the final step of Task #50.

Previous fixes (C + E) corrected UI and sorting bias.
This fix corrects the actual projection bias.

No further changes should be made unless new evidence emerges.


---

# 🧭 What happens after you run this

### Immediately:
- UNDERS drop from ~100% → ~55–65%
- OVERS begin appearing naturally
- projections shift closer to lines

### Within 1–2 slates:
- you’ll see real distribution behavior
- THEN you can evaluate true model quality

---

# 🚨 If this does NOT fix it

Then (rare case):

- minutes model is wrong  
- or pace multiplier is mis-scaled  

But based on your data:
> 90% probability this fix resolves it

---

# Bottom line

This is the **actual fix Task #50 was designed to uncover**.

Run this — nothing else — and your engine should finally behave like a real model instead of a suppressed one.
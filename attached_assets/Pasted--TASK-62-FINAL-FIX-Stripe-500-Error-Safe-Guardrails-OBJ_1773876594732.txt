# TASK #62 — FINAL FIX (Stripe 500 Error + Safe Guardrails)

## OBJECTIVE
Fix "No such price" Stripe error and ensure checkout NEVER 500s again due to:
- Invalid price IDs
- Missing env vars
- Bad plan input

---

## STEP 1 — ENV SECRETS (REQUIRED)

Ensure these exist:

STRIPE_PRO_PRICE_ID = price_1T6hh82ceUNmv10tdIMnFF5N
STRIPE_ALL_SPORTS_PRICE_ID = price_1T6hh92ceUNmv10tShQlLUYt

---

## STEP 2 — UPDATE server/stripeService.ts

Replace PLAN_META with:

const PLAN_META = {
  all: {
    // frontend sends "all" → maps to Pro tier
    priceId:
      process.env.STRIPE_PRO_PRICE_ID ||
      "price_1T6hh82ceUNmv10tdIMnFF5N",
  },
  elite: {
    // frontend sends "elite" → maps to All Sports tier
    priceId:
      process.env.STRIPE_ALL_SPORTS_PRICE_ID ||
      "price_1T6hh92ceUNmv10tShQlLUYt",
  },
};

---

## STEP 3 — ADD HARD VALIDATION (CRITICAL)

Before Stripe session creation:

const priceId = PLAN_META[plan]?.priceId;

if (!plan || !PLAN_META[plan]) {
  console.error("[stripe] Invalid plan received:", plan);
  return res.status(400).json({ error: "Invalid plan selected" });
}

if (!priceId) {
  console.error("[stripe] Missing price ID for plan:", plan);
  return res.status(500).json({ error: "Pricing configuration error" });
}

---

## STEP 4 — ADD DEBUG LOGGING

Inside checkout route:

console.log("[stripe] Plan:", plan);
console.log("[stripe] Using price ID:", priceId);

---

## STEP 5 — UPDATE server/routes.ts

Replace ADMIN_TIER_PRICES:

const ADMIN_TIER_PRICES = {
  all: {
    stripePriceId:
      process.env.STRIPE_PRO_PRICE_ID ||
      "price_1T6hh82ceUNmv10tdIMnFF5N",
  },
  elite: {
    stripePriceId:
      process.env.STRIPE_ALL_SPORTS_PRICE_ID ||
      "price_1T6hh92ceUNmv10tShQlLUYt",
  },
};

---

## STEP 6 — REMOVE OLD PRICE IDS (MANDATORY)

Delete ALL occurrences of:

price_1T6fl12cW8Vmrgt3B6ffBIuw
price_1T6fly2cW8Vmrgt3WU9uHL7L

---

## STEP 7 — STARTUP LOGS

Inside registerStripeRoutes:

console.log("[stripe] Pro price ID:", process.env.STRIPE_PRO_PRICE_ID || "(fallback)");
console.log("[stripe] All Sports price ID:", process.env.STRIPE_ALL_SPORTS_PRICE_ID || "(fallback)");

---

## STEP 8 — DO NOT TOUCH

- server/stripeClient.ts
- Checkout session logic (except priceId)
- Frontend plan values ("all", "elite")

---

## DONE CRITERIA

✔ No invalid price IDs remain  
✔ PLAN_META uses env vars  
✔ ADMIN_TIER_PRICES uses env vars  
✔ Invalid plan no longer crashes server  
✔ Checkout returns Stripe session URL  
✔ Clicking upgrade opens Stripe checkout  
✔ No HTTP 500 errors  

---

## FAILURE PROTECTION (IMPORTANT)

If Stripe fails:
- System returns clean JSON error
- Does NOT crash server
- Logs exact issue for debugging

---

## RESULT

Checkout becomes:
- Deterministic
- Safe
- Production-ready

Users can now complete payments without failure.
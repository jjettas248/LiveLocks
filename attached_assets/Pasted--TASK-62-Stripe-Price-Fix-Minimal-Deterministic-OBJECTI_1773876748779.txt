# TASK #62 — Stripe Price Fix (Minimal + Deterministic)

## OBJECTIVE
Fix "No such price" Stripe error by replacing invalid hardcoded price IDs with correct live IDs via environment variables.

---

## STEP 1 — SET ENV SECRETS

Add:

STRIPE_PRO_PRICE_ID = price_1T6hh82ceUNmv10tdIMnFF5N
STRIPE_ALL_SPORTS_PRICE_ID = price_1T6hh92ceUNmv10tShQlLUYt

---

## STEP 2 — UPDATE server/stripeService.ts

Replace ONLY the priceId fields inside PLAN_META:

const PLAN_META = {
  all: {
    priceId:
      process.env.STRIPE_PRO_PRICE_ID ??
      "price_1T6hh82ceUNmv10tdIMnFF5N",
  },
  elite: {
    priceId:
      process.env.STRIPE_ALL_SPORTS_PRICE_ID ??
      "price_1T6hh92ceUNmv10tShQlLUYt",
  },
};

---

## STEP 3 — UPDATE server/routes.ts

Replace ONLY stripePriceId values:

const ADMIN_TIER_PRICES = {
  all: {
    stripePriceId:
      process.env.STRIPE_PRO_PRICE_ID ??
      "price_1T6hh82ceUNmv10tdIMnFF5N",
  },
  elite: {
    stripePriceId:
      process.env.STRIPE_ALL_SPORTS_PRICE_ID ??
      "price_1T6hh92ceUNmv10tShQlLUYt",
  },
};

---

## STEP 4 — REMOVE INVALID PRICE IDS

Delete ALL occurrences of:

price_1T6fl12cW8Vmrgt3B6ffBIuw
price_1T6fly2cW8Vmrgt3WU9uHL7L

No exceptions.

---

## STEP 5 — ADD STARTUP LOG (ONE LINE EACH)

Inside registerStripeRoutes:

console.log("[stripe] Pro:", process.env.STRIPE_PRO_PRICE_ID ?? "fallback");
console.log("[stripe] All Sports:", process.env.STRIPE_ALL_SPORTS_PRICE_ID ?? "fallback");

---

## DO NOT CHANGE

- server/stripeClient.ts
- Checkout session creation logic
- Frontend plan values ("all", "elite")
- Any unrelated code

---

## DONE CRITERIA

✔ No invalid price IDs exist anywhere  
✔ PLAN_META uses env vars  
✔ ADMIN_TIER_PRICES uses env vars  
✔ Server logs show correct price IDs  
✔ Clicking upgrade opens Stripe checkout  
✔ No HTTP 500 errors  

---

## RESULT

Stripe receives valid price IDs → checkout succeeds → payments enabled
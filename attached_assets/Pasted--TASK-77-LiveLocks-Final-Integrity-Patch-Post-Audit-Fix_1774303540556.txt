# TASK: #77 - LiveLocks Final Integrity Patch (Post-Audit Fix)

## SYSTEM:
Core Engine + Data Integrity + Odds Pipeline + UX Contract

## OBJECTIVE:
Resolve all blocking issues from Task #76 by enforcing strict data integrity, eliminating ESPN contamination, standardizing degraded-state semantics, and completing missing system-wide coverage.

This task must result in a production-safe, market-driven system.

---

## CRITICAL RULES:
- No ESPN-derived line, spread, total, or odds may enter engine input
- No synthetic or default lines may influence edge calculations
- `isDegraded` must ONLY represent stale odds cache usage
- If no compliant data exists → skip or mark unavailable (never fabricate)
- Backend correctness must be completed before UI adjustments

---

## PHASE A — REMOVE ESPN FROM ENGINE INPUTS (BLOCKING)

### Problem:
NCAAB pipeline still uses:
- ESPN `overUnder`
- ESPN `spreadDetails`

### Fix:

In `server/ncaabService.ts`:

REMOVE:
- any assignment of `total`, `spread`, `favorite` from ESPN

REPLACE WITH:

```ts
if (!oddsServiceDataAvailable) {
  logSkip("No compliant odds source")
  continue
}
Behavior:

If odds service fails AND no cache exists:
→ skip market
→ do NOT fallback to ESPN

Acceptance:

Engine input contains ZERO ESPN-derived line values

All lines originate from oddsService or its cache

PHASE B — FIX DEGRADED CONTRACT (BLOCKING)
Problem:

isDegraded currently represents multiple meanings

Fix:

Define contract:

isDegraded === true ONLY IF:
  odds source = lastKnownOdds cache (stale real data)
REMOVE:

any isDegraded assignment from:

ESPN fallback

missing data cases

ADD:

dataUnavailable: true for:

no odds

skipped markets

UI mapping:
State	Meaning
isDegraded	stale odds
dataUnavailable	no valid market
Acceptance:

isDegraded is consistent across NBA/NCAAB/MLB

UI messaging matches actual data source

PHASE C — REMOVE DEFAULT_BOOK_LINE FROM ENGINE
Problem:

Synthetic fallback corrupts edge calculations

Fix:

Find all usages of:

DEFAULT_BOOK_LINE
Replace with:
if (!bookLine) {
  logSkip("Missing sportsbook line")
  return null
}
Acceptance:

No synthetic line enters engineInput

All plays are based on real market data only

PHASE D — COMPLETE ENGINE GUARD-RAILS (ALL SPORTS)
Implement shared validator:
function validateEngineInput(input) {
  if (!input.line) return "missing line"
  if (!input.odds) return "missing odds"
  if (!input.gameId) return "missing gameId"
  if (input.requiresPlayer && !input.player) return "missing player"
  return null
}
Apply to:

NBA (routes.ts)

NCAAB (ncaabService.ts)

MLB (ensure parity)

Acceptance:

No engine call executes with incomplete data

All skips are logged with explicit reason

PHASE E — MLB STATE NORMALIZATION FIX
Fix:

In normalizeMLBState():

"Pre-Game" → "pregame"
"Preview" → "pregame"
REMOVE:

"preview" from system vocabulary

Acceptance:

Only allowed states:

live

pregame

final

PHASE F — COMPLETE ODDS FALLBACK CONSISTENCY
Ensure in oddsService.ts:

ALL failure paths trigger fallback:

429 quota

network error

malformed payload

empty bookmakers array

if (!bookmakers || bookmakers.length === 0) {
  return lastKnownOdds
}
Acceptance:

No empty arrays returned when cache exists

isDegraded: true always set on fallback

PHASE G — COMPLETE UI CONTRACT (ALL SURFACES)
Requirements:
Banner (system-level)

Show ONLY when:

plays.some(p => p.isDegraded)

Text:

Using last known lines
Empty state

Show when:

plays.length === 0

Text:

No strong edges right now
No banner when:

dataUnavailable only

Apply to:

dashboard.tsx (NBA)

mlb-live.tsx

ncaab-admin-tab.tsx

Acceptance:

consistent UX across all sports

no misleading degraded messaging

PHASE H — ANALYTICS HARD VALIDATION
Verify /api/analytics/summary:

Test cases:

today

7d

30d

empty dataset

invalid range

Add safeguards:
if (total === 0) {
  winRate = 0
  roi = 0
}
Acceptance:

no crashes

no NaN values

correct aggregation

PHASE I — FINAL VERIFICATION

Run 1 test per sport:

NBA

plays generate

degraded fallback works

no ESPN contamination

NCAAB

no ESPN lines in engineInput

skips occur instead of fallback

UI shows correct empty state

MLB

live games pass gating

props hydrate via matchers

no DEFAULT_BOOK_LINE usage

SUCCESS CRITERIA:

ZERO ESPN-derived lines in engine inputs

ZERO synthetic/default lines used in calculations

isDegraded strictly equals stale odds usage

All sports share identical guard-rail logic

UI reflects true system state accurately

MLB live pipeline fully functional and reliable

Analytics endpoint stable and correct

OUTPUT:

Return exactly:

Root causes fixed

Files modified

File-level diffs

Before/after API responses (NBA, NCAAB, MLB)

Proof ESPN is no longer used in engine inputs

Do not summarize.
Do not partially complete.
Do not leave fallback paths ambiguous.


---

# 🧠 Strategic Insight (important)

You’re very close, but this is the key shift:

### ❌ Current system (partially)
- “Always produce output, even if fake or degraded”

### ✅ Correct system
- “Only produce output when data integrity is real”
- otherwise → **skip cleanly + communicate clearly**

That’s what separates:
- a *working app*
vs
- a *trusted product*

---

# Bottom line

Your rejection was correct because:

- ESPN contamination = **invalid system**
- degraded contract confusion = **user trust issue**
- synthetic lines = **bad edge math**

Fix Task #77 and you’ll have:
👉 a **true production-grade betting intelligence engine**

---

If you want next step:
I can audit the **actual diffs Replit sends back** and tell you exactly if it’s finally clean or s
# TASK #93 ADDENDUM: HARD SUCCESS GATE ENFORCEMENT

## SYSTEM:
Core Engine + UX Layer

## OBJECTIVE:
Patch the existing Task #93 implementation so it cannot declare halftime pipeline restoration prematurely.

This is NOT a rewrite.
This is a strict compliance patch to satisfy the gating criteria that the prior diff missed.

---

## ROOT CAUSE:
The current implementation emits:

STATUS: HALFTIME PIPELINE RESTORED — TASK #50 UNBLOCKED

too early, based only on server-side halftime games / plays existing.

That violates the task because restoration must only be declared after ALL six success conditions are satisfied, including:
1. active endpoint confirmed
2. halftime detection confirmed
3. second-half parse status confirmed
4. server response contains generated plays
5. client receives data
6. Quick View visibly renders

The current implementation also has incomplete evidence logging:
- [HT_ENDPOINT_HIT] does not include path/query
- [SECOND_HALF_MARKET_AUDIT] does not include period encoding and validation rejection details

---

## IMPLEMENTATION:

### Step 1: Replace premature RESTORED status logic
In the current server-side status emission logic, REMOVE any condition that declares:

STATUS: HALFTIME PIPELINE RESTORED — TASK #50 UNBLOCKED

based only on:
- halftimeGames > 0
- playsGenerated > 0

That is insufficient.

Replace with this rule:

- The server may only emit a BLOCKED status by default
- RESTORED may only be emitted after explicit client confirmation is supplied through the active request flow OR after a separate verification flag is proven true

Until then, keep:

```ts
console.log("STATUS: HALFTIME PIPELINE STILL BLOCKED — REASON: awaiting client assertion and Quick View confirmation");

Do not declare RESTORED optimistically.

Step 2: Add complete [HT_ENDPOINT_HIT] logging

In the active halftime route, ensure this exact log exists at request entry:

console.log("[HT_ENDPOINT_HIT]", {
  path: req.path,
  query: req.query,
  method: req.method,
  timestamp: Date.now(),
});

This must be in the active route actually used by the client.

Step 3: Expand [SECOND_HALF_MARKET_AUDIT]

In the active 2H parsing / validation path in oddsService.ts, expand the audit log so it includes:

market key / label
period encoding
normalized market type
validation result
validation rejection reason if rejected

Use a structure like:

console.log("[SECOND_HALF_MARKET_AUDIT]", parsedMarkets.map((m) => ({
  gameId: m.gameId,
  marketKey: m.marketKey ?? null,
  marketType: m.marketType ?? null,
  rawPeriod: m.rawPeriod ?? null,
  normalizedPeriod: m.normalizedPeriod ?? null,
  line: m.line ?? null,
  overOddsAmerican: m.overOddsAmerican ?? null,
  underOddsAmerican: m.underOddsAmerican ?? null,
  isValid: m.isValid ?? null,
  rejectionReason: m.rejectionReason ?? null,
})));

If validation happens separately, log rejected candidates explicitly before they are discarded.

Step 4: Add strict response assertion payload

At the final res.json(...) point of the active halftime route, ensure this exact assertion log exists:

console.log("[HT_RESPONSE_ASSERT]", {
  totalGames: games.length,
  halftimeGames: games.filter(g => g.isHalftime).length,
  totalMarkets: parsedMarkets.length,
  secondHalfMarkets: secondHalfMarkets.length,
  playsGenerated: plays.length,
});

This is server truth only. It must NOT be used alone to declare RESTORED.

Step 5: Add / preserve client assertion

In dashboard.tsx, immediately after the active halftime fetch resolves, ensure:

console.log("[HT_CLIENT_ASSERT]", {
  received: data?.length ?? 0,
});

If the response shape is not a bare array, log the actual count from the active field used by the UI.

Step 6: Add Quick View render assertion

In the actual Quick View / live-boxscore render path, add:

console.log("[QUICK_VIEW_RENDER_ASSERT]", {
  renderedCount: renderedPlays?.length ?? 0,
});

If there are intermediate transforms, log the final rendered dataset only here.

This assertion is required to satisfy the sixth success condition.

Step 7: Enforce the hard success gate centrally

Create one central boolean gate in the active flow, with all six conditions represented explicitly.

Example:

const successGate = {
  endpointHitConfirmed: true,
  halftimeDetected: halftimeGames > 0,
  secondHalfParseConfirmed: secondHalfMarkets > 0 || secondHalfSourceAbsenceConfirmed === true,
  responseHasPlays: plays.length > 0,
  clientReceivedData: false, // must not be assumed true on server
  quickViewRendered: false,  // must not be assumed true on server
};

Server rule:

never set clientReceivedData or quickViewRendered to true on assumption
if those are not verified in the active flow, final status remains BLOCKED

If you cannot bridge client confirmation to server cleanly in this task, then the server must emit:

console.log("STATUS: HALFTIME PIPELINE STILL BLOCKED — REASON: server restored but client/Quick View not yet verified");

That is acceptable and correct.

Step 8: Final status rule

Only emit:

console.log("STATUS: HALFTIME PIPELINE RESTORED — TASK #50 UNBLOCKED");

if and only if all six success conditions are proven.

Otherwise emit one of:

awaiting client assertion
awaiting Quick View confirmation
no valid 2H markets confirmed
endpoint mismatch
halftime detection not yet confirmed

No optimistic success messages.

DO NOT:
Do not rewrite halftime math
Do not touch MLB or NCAAB
Do not weaken the task gate
Do not declare RESTORED from server-side evidence alone
Do not remove any existing useful logs
Do not fabricate client verification
SUCCESS CRITERIA:

The task is only accepted if:

[HT_ENDPOINT_HIT] includes path/query/method/timestamp
[SECOND_HALF_MARKET_AUDIT] includes period encoding + rejection reasons
[HT_RESPONSE_ASSERT] exists in the active route
[HT_CLIENT_ASSERT] exists in the active client fetch path
[QUICK_VIEW_RENDER_ASSERT] exists in the actual render path
STATUS remains BLOCKED unless all six gates are actually verified
OUTPUT:

Return:

exact files changed
exact code diff
final status emission logic
sample log payloads for:
[HT_ENDPOINT_HIT]
[SECOND_HALF_MARKET_AUDIT]
[HT_RESPONSE_ASSERT]
[HT_CLIENT_ASSERT]
[QUICK_VIEW_RENDER_ASSERT]
explicit statement whether Task #50 is truly unblocked or still blocked

---

# What this does

It fixes the three real misses:

## 1. Premature success
You stop falsely unblocking Task #50

## 2. Incomplete endpoint evidence
You prove the client is hitting the route you patched

## 3. Weak 2H audit
You make validation failures visible instead of silent

---

# What to do after this patch

If the result is:

### `STATUS: HALFTIME PIPELINE STILL BLOCKED`
That is fine. It means the task is now honest.

Then you inspect:
- `[HT_CLIENT_ASSERT]`
- `[QUICK_VIEW_RENDER_ASSERT]`

and patch the exact remaining failure.

### `STATUS: HALFTIME PIPELINE RESTORED — TASK #50 UNBLOCKED`
Then Task #93 is truly done, and you move to Task #50.

---

# Bottom line

**Deny the current implementation.**  
Apply the addendum above exactly.
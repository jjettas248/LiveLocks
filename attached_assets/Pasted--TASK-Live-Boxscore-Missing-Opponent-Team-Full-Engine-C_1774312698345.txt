# TASK: Live Boxscore Missing Opponent Team — Full Engine Coverage Fix

## SYSTEM:
Core Engine + Data Pipeline + UX Layer

## OBJECTIVE:
Fix the live boxscore so BOTH teams are fully processed and enriched. Eliminate the current bug where only the first team (top half) receives engineOutput and the opponent team (bottom half) is missing entirely.

---

## CONTEXT:
Current behavior:
- Top team fully enriched (badges render)
- Opponent team has zero enrichment
- Indicates engine is only processing one team

Root cause:
- Player extraction or engine loop is scoped to a single team
- OR early return/break exits before second team processes

This is a DATA PIPELINE bug, not a UI mapping issue.

---

## REQUIREMENTS:

### 1. UNIFY PLAYER EXTRACTION (CRITICAL)

Locate where players are pulled from liveStats / boxscore.

REPLACE any single-team extraction:

```ts
const players = game.homeTeam.players

OR:

const players = boxscore[0].players

WITH:

const players = [
  ...(game.homeTeam?.players || []),
  ...(game.awayTeam?.players || [])
]

OR if using grouped structure:

const players = boxscore.flatMap(team => team.players || [])

This MUST include both teams.

2. REMOVE TEAM-LEVEL LOOP BREAKS / RETURNS

Search entire engine pipeline for:

return
break

inside loops like:

for (const team of teams)
for (const game of games)

REPLACE with:

continue

Ensure loop always processes BOTH teams.

3. GUARANTEE ENGINE RUNS FOR ALL PLAYERS

Final engine loop MUST be:

for (const player of players) {
  if (!engineOutput[player.id]) {
    engineOutput[player.id] = {}
  }

  for (const statType of statTypes) {
    const input = buildEngineInput(player, statType)

    if (!input || input.line == null || !Number.isFinite(input.line)) {
      engineOutput[player.id][statType] = null
      continue
    }

    const result = runEngine(input)

    engineOutput[player.id][statType] = {
      probability: result.probability,
      betDirection: result.betDirection,
      edge: result.edge,
      line: input.line,
      statType
    }
  }
}
4. NEVER SKIP PLAYER — ONLY SKIP STAT

DO NOT do:

if (!validInput) continue // ❌ skips entire player

Instead:

if (!validInput) {
  engineOutput[player.id][statType] = null
  continue
}
5. FORCE FULL PLAYER COVERAGE (SAFETY PASS)

After engine loop:

for (const player of players) {
  if (!engineOutput[player.id]) {
    engineOutput[player.id] = {}
  }
}
6. FRONTEND SAFE LOOKUP (NO HARD FAILS)

In live-boxscore.tsx:

const engineEntry =
  engineOutput?.[stat.playerId]?.[watchedStatType] ?? null

DO NOT allow undefined access to break rendering.

7. STRICT BADGE ELIGIBILITY (KEEP FROM PRIOR FIX)
const minutes =
  typeof stat.minutes === "number"
    ? stat.minutes
    : parseFloat(stat.minutes)

const shouldRenderBadge =
  engineEntry && minutes >= 3
8. DEBUG VALIDATION (REQUIRED)

Add temporarily:

console.log("[boxscore] total players:", players.length)
console.log("[boxscore] engineOutput players:", Object.keys(engineOutput).length)
console.log(
  "[boxscore] teams:",
  [...new Set(players.map(p => p.team))]
)

Expected:

player counts match
BOTH teams present
9. RESPONSE SHAPE (DO NOT BREAK)

Ensure backend still returns:

{
  signals: Signal[],
  engineOutput: Record<number, Record<string, EngineEntry>>
}
signals[] remains filtered (edge >= threshold)
engineOutput is FULLY unfiltered
DO NOT:
Do not modify probability calculations
Do not filter by team
Do not reintroduce name-based matching
Do not change highlight thresholds
Do not break halftime plays panel
Do not remove fallback logic
SUCCESS CRITERIA:
BOTH teams fully enriched in live boxscore
Top and bottom halves display badges correctly
engineOutput contains ALL playerIds
No missing opponent players
No regression in plays panel or UI behavior
Debug logs confirm both teams present
OUTPUT:
Full multi-team player processing
No early loop exits
Complete engineOutput coverage
Stable frontend rendering

---

## Bottom line

Your system right now is:
> **processing one team correctly instead of both teams incorrectly**

This prompt fixes:
- player extraction
- loop scope
- engine coverage
- and guards against regression

Once applied, the **entire bottom half (opponent team)** will populate immediately.
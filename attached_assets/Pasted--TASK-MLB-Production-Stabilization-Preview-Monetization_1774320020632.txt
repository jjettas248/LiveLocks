# TASK: MLB Production Stabilization + Preview Monetization + Premium UI Overhaul

## SYSTEM:
Core Engine + UX + Conversion

## OBJECTIVE:
1. Eliminate ALL fake MLB signals (18% bug, fallback logic)
2. Enforce strict real-data-only signal generation
3. Implement Preview + Locked Monetization (Free + Pro users)
4. Keep MLB full functionality exclusive to All Sports users
5. Upgrade MLB UI to NBA-level quality
6. Redesign Today’s Games chips to clearly show matchups and feel premium

--------------------------------------------------
## PHASE 1 — PRODUCT INTEGRITY (NON-NEGOTIABLE)
--------------------------------------------------

### 1. Remove ALL fallback probability logic
Search and delete:
- ?? 18
- || 18
- any hardcoded default probability

RULE:
- Probability must NEVER be fabricated
- If inputs are invalid → skip player

---

### 2. Strict signal validation (CRITICAL)

Inside MLB signal generation:

const hasValidLine = line !== null && line !== undefined && line > 0
const hasValidStats = playerContext?.currentStats != null

if (!hasValidLine || !hasValidStats) {
  return null
}

Filter null signals before returning.

FINAL RULE:
- NO line → NO signal
- NO stats → NO signal
- NO partial signals EVER

REMOVE:
- fallback lines (0.5)
- static edges
- default probabilities

---

### 3. Player context isolation

Each player must have a fresh:

{
  playerId,
  gameId,
  battingOrderSlot,
  currentStats,
  inning,
  isTopInning,
  pitcherContext,
  parkFactor
}

NO shared objects  
NO mutation across players  

---

### 4. Probability / odds separation

Correct flow:

- compute probability (engine only)
- compute edge ONLY if odds exist
- odds nullable
- edge nullable

INVALID:
if (!odds) return fallbackProbability

---

### 5. Live gating (strict)

Normalize status:

function normalizeMlbStatus(raw) {
  const s = (raw ?? "").trim().toLowerCase().replace(/\s+/g, "_");
  if (s === "live" || s === "in_progress" || s === "inprogress") return "live";
  if (s === "preview" || s === "scheduled") return "preview";
  if (s === "final" || s === "completed") return "final";
  return "unknown";
}

Only allow signals when:
normalizedStatus === "live"

Else:
return []

NO cache bypass  
NO loose gating  

---

### 6. Signal integrity filter

Drop signals if:
- playerId missing
- probability invalid
- probability duplicated excessively
- game mismatch

If probability cluster detected:
return []

---

### 7. Debug logging (REQUIRED)

Log per player:

{
  playerId,
  playerContext: {
    gameId,
    battingOrderSlot,
    inning,
    isTopInning,
    currentStats,
    pitcherContext,
    parkFactor
  },
  probability
}

NO output-based logs  
ONLY engine input logs  

---

--------------------------------------------------
## PHASE 2 — PREVIEW + PAYWALL SYSTEM
--------------------------------------------------

### 8. Tier-based response

MLB FULL ACCESS = ONLY "elite" (All Sports)

If NOT elite:
→ NEVER return real signals

---

### 9. Preview Mode (pre-odds)

Detect:

const isPreviewMode = !games.some(g => g.hasOdds === true)

Return:

{
  mode: "preview",
  games,
  players: [...]
}

NO:
- probability
- edge
- bet recommendation

---

### 10. Locked Preview (Free + Pro users)

Return:

{
  mode: "preview_locked",
  games,
  previewPlayers: players.slice(0, 6)
}

ONLY:
- name
- matchup
- projection
- tags

DO NOT include:
- probability
- edge
- bet

---

### 11. UI Mode Split

File: mlb-live.tsx

IF mode !== "live":
→ DO NOT render Edge Signals

Render:

HEADER:
"Projected Opportunities"

CARD:
- Player
- Matchup
- Projection
- Tags

LOCKED:
Probability 🔒
Edge 🔒

CTA:
"Unlock MLB Edges"

---

### 12. Real Signal Mode (elite only)

Render ONLY IF:

mode === "live"
AND user is elite
AND signal has:
- real line
- real probability
- real edge

---

### 13. Empty State

"No live edges yet.
Lines are still forming.

All Sports users get alerted instantly."

CTA: Upgrade

---

--------------------------------------------------
## PHASE 3 — GAME CHIP UI REDESIGN (CRITICAL)
--------------------------------------------------

File: mlb-live.tsx

REPLACE existing chips completely.

---

### NEW STRUCTURE

TOP ROW:
AWAY_ABBR @ HOME_ABBR

Example:
NYY @ BOS

Right:
LIVE or PRE badge

---

SECOND ROW:
Score

3 – 2

Below:
Top 7th / Bottom 5th / Preview

---

THIRD ROW:
Context pills:

Fenway • Cole vs Sale • 72°, Wind Out

---

### STYLE

- flex column
- p-3
- rounded-xl
- border-white/10
- subtle elevated background
- hover highlight

---

### LAYOUT

- 2–3 per row desktop
- wrap naturally
- no horizontal scroll

---

### REMOVE

- Field / Park / Stadium labels
- “May vs Mautz” raw text
- broken "@ 0-0 Preview" strings

---

### REQUIRED BACKEND SHAPE

{
  gameId,
  awayAbbr,
  homeAbbr,
  awayScore,
  homeScore,
  inning,
  isTopInning,
  status,
  parkName,
  probableAwayPitcher,
  probableHomePitcher,
  weatherSummary
}

Frontend must ONLY use this.

---

--------------------------------------------------
## PHASE 4 — PREMIUM UX COMPLETION
--------------------------------------------------

### 14. No auto-selection
- selectedGameId = null on load
- show only game chips initially

---

### 15. Reset state on game switch
Reset:
- player
- calc result
- market
- line

---

### 16. Matchup card upgrade

Include:

- header (player + matchup)
- live stats
- current form
- pitcher context
- park/weather
- market selector
- sportsbook + manual entry
- prediction output

---

### 17. Market support

BATTERS:
- hits
- total_bases
- hrr
- batter_k
- home_runs

PITCHERS:
- pitcher_k
- walks_allowed
- hits_allowed

---

### 18. Manual fallback

If no odds:
- show manual inputs
- allow calculate

Required:
- line
- odds

---

### 19. Calculate flow

POST /api/mlb/calculate

Return result immediately.

---

--------------------------------------------------
## DO NOT TOUCH
--------------------------------------------------

DO NOT:
- modify engine math
- fabricate probabilities
- guess lines
- expose real edges to non-elite users
- add AI-style comments

---

--------------------------------------------------
## SUCCESS CRITERIA
--------------------------------------------------

PRE-OPENING DAY:
- ZERO edge signals
- clean preview UI
- no fake probabilities

POST-OPENING DAY:
- signals only with real odds
- no 18% clustering
- premium UI parity with NBA

FREE / PRO:
- see preview only
- understand value gap

ALL SPORTS:
- full MLB system

---

## OUTPUT:
- Minimal diffs
- Clean UI
- No placeholder data
- Production-ready
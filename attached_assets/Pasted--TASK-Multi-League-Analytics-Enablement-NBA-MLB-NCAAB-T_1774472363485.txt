# TASK: Multi-League Analytics Enablement (NBA + MLB + NCAAB Toggle + Data Segmentation)

## SYSTEM:
Analytics (PRIMARY) + UX Layer (SECONDARY)

## OBJECTIVE:
Upgrade the Analytics system from NBA-only to fully multi-league (NBA, MLB, NCAAB) by introducing a league selector that:

- Dynamically filters all analytics metrics by league
- Ensures backend queries are league-aware
- Maintains a single unified analytics system (no duplication)
- Enables future cross-league comparisons without refactor

This is NOT a UI-only task. This requires full pipeline alignment (UI → API → storage).

---

## ROOT PROBLEM:

Analytics is currently hardcoded to NBA:
- Title shows "NBA Live 2H Plays"
- Backend queries return NBA-only data
- No league state exists in frontend
- No league filter exists in backend endpoints

Result:
- MLB and NCAAB analytics are invisible
- Model performance cannot be evaluated across leagues
- System is not scalable

---

## REQUIREMENTS:

--------------------------------------------------
### PHASE 1 — ADD LEAGUE STATE (FRONTEND)
--------------------------------------------------

File: client/src/pages/analytics.tsx

Add league state:

```ts
const [selectedLeague, setSelectedLeague] = useState<"NBA" | "MLB" | "NCAAB">("NBA");
PHASE 2 — ADD LEAGUE TOGGLE UI

Place directly above "Model Performance" section.

<div className="flex gap-2 mb-6">
  {["NBA", "MLB", "NCAAB"].map((league) => (
    <button
      key={league}
      onClick={() => setSelectedLeague(league as any)}
      className={`px-4 py-2 rounded-lg font-medium transition ${
        selectedLeague === league
          ? "bg-blue-600 text-white"
          : "bg-gray-800 text-gray-300 hover:bg-gray-700"
      }`}
    >
      {league}
    </button>
  ))}
</div>
PHASE 3 — MAKE ALL DATA FETCHES LEAGUE-AWARE

Replace ALL analytics fetch calls.

BEFORE:

fetch("/api/analytics")

AFTER:

fetch(`/api/analytics?league=${selectedLeague}`)

Ensure:

useEffect dependency includes selectedLeague
data refetches on toggle change
PHASE 4 — UPDATE BACKEND ANALYTICS ROUTE

File: server/routes.ts

Locate analytics endpoint.

REPLACE:

const plays = await getSettledPlays("NBA");

WITH:

const league = req.query.league || "NBA";

const plays = await getSettledPlays(league);
PHASE 5 — ENFORCE LEAGUE FILTERING IN STORAGE

File: server/storage.ts

Ensure:

getSettledPlays(league: string)

filters correctly:

return db.plays.filter(p => p.league === league);

DO NOT:

fallback to mixed league data
merge leagues in memory
PHASE 6 — VERIFY DATA INTEGRITY

Ensure all stored plays include:

{
  league: "NBA" | "MLB" | "NCAAB"
}

If missing:

TRACE ingestion pipeline and enforce:

league: game.sport === "MLB" ? "MLB" :
        game.sport === "NCAAB" ? "NCAAB" :
        "NBA"

This must exist BEFORE analytics queries.

PHASE 7 — DYNAMIC TITLE UPDATE

Replace static title:

"NBA Live 2H Plays"

WITH:

`${selectedLeague} Live Plays — settled automatically from final box scores`
PHASE 8 — ENSURE FULL METRIC RECOMPUTATION

All of the following must update when league changes:

Win rate
ROI
Confidence buckets (60–69, 70–79, etc.)
Total settled
Recent plays table

NO cached NBA values allowed.

PHASE 9 — ADD SAFETY GUARD

If no data exists:

if (!plays.length) {
  return {
    total: 0,
    message: "No data available for selected league"
  };
}

Frontend must render empty state cleanly.

DO NOT:
Create separate analytics systems per league
Hardcode league values anywhere
Merge multiple leagues into a single dataset
Break existing NBA analytics behavior
Duplicate endpoints
SUCCESS CRITERIA:
Toggle between NBA / MLB / NCAAB updates analytics instantly
Each league shows correct win rate, ROI, totals
Recent plays table reflects selected league only
No cross-league contamination
No empty MLB unless truly no data exists
System supports adding new leagues without refactor
OUTPUT:
Exact code changes
File-level diffs
No placeholders
No pseudo code
POST-IMPLEMENT VALIDATION:
NBA numbers remain unchanged from baseline
MLB shows independent metrics (not mirroring NBA)
Switching leagues causes visible metric changes
Console logs confirm correct league parameter passing

---

# Why this is a 10/10 prompt

This is not just “add a toggle.”

It enforces:
- **Full data pipeline integrity**
- **Future scalability (multi-sport)**
- **No duplication**
- **No hidden bugs (cross-league bleed)**
- **Consistent architecture**

---

# What this unlocks immediately

- You can validate MLB model performance
- You can compare leagues
- You can market results per sport
- You can expand to NFL later without redesign

---

If you want next level after this:

👉 I can give you a **“League Performance Comparison Dashboard”** (side-by-side ROI, hit rate, volume) — that’s where this becomes a real edge.
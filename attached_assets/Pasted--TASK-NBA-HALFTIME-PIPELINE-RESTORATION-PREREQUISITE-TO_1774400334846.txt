# TASK: NBA HALFTIME PIPELINE RESTORATION (PREREQUISITE TO TASK #50)

## SYSTEM:
Core Engine + UX Layer

## OBJECTIVE:
Restore the active halftime pipeline so the system can:
1. correctly detect games at halftime
2. ingest or fall back for second-half lines
3. repopulate 2H Plays and Quick View

This task must be completed BEFORE Task #50 is used again.

---

## ROOT CAUSE:
The current problem is not primarily display-contract drift. The system is failing earlier in the pipeline.

Evidence:
- UI shows "0 Games at Halftime"
- UI shows no second-half lines
- Quick View is not functioning

This indicates one or more precondition failures:
A. halftime game-state detection no longer matches live feed shape
B. second-half market ingestion/parsing is failing
C. UI is hard-gated on halftime/2H-line presence and is returning empty

Task #50 should not be re-run until halftime data exists again.

---

## IMPLEMENTATION:

### Step 1: Identify the active halftime data path
Document the exact client file and endpoint used for:
- 2H Plays
- halftime game count
- live box score quick view

Return:
- client file(s)
- endpoint URL(s)
- hook/function names

---

### Step 2: Instrument active server response for game-state visibility
In the actual active server route for halftime/live game data, add:

```ts
console.log("[GAME_STATE_AUDIT]", games.map((g) => ({
  gameId: g.id,
  homeTeam: g.homeTeam,
  awayTeam: g.awayTeam,
  period: g.period,
  clock: g.clock,
  displayClock: g.displayClock,
  status: g.status,
  gameStatus: g.gameStatus,
  isLive: g.isLive,
  isHalftime: g.isHalftime,
})));

Goal:
Prove whether the live feed is delivering halftime in a different shape than the current detection logic expects.

Step 3: Fix halftime detection in the ACTIVE path only

Audit the current halftime detection logic and make it tolerant to the actual live data shape.

Support all plausible halftime conditions from the live feed, including patterns like:

period === 2 and clock at 0 / 0.0 / 00:00
explicit status/gameStatus/display text containing HALFTIME
period transition states where halftime is represented before Q3 begins

Do NOT hardcode only one narrow condition.
Do NOT patch dead code paths.
Patch only the active route/path used by the UI.

After patching, add:

console.log("[HALFTIME_DETECTION_RESULT]", {
  totalGames: games.length,
  halftimeGames: games.filter((g) => g.isHalftime).length,
  ids: games.filter((g) => g.isHalftime).map((g) => g.id),
});
Step 4: Audit second-half line ingestion

In the exact path that builds 2H Plays, inspect the active sportsbook market parsing.

Search for:

2H
secondHalf
halftime
3rd quarter / second half market mapping
marketType / period filters
line validation that may be rejecting 2H markets

Add an audit log:

console.log("[SECOND_HALF_MARKET_AUDIT]", parsedMarkets.map((m) => ({
  gameId: m.gameId,
  marketType: m.marketType,
  period: m.period,
  line: m.line,
  overOdds: m.overOddsAmerican,
  underOdds: m.underOddsAmerican,
})));

Goal:
Determine whether:

no 2H markets are arriving
2H markets are arriving but being filtered out
2H markets are arriving but validation rejects them
Step 5: Restore 2H market matching / parsing

Patch only the proven failure point:

if naming mismatch: update the parser to accept the actual market labels returned
if period mismatch: support the actual period encoding returned
if validation is too strict: loosen only the invalid rejection that is blocking real 2H lines

Do NOT fabricate lines.
Do NOT change global market validation without proof.

After patching, log:

console.log("[SECOND_HALF_MARKET_RESULT]", {
  totalParsed: parsedMarkets.length,
  totalSecondHalfEligible: secondHalfMarkets.length,
  gamesWithSecondHalfMarkets: [...new Set(secondHalfMarkets.map((m) => m.gameId))].length,
});
Step 6: Unblock Quick View if it is hard-gated on empty 2H state

Inspect the Quick View / live box score path for hard gates like:

return [] if no halftime games
return [] if no 2H lines
return null if !isHalftime
suppress rendering when a derived dataset is empty upstream

If Quick View is incorrectly coupled to 2H market presence, decouple it.
Quick View should still function from active live game / box score data even if 2H plays are unavailable.

Add logs:

console.log("[QUICK_VIEW_SOURCE_AUDIT]", {
  inputCount: sourcePlays.length,
  renderedCount: renderedPlays.length,
});
Step 7: Validate restoration before returning to Task #50

Confirm all of the following:

UI now shows at least one halftime game when appropriate
2H Plays no longer shows "0 Games at Halftime" during actual halftime
second-half lines are parsed or correctly reported as absent from source
Quick View renders again
Only after this is restored, determine whether Task #50 is still needed
DO NOT:
Do NOT rerun Task #50 as the primary task
Do NOT rewrite NBA probability math
Do NOT touch MLB or NCAAB
Do NOT add fake lines
Do NOT force display output when upstream data is invalid
Do NOT patch inactive routes
SUCCESS CRITERIA:
Halftime detection works in the active pipeline
2H line ingestion works or is accurately diagnosed at source level
Quick View is no longer empty due to upstream gating
The app returns to a valid operating state
Only then should Task #50 be used for distribution/bias reconciliation
OUTPUT:

Return:

exact active client endpoint(s)
exact active server route(s)
[GAME_STATE_AUDIT] output
[HALFTIME_DETECTION_RESULT] output
[SECOND_HALF_MARKET_AUDIT] output
[SECOND_HALF_MARKET_RESULT] output
[QUICK_VIEW_SOURCE_AUDIT] output
exact minimal code diff applied
explicit statement: "Task #50 is now unblocked" or "Task #50 remains blocked because ..."

---

# Decision rule

Use this sequence:

## First:
Run the **Halftime Pipeline Restoration** task above.

## Then:
Only if halftime data is flowing again, run Task #50.

---

# Practical next step

Do **not** approve Task #50 as the current task.  
Approve the **prerequisite restoration task** first.
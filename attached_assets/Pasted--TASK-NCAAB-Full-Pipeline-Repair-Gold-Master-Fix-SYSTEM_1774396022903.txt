# TASK: NCAAB Full Pipeline Repair (Gold Master Fix)

## SYSTEM:
Core Engine

## OBJECTIVE:
Fully restore NCAAB end-to-end so that:
- sportsbook data is fully captured (H1, H2, Full)
- engineInput is always structurally valid
- engine produces consistent markets
- UI never renders empty during live games
- no reliance on fallback hacks or projection-only patches

---

## CONTEXT:

Current failure:
- Live games exist but no plays render
- Top plays card missing
- "No strong edges" shown incorrectly

Root cause chain:
1) extractLines incomplete (loop break + key mismatch)
2) partial hydration (line without odds, odds without line)
3) overwrite behavior across bookmakers
4) engine receives invalid inputs
5) buildMarket() returns unavailableMarket()
6) zero plays → UI empty

---

## REQUIREMENTS

### 1. FULL BOOKMAKER ITERATION (MANDATORY)

In extractLines:

- REMOVE any break statements in bookmaker loop
- ALL bookmakers must be scanned every time

---

### 2. FLEXIBLE MARKET KEY MATCHING

Implement helpers:

function isH2TotalKey(key)
function isH2SpreadKey(key)

Rules:
- normalizeMarketKey(key)
- match:
  - "h2"
  - "2h"
  - "second_half"
  - "2nd_half"

Replace ALL strict checks:
m.key === "h2_totals"
m.key === "h2_spreads"

---

### 3. FIRST-VALID-WINS PER MARKET (CRITICAL)

For EACH market independently:

Markets:
- full_total
- full_spread
- h1_total
- h1_spread
- h2_total
- h2_spread

RULE:

IF field is null → assign  
ELSE → NEVER overwrite

Example:

if (h2TotalLine === null && detectedLine) {
  h2TotalLine = line
  h2OverOddsAmerican = odds
}

---

### 4. ATOMIC LINE + ODDS HYDRATION (CRITICAL)

DO NOT allow partial markets.

RULES:

A) If line is assigned → try to assign odds from same bookmaker  
B) If odds missing → allow later bookmaker to fill odds ONLY  
C) NEVER mix mismatched lines and odds incorrectly  

VALID STATES:
- line + odds → valid
- line + fallback odds (-110) → valid

INVALID:
- odds without line → ignore
- line overwritten → forbidden

---

### 5. ODDS FALLBACK (PERMANENT, NOT TEMP)

Before engine:

IF line exists AND odds null:
→ odds = -110

Apply to:
- full
- H1
- H2

This is REQUIRED for system stability.

---

### 6. STRICT ENGINE INPUT CONTRACT

Before runNCAABEngine:

FOR EACH MARKET:

IF line == null:
→ market unavailable

IF line exists:
→ guarantee odds exists (real OR fallback)

DO NOT allow:
- line null with odds
- odds null with line

---

### 7. REMOVE GLOBAL MARKET FAILURE CASCADE

Current issue:
- H2 missing → entire game dies

FIX:

Each market validated independently.

NEVER do:
if (any market invalid) → kill game

ALWAYS:
only nullify THAT market

---

### 8. ENGINE INPUT GUARANTEE

You must guarantee:

At least ONE of:
- full_total
- full_spread

is valid IF:
- game is live
- ESPN score exists

---

### 9. ESPN-BASED LINE DERIVATION (NOT FALLBACK — CORE)

If sportsbook line is missing:

Derive full_total using pace model:

projectedTotal =
(currentScore / elapsedTime) * totalGameTime

Clamp within realistic bounds.

Set:
totalLine = projectedTotal
odds = -110
source = "derived"

This is NOT optional.
This removes sportsbook dependency.

---

### 10. PRE-ENGINE TRACE (MANDATORY)

Log EXACT engine input:

[NCAAB ENGINE INPUT]
{
  gameId,
  full_total,
  full_spread,
  h1_total,
  h1_spread,
  h2_total,
  h2_spread,
  odds_all,
  source_flags
}

---

### 11. ENGINE OUTPUT VALIDATION

After engine runs:

ASSERT:

markets.length > 0 for live games

IF 0:
→ log hard error:
[NCAAB FAILURE] zero markets generated

---

### 12. UI CONTRACT (NO CHANGE REQUIRED)

UI must ONLY consume:

engineOutput.markets

DO NOT:
- compute anything client-side
- fallback in UI

---

## SUCCESS CRITERIA

- Live game ALWAYS shows plays
- Top plays card ALWAYS renders when games active
- H1/H2 appear when sportsbook provides them
- No false "No strong edges"
- No dependency on sportsbook completeness
- Engine never outputs zero for active games

---

## OUTPUT

- exact code changes
- file-level diffs
- no placeholders
- no partial implementations
# Task: Correct LiveLocks transactional email logic

We need to fix two logic risks in the Resend lifecycle email integration without breaking the existing architecture.

DO NOT change:
- Auth flow structure
- Stripe webhook structure
- Calculate / play access logic
- Email template IDs
- Existing database schema

Only fix the logic described below.

--------------------------------------------------

ISSUE 1 — Subscription tier email mapping

Current behavior may incorrectly map subscription tiers to email templates.

Correct behavior should be:

if tier === "pro"
    sendProWelcomeEmail(user.email)

if tier === "allsports"
    sendAllSportsWelcomeEmail(user.email)

If the system uses legacy tier names:

"all" -> Pro plan
"elite" -> All Sports plan

then map accordingly:

tier === "all"     -> sendProWelcomeEmail
tier === "elite"   -> sendAllSportsWelcomeEmail

Steps:

1. Inspect shared/schema.ts or user model to determine the actual stored tier values.
2. Normalize mapping so the correct welcome email is sent.
3. Ensure only ONE welcome email fires on checkout completion.

Files to inspect:
server/webhookHandlers.ts
server/stripeService.ts
server/routes.ts

Remove any duplicate email triggers so only the webhook handler sends subscription emails.

--------------------------------------------------

ISSUE 2 — Free play wall email off-by-one bug

The wall email must fire exactly when a user reaches 15 used free plays.

Correct behavior:

After incrementPlaysUsed(user.id):

IF the updated playsUsed === 15
THEN sendWallEmail(user.email)

This must only fire once.

Correct implementation example:

const priorPlays = user.playsUsed
await storage.incrementPlaysUsed(user.id)

if (priorPlays + 1 === 15) {
   sendWallEmail(user.email)
}

OR if increment returns updated user:

const updatedUser = await storage.incrementPlaysUsed(user.id)

if (updatedUser.playsUsed === 15) {
   sendWallEmail(user.email)
}

Ensure:

- No duplicate sends
- No send at 14 or 16
- Wrapped in try/catch
- Does not block request

File:
server/auth.ts
requirePlayAccess middleware

--------------------------------------------------

ISSUE 3 — Prevent duplicate subscription emails

Ensure emails are only triggered in ONE location.

Preferred location:

server/webhookHandlers.ts
checkout.session.completed

If subscription activation exists elsewhere (checkout-complete route),
REMOVE duplicate sends there.

--------------------------------------------------

ISSUE 4 — Confirm cron queries

Cron job runs daily at 9am ET.

Ensure SQL queries enforce:

NUDGE:
createdAt between 3 and 4 days ago
playsUsed BETWEEN 1 AND 14
subscriptionTier IS NULL

WINBACK:
createdAt between 14 and 15 days ago
playsUsed = 0
subscriptionTier IS NULL

These should send ONCE per user.

File:
server/index.ts

--------------------------------------------------

FINAL VALIDATION

After changes:

1. TypeScript compiles without errors
2. Server starts with no warnings except optional RESEND_API_KEY warning
3. Test route /api/test-email still works
4. Welcome + how-to emails send on signup
5. Wall email fires exactly at play #15
6. Subscription welcome emails map to correct plan
7. Cron queries return correct users

Do NOT change any code unrelated to the email integration.

Return the final diff summary when complete.
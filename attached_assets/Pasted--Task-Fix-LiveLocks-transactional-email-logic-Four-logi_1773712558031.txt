# Task: Fix LiveLocks transactional email logic

Four logic corrections are required in the Resend lifecycle email integration.

IMPORTANT:
Do NOT change:
- auth flow structure
- Stripe webhook structure
- play access logic
- email template IDs
- Resend client configuration
- database schema

Only adjust the email trigger logic.

--------------------------------------------------

ISSUE 1 — Correct subscription tier mapping

Inspect server/webhookHandlers.ts.

The checkout.session.completed handler must map tiers correctly:

tier === "all"      -> sendProWelcomeEmail(user.email)
tier === "elite"    -> sendAllSportsWelcomeEmail(user.email)

If the system stores tiers as:

"pro"
"allsports"

then map accordingly:

tier === "pro"          -> sendProWelcomeEmail
tier === "allsports"    -> sendAllSportsWelcomeEmail

Steps:

1. Inspect how the tier value is stored on the user record.
2. Normalize the mapping so the correct welcome email is sent.
3. Ensure this logic also applies to the upgrade path in:

customer.subscription.updated

Upgrade rule:

previous tier = "all"
new tier = "elite"

Send:

sendAllSportsWelcomeEmail(user.email)

Do NOT resend on renewals.

--------------------------------------------------

ISSUE 2 — Remove duplicate subscription emails

Subscription welcome emails must ONLY fire from:

server/webhookHandlers.ts

Search these files for any other calls:

sendProWelcomeEmail
sendAllSportsWelcomeEmail

Files to inspect:

server/routes.ts
server/stripeService.ts

If any of these functions are called outside webhookHandlers.ts:

REMOVE them.

Only the webhook should trigger subscription welcome emails.

--------------------------------------------------

ISSUE 3 — Fix play wall email to be non-blocking

File:
server/auth.ts

Function:
requirePlayAccess middleware

The current implementation likely uses:

await sendWallEmail(user.email)

This blocks the request.

Change to fire-and-forget.

Correct implementation:

try {
  sendWallEmail(user.email).catch(() => {});
} catch {}

The trigger condition must remain:

if (user.playsUsed + 1 === 15)

This ensures the wall email fires exactly when the user reaches play #15.

Confirm it never fires:

at play #14
at play #16

--------------------------------------------------

ISSUE 4 — Verify cron query date windows

File:
server/index.ts

Cron runs:

0 9 * * * (America/New_York)

Verify two queries.

NUDGE USERS:

createdAt BETWEEN
(now - 4 days)
and
(now - 3 days)

AND

playsUsed BETWEEN 1 AND 14

AND

subscriptionTier IS NULL

WINBACK USERS:

createdAt BETWEEN
(now - 15 days)
and
(now - 14 days)

AND

playsUsed = 0

AND

subscriptionTier IS NULL

Fix any incorrect variable names or date calculations.

Ensure the SQL query enforces the play count bounds (not just JS filtering).

--------------------------------------------------

VALIDATION

After changes:

1. TypeScript compiles with no errors.
2. Server starts successfully.
3. Only acceptable warning is optional RESEND_API_KEY.
4. /api/test-email route still works.
5. Signup sends welcome + how-to emails.
6. Wall email fires exactly on play #15.
7. Subscription welcome emails fire once from the webhook.
8. Cron queries correctly identify nudge and winback users.

Do not change any unrelated code.

Return a summary of the final changes.
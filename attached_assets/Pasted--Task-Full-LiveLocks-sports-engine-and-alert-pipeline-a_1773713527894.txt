# Task: Full LiveLocks sports engine and alert pipeline audit

Perform a deep system audit of the sports engines and alert pipeline used by LiveLocks.

Do NOT modify code yet. First analyze and report.

The audit must verify end-to-end correctness of:

• NBA engine
• NCAAB engine
• MLB engine
• data ingestion
• edge detection
• alert generation
• edge caching
• frontend rendering

Return a full report of working systems, broken pipelines, and risks.

--------------------------------------------------

SECTION 1 — DATA INGESTION

Inspect services responsible for pulling live game data.

Possible sources:

• ESPN feeds
• Odds API
• sportsbook parsers

Verify:

1. ESPN scoreboard data successfully loads.
2. Game status fields (period, inning, clock, etc.) parse correctly.
3. No sport uses a mismatched feed structure.
4. Player stats used by engines exist in the feed.

Check that ingestion services return:

• teams
• scores
• game clock
• player stats
• current odds

Report any fields referenced in engines that do not exist in the feed.

--------------------------------------------------

SECTION 2 — NBA ENGINE AUDIT

Inspect files under:

server/nba/

Verify engine logic components:

• halftime detection
• tempo multiplier
• market anchoring
• probability calibration
• edge threshold detection

Confirm halftime detection works for all formats including:

period = 2 AND clock = "0.0"

Ensure these games trigger halftime calculations.

Verify probability values remain in valid range:

2% – 98%

Confirm edge detection:

edge = finalMean - liveLine

Check engine outputs:

• probability
• projection
• recommended side
• edge strength

Confirm NBA alerts are generated when thresholds are exceeded.

--------------------------------------------------

SECTION 3 — NCAAB ENGINE AUDIT

Inspect files under:

server/ncaab/

Verify H1 market parsing logic.

Check normalization functions:

normalizeMarketKey
isH1TotalKey
isH1SpreadKey

Confirm the system correctly matches sportsbook odds to:

• H1 totals
• H1 spreads

Verify price extraction handles both:

price
odds

Confirm the system correctly determines:

OVER / UNDER
team spread outcomes

Ensure no synthetic fallback markets are created.

Verify edge detection produces:

• projection
• probability
• recommended bet

--------------------------------------------------

SECTION 4 — MLB ENGINE AUDIT

Inspect files under:

server/mlb/

Verify plate appearance distribution model:

estimatePADistribution()

Ensure the function returns normalized probabilities:

{1: p1, 2: p2, 3: p3}

Verify the engine replaces deterministic PA estimates with distribution weighting.

Check calculation:

binomialOverProbability(paCount, adjustedHitRate, neededHits)

Confirm expectedHits calculation uses weighted distribution.

Validate logic for:

neededHits === 0
Math.ceil(bookLine)

Ensure MLB engine outputs:

• hit probability
• expected hits
• edge

--------------------------------------------------

SECTION 5 — EDGE DETECTION PIPELINE

Trace the pipeline from engine output to alert generation.

Verify steps:

1. Engine produces probability.
2. Edge calculation occurs.
3. Edge passes threshold.
4. Alert object created.
5. Alert added to edge cache.
6. Alert returned through API.

Confirm no step silently drops signals.

Check that edge thresholds are consistent across sports.

--------------------------------------------------

SECTION 6 — EDGE CACHE SYSTEM

Inspect:

edgeCache
mlbEdgeCache
liveGameOrchestrator

Verify:

• alerts are stored in cache
• cache resets properly between games
• stale alerts are removed
• cache does not grow indefinitely

Confirm cache keys are unique per game/player/market.

--------------------------------------------------

SECTION 7 — ALERT DELIVERY

Verify alerts reach the frontend.

Trace flow:

engine → cache → API route → frontend request → UI rendering

Inspect server routes that expose alerts.

Confirm:

• endpoints return signals
• response shape matches frontend expectations
• no filtering removes valid alerts

--------------------------------------------------

SECTION 8 — FRONTEND DISPLAY

Inspect:

client/src/pages/

Verify live pages:

• NBA Live
• NCAAB Live
• MLB Live

Confirm the UI correctly renders:

• game header
• player props
• projections
• probabilities
• edge indicators

Ensure:

alerts appear without requiring manual refresh if the system supports polling.

--------------------------------------------------

SECTION 9 — SPORTSBOOK ODDS MATCHING

Verify sportsbook odds ingestion matches engine markets.

Confirm mapping between:

• sportsbook line
• engine market
• player stat category

Check for mismatches such as:

• points vs points+rebounds
• hits vs total bases
• H1 vs full game totals

Ensure engine is comparing correct market lines.

--------------------------------------------------

SECTION 10 — ALERT GENERATION TEST

Simulate engine outputs.

Verify that when a strong edge occurs:

• alert object is created
• alert stored in cache
• alert returned by API
• alert visible in UI

Confirm no silent failure exists in this pipeline.

--------------------------------------------------

SECTION 11 — PERFORMANCE RISKS

Identify potential bottlenecks:

• repeated feed polling
• large player loops
• synchronous calculations in request handlers
• heavy cron tasks

Report any operations that could degrade performance under load.

--------------------------------------------------

SECTION 12 — FINAL REPORT

Return a structured audit report containing:

WORKING SYSTEMS
BROKEN FEATURES
DATA INGESTION ISSUES
ENGINE LOGIC RISKS
ALERT PIPELINE FAILURES
CACHE PROBLEMS
FRONTEND INTEGRATION ISSUES
PERFORMANCE RISKS

Do not modify code yet. Only audit and report.
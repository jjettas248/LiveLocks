1. Undefined data access
games.map(...)

But games is:

undefined
null
or still loading

👉 Desktop sometimes survives this
👉 Mobile Safari hard crashes → black screen

2. State mismatch after your render refactor

You changed:

panelState = PREVIEW | SIGNAL

If any component expects:

game.signals.map(...)

But:

signals === undefined

👉 crash → blank screen

3. Auth / routing mismatch (VERY COMMON with Wouter)

If MLB route is protected and:

auth hasn’t resolved yet
or useAuth() returns null briefly

Then:

if (!user) return null

👉 results in empty render = black screen

🔬 QUICK CONFIRM (DO THIS FIRST)

Open mobile → inspect or use console:

If you see:

TypeError: Cannot read properties of undefined (reading 'map')

👉 That’s your issue.

✅ FIX (MASTER PATCH — APPLY THIS EXACTLY)
1. Guard ALL .map() calls

In mlb-live.tsx:

{games?.length > 0 ? (
  games.map((game) => (
    <GameCard key={game.gameId} game={game} />
  ))
) : (
  <div className="text-center text-gray-400 py-8">
    No games available
  </div>
)}
2. Add HARD loading state

At top of component:

if (isLoading) {
  return (
    <div className="flex items-center justify-center h-screen text-white">
      Loading MLB...
    </div>
  );
}
3. Add NULL safety for signals

Anywhere you use signals:

const signals = game.signals || [];
4. Add global error boundary (CRITICAL)

Create:

class ErrorBoundary extends React.Component {
  state = { hasError: false };

  static getDerivedStateFromError() {
    return { hasError: true };
  }

  render() {
    if (this.state.hasError) {
      return <div className="text-white p-4">Something went wrong.</div>;
    }

    return this.props.children;
  }
}

Wrap MLB page:

<ErrorBoundary>
  <MLBLive />
</ErrorBoundary>

👉 Prevents total blackouts

5. Fix routing race condition (Wouter)

In your router:

if (!user && isLoadingAuth) return null;

❌ BAD → blank screen

Replace with:

if (isLoadingAuth) {
  return <div className="text-white p-4">Loading...</div>;
}
⚠️ SECONDARY POSSIBILITY (LESS LIKELY)
CSS / overflow issue

If you recently added:

height: 100vh;
overflow: hidden;

👉 Mobile Safari can render black

Fix:

min-height: 100vh;
overflow-y: auto;
🧠 Why This Happened NOW

Your recent changes introduced:

stricter render contract
conditional states (PREVIEW / SIGNAL)
decoupled entitlement

👉 That increases chances of:

undefined states
missing arrays
partial objects
🚀 Final Action Plan
Immediate (5 min fix)
Add .map() guards
Add loading state
Add error boundary
Next (15 min hardening)
Ensure all API responses default to:
signals: []
games: []
Final (production safe)
Add NO_DATA state (prevents silent crashes later)
Audit the NBA edge rendering logic so we can verify exactly how edges are ordered and displayed. Do NOT modify any code. Only inspect and report.

Context:
The NBA dashboard renders edges from `groupFiltered` in `client/src/pages/dashboard.tsx`. We need to confirm how the list is ordered and what data fields are shown on each edge card before making any UI adjustments.

Tasks:

1. Locate where `groupFiltered` is defined
   File: `client/src/pages/dashboard.tsx`
   Identify the exact line where:

   ```js
   const groupFiltered = filterByBook(group.plays.filter(filterPlay), nbaBookFilter);
   ```

   appears.

2. Determine whether the edges are sorted after filtering
   Check for any `.sort()` operations applied to:

   * `group.plays`
   * `groupFiltered`
   * the array passed into `groupFiltered.map()`

   If sorting exists, report:

   * the exact `.sort()` code
   * the field used (edge, probability, projection, etc.)
   * whether it sorts ascending or descending

3. Identify the order of operations before rendering
   Trace the sequence from:

   * raw engine output (`group.plays`)
   * filtering (`filterPlay`)
   * sportsbook filtering (`filterByBook`)
   * any sorting
   * rendering via `groupFiltered.map()`

4. Extract the full edge card render block
   Copy the entire code block for:

   ```js
   groupFiltered.map((play, idx) => {
   ```

   through the closing JSX of the card component.

5. List all fields displayed in the card UI
   For each edge card, list which properties from `play` are rendered.
   Example fields might include:

   * `play.playerName`
   * `play.team`
   * `play.opponent`
   * `play.line`
   * `play.betDirection`
   * `play.probability`
   * `play.expectedTotal`
   * `play.edge`
   * `play.lineSource`

6. Confirm the meaning of `play.edge`
   Locate where `play.edge` is calculated or passed from the API.
   Report whether it represents:

   * probability delta vs sportsbook
   * EV %
   * projection delta
   * another metric

7. Report whether the render order matches the engine output order
   Confirm if the first cards rendered are simply the order received from the backend.

Output format:
Provide a short report with:

* The exact sorting logic (or confirmation that none exists)
* The exact JSX block used to render edge cards
* The list of displayed `play` fields
* The meaning of `play.edge`
* Confirmation of whether cards appear in engine order or sorted order

Important:
Do NOT change any code. Only inspect and report.

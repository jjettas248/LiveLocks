Before approving Task #24, perform a strict implementation audit against the specification.

Do NOT restate the plan. Inspect the actual code and output the relevant snippets proving compliance.

Run these checks:

1. Direction Derivation
Verify calibration direction is derived strictly from model probability:

const direction = probability >= 50 ? "OVER" : "UNDER"

Confirm there is no use of req.direction in the calibration step.

2. Directional Calibration Logic
Confirm calibration operates on directional confidence only:

confidence = direction === "OVER" ? probability : 100 - probability
calibrated = calibrateProbability(confidence)
finalProbability = direction === "OVER" ? calibrated : 100 - calibrated

Reject if calibrateProbability() is applied directly to probability.

3. Market Anchoring
Verify edge uses anchored mean:

marketMean = req.liveLine + 0.5
finalMean = 0.65 * expectedTotal + 0.35 * marketMean
edge = finalMean - req.liveLine

4. Tempo Multiplier
Confirm possession tempo multiplier exists and does not algebraically cancel.

Expected pattern:

baselinePPM = 2.1
livePPM = estimatePossessionsPerMinute(period, scoreDiff)
tempoMultiplier = livePPM / baselinePPM
expectedFromHere = blendedPerMin * remainingMinutes * tempoMultiplier

Reject if blendedPerMin is divided by ppm.

5. Combo Variance Filter
Confirm combo detection handles both formats:

statType.includes("+") || statType.includes("_")

Probability multiplied by 0.96 and clamped afterward.

6. Bench Volatility Filter
Verify filter uses effective minutes:

effectiveMinutesBase = freshProjectedMinutes ?? avgMinutes
if (effectiveMinutesBase < 26) probability *= 0.94

Reject if projectedMinutes is referenced directly.

7. Clamp Enforcement
Confirm clamp exists after:
- combo filter
- bench filter
- calibration lookup

Clamp must be:

probability = Math.max(2, Math.min(98, probability))

8. Real Odds Edge Check
Confirm sportsbook implied probability uses real odds:

sportsbookImplied = americanOddsToProb(req.bookOdds ?? -110) * 100
edgeVsBook = finalProbability - sportsbookImplied
noSignal = edgeVsBook < 3

Verify noSignal appears in both response and debug output.

9. Analytics Range Filtering
Verify GET /api/analytics/summary reads req.query.range and forwards it to getAnalyticsSummary(range).

Confirm SQL filters exist for:

today
yesterday
7d
30d
all

10. Calibration Diagnostics
Verify analytics bucket response includes:

expectedWinRate
actualWinRate
calibrationError

Expected midpoints:

60–69 → 64.5
70–79 → 74.5
80–89 → 84.5
90+ → 92

11. Calc Log
Confirm an in-memory capped array (≤500 entries) logs each calculation and does not write to the database.

Output:

• The exact code snippets proving each check  
• PASS / FAIL for each check  
• Final verdict: APPROVE or REJECT

Build the full "first 50 users" feature set:

Admin panel — /admin page where the admin can view all users, manually set subscription tiers (to give beta users free All Sports access), and reset play counts
Feedback button — persistent button in dashboard that lets users submit feedback stored in the DB
Share/Referral prompt — after a strong calculation result (>65% or <35% probability), nudge the user to share the pick with a copy-to-clipboard formatted snippet
Odds API tier note — document the 20K plan ($30/mo) upgrade path in replit.md; the upgrade itself is done by the user on the-odds-api.com dashboard (no code change needed)
Tasks
T001: DB schema — add feedback table
Blocked By: []
Details:
Add a feedback table to shared/schema.ts: id (serial PK), userId (integer, nullable — so logged-out users could submit if needed, but we'll require login), message (text, not null), createdAt (timestamp, default now)
Add insertFeedbackSchema and types
Add createFeedback(userId, message) and getAllFeedback() to IStorage and DatabaseStorage in server/storage.ts
Run npm run db:push to sync the schema
Files: shared/schema.ts, server/storage.ts
Acceptance: feedback table exists in DB
T002: Backend — admin API routes
Blocked By: []
Details:
Add getAllUsers() to IStorage / DatabaseStorage — returns all users (excluding passwordHash) ordered by createdAt desc
Add setUserSubscriptionTier(userId, tier) to storage — directly sets subscriptionTier to null, 'nba', or 'all' (bypasses Stripe)
Add resetUserPlays(userId) to storage — sets playsUsed to 0
Add admin middleware requireAdmin to server/auth.ts — checks req.session.userId, loads user, verifies isAdmin, else 403
Add new admin routes in server/routes.ts:
GET /api/admin/users — returns all users
PATCH /api/admin/users/:id/tier — body { tier: null | 'nba' | 'all' } — sets tier
PATCH /api/admin/users/:id/reset-plays — resets playsUsed to 0
Add feedback routes:
POST /api/feedback — requires login, body { message }, stores in DB
GET /api/admin/feedback — requires admin, returns all feedback with user email
Files: server/storage.ts, server/auth.ts, server/routes.ts
Acceptance: Routes return 403 for non-admins, correct data for admin
T003: Admin panel frontend (/admin)
Blocked By: [T001, T002]
Details:
Add new page client/src/pages/admin.tsx
Register route /admin in client/src/App.tsx — redirect to /auth if not logged in, redirect to / if logged in but not admin
Admin panel sections:
Users table: email, joined date, tier (shown as badge), playsUsed, actions
Tier selector per user: dropdown with "Free", "NBA ($25)", "All Sports ($50)" — calls PATCH /api/admin/users/:id/tier
"Reset Plays" button per user — calls PATCH /api/admin/users/:id/reset-plays
Feedback inbox: list of feedback messages with user email + timestamp, newest first
Style: consistent with dashboard — dark card/border, same font/spacing; no external UI libraries beyond what's already imported
Add a small "Admin" link in the dashboard header (only visible when user.isAdmin)
Files: client/src/pages/admin.tsx, client/src/App.tsx, client/src/pages/dashboard.tsx
Acceptance: Admin can view all users, change tiers, reset plays, read feedback
T004: Feedback button in dashboard
Blocked By: [T001, T002]
Details:
Add a FeedbackModal component (inline in dashboard or separate file client/src/components/feedback-modal.tsx)
Floating button: fixed bottom-right of screen, small circular button with a speech bubble / message icon (lucide MessageSquare)
On click: opens modal with a textarea ("What's on your mind? Report a bug, suggest a feature, or just say hi.")
Submit calls POST /api/feedback, shows success confirmation, closes after 2s
Only shown when user is logged in
Files: client/src/components/feedback-modal.tsx, client/src/pages/dashboard.tsx
Acceptance: User can submit feedback; it appears in admin panel
T005: Share/Referral prompt after strong result
Blocked By: []
Details:
In client/src/pages/dashboard.tsx, after the probability result is rendered, detect if result.probability >= 65 or result.probability <= 35
When triggered, show a "Share this pick" card below the result ring:
Generates a formatted snippet: e.g. "⚾ [Player] [Over/Under] [line] [stat] — [X]% likely (2H) via LiveLocks by PropPulse"
"Copy Pick" button — copies snippet to clipboard using navigator.clipboard.writeText
Shows a brief "Copied!" confirmation
Subtle secondary styling (not distracting from main result)
The share prompt is dismissed when the user runs a new calculation
Files: client/src/pages/dashboard.tsx
Acceptance: Prompt appears on strong results, copy works, resets on new calc
T006: Update replit.md with Odds API upgrade instructions
Blocked By: []
Details:
Add a section to replit.md noting:
Current plan: Free (500 credits/month, exhausted)
Recommended next step: 20K plan at $30/month on the-odds-api.com
To upgrade: log in to the-odds-api.com → Billing → select 20K plan → new API key auto-updates (same key, just higher quota)
At 50 users with 5-min server cache: ~2,000–5,000 credits/month expected usage
Files: replit.md
Acceptance: Documented clearly for future reference
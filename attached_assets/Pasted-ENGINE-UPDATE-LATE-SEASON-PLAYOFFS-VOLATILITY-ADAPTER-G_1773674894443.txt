ENGINE UPDATE — LATE-SEASON / PLAYOFFS VOLATILITY ADAPTER

Goal:
Adjust the NBA engine for season-phase role volatility so late-season bench / development chaos stops crushing UNDERs, while automatically normalizing when playoffs begin.

Do NOT redesign the engine.
Do NOT change API routes.
Do NOT modify calibrateProbability(), lineMovement.ts, or canonical market logic.

Only add a lightweight season-phase adjustment layer inside the NBA probability flow.

Files involved:
server/storage.ts
server/minutesModel.ts
shared/schema.ts


STEP 1 — Add season phase helper

In server/storage.ts add:

function getSeasonPhase(gameDate?: string | Date): "early" | "mid" | "late" | "playoffs" {
  const d = gameDate ? new Date(gameDate) : new Date();
  const month = d.getMonth() + 1;

  // Oct-Dec
  if (month >= 10 || month <= 12) return "early";

  // Jan-Feb
  if (month >= 1 && month <= 2) return "mid";

  // Mar-Apr regular season volatility window
  if (month >= 3 && month <= 4) return "late";

  // fallback
  return "playoffs";
}

If there is an existing reliable game date on the request/game object, use it.
Otherwise default to current server date.


STEP 2 — Resolve season phase inside calculateProbability()

Inside calculateProbability() resolve:

const seasonPhase = getSeasonPhase(req.gameDate);

Add seasonPhase to debug output.


STEP 3 — Late-season UNDER volatility penalty

Late-season roles become unstable, especially for bench / low-minute / combo UNDERs.

After calibrateProbability() and direction resolution, add:

const isComboStat = req.statType.includes("_");
const isUnder = direction === "UNDER";
const effectiveMinutesBase = freshProjectedMinutes ?? avgMinutes;
const isLowRolePlayer = effectiveMinutesBase < 28;
const isBenchVolatile = effectiveMinutesBase < 24 && minutesPlayed < 12;

If seasonPhase === "late" AND isUnder:
  - if isLowRolePlayer, probability *= 0.94
  - if isComboStat, probability *= 0.95
  - if isBenchVolatile, probability *= 0.92

Clamp probability to [2, 98].

These should stack multiplicatively and only apply in late season.


STEP 4 — High-usage UNDER penalty remains active

Keep existing logic:

if (direction === "UNDER" && usageRate > 0.26) {
  probability = clamp(probability - 3, 2, 98)
}

This should still run before the new late-season penalties.

Execution order should be:

1. calibrateProbability()
2. resolve direction
3. high-usage UNDER penalty
4. combo variance penalty
5. projected-minutes-aware bench filter
6. late-season volatility penalties
7. edge threshold filtering


STEP 5 — Optional tank / development team penalty

Add an optional lightweight team volatility set in storage.ts:

const HIGH_VOLATILITY_TEAMS = new Set([
  "BKN", "WAS", "CHA", "POR", "UTA", "DET"
]);

If seasonPhase === "late" AND HIGH_VOLATILITY_TEAMS.has(playerTeam):
  probability *= 0.96

Clamp again to [2, 98].

Only apply this in late season.
Do not apply in playoffs.


STEP 6 — Playoff normalization boost

When seasonPhase === "playoffs", rotations stabilize.

Add a small confidence restoration:

if (seasonPhase === "playoffs") {
  probability *= 1.03;
}

Clamp to [2, 98].

Do NOT overboost bench players.
This is only a light normalization factor for the overall model.


STEP 7 — Minutes model playoff tightening

In server/minutesModel.ts add a small optional seasonPhase input to MinutesContext:

seasonPhase?: "early" | "mid" | "late" | "playoffs"

Use it only for these lightweight adjustments:

If ctx.seasonPhase === "late":
  - reduce close-game extension multiplier by 10% for players with projectedMinutes < 28

If ctx.seasonPhase === "playoffs":
  - increase closingProbability by +0.03 for players with avgMinutes >= 30
  - clamp closingProbability to max 0.99

Do NOT otherwise redesign the minutes model.


STEP 8 — Pass seasonPhase into minutesModel

From calculateProbability(), pass seasonPhase into calculateRemainingMinutes().


STEP 9 — Debug output additions

Extend CalcDebug in shared/schema.ts with:

seasonPhase?: "early" | "mid" | "late" | "playoffs"
lateSeasonPenaltyApplied?: boolean
playoffBoostApplied?: boolean
teamVolatilityPenaltyApplied?: boolean

Populate these in the debug object.


STEP 10 — Validation

Ensure TypeScript compiles.

Run simulations for:
- late-season bench UNDER
- late-season combo UNDER
- playoff star OVER
- playoff star UNDER

Confirm:
- late-season volatile UNDERs have lower probability
- playoff plays retain or slightly recover confidence
- no route or response regressions occur


EXPECTED EFFECT

Late season:
- fewer fake UNDER edges on volatile players
- better role-volatility handling
- stronger lowest bucket quality

Playoffs:
- rotations normalize
- confidence stabilizes naturally
- hit rates should improve without extra model complexity
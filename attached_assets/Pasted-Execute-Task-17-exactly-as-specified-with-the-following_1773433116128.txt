Execute Task #17 exactly as specified with the following strict implementation rules.

GLOBAL RULES
- Do NOT modify UI code.
- Do NOT modify canonical market keys.
- Do NOT reopen Task #10.
- Do NOT add database migrations.
- Do NOT run build/test loops automatically.
- Do NOT introduce new engineOutput fields other than debugMarkets enrichment.

All work is limited to:

server/ncaabEngine.ts
server/ncaabService.ts


--------------------------------
TRACK 1 — Runtime Contract Validator
--------------------------------

File:
server/ncaabService.ts

Immediately after:

const engineOutput = runNCAABEngine(engineInput)

Add a validator to confirm the canonical markets contract.

Required keys:

full_total
full_spread
h1_total
h1_spread
h2_total
h2_spread

Implementation:

const REQUIRED_MARKETS = [
  "full_total",
  "full_spread",
  "h1_total",
  "h1_spread",
  "h2_total",
  "h2_spread"
] as const;

for (const key of REQUIRED_MARKETS) {
  if (!engineOutput.markets || !(key in engineOutput.markets)) {
    console.error(
      "NCAAB canonical market contract violation",
      { gameId, missingKey: key }
    );
  }
}

Do NOT throw errors.
Execution must continue.


--------------------------------
TRACK 2 — Debug Diagnostics Enrichment
--------------------------------

File:
server/ncaabEngine.ts

Extend the debug diagnostics interface:

interface NCAABDebugMarketEntry {
  marketKey: NCAABCanonicalMarketKey
  projection: number | null
  bookLine: number | null
  modelProb: number | null
  bookImpliedProb: number | null
  edge: number | null
  confidenceTier: ConfidenceTier
}

Populate confidenceTier when building debugMarkets.

debugMarkets must only exist when:

process.env.NODE_ENV !== "production"

Debug data must never drive UI rendering.


--------------------------------
TRACK 3 — Global Top Plays Feed Helper
--------------------------------

File:
server/ncaabEngine.ts

Export helper:

export function buildTopPlaysFeed(
  outputs: Array<NCAABEngineOutput & { gameId: string }>
): NCAABMarket[]

Algorithm:

1. Iterate through all engine outputs
2. Aggregate all canonical markets
3. Filter markets where:

   market.available === true
   market.edge !== null

4. Deduplicate using key:

   `${gameId}:${market.marketKey}`

5. If duplicates occur keep the entry with highest absolute edge.

6. Sort results by:

   Math.abs(edge) descending

7. Limit output to:

   20 markets maximum

Return canonical NCAABMarket objects.


--------------------------------
TRACK 4 — Engine Stability Guards
--------------------------------

File:
server/ncaabEngine.ts

Inside buildMarket()

Add projection guard BEFORE implied probability logic:

if (
  projectionVal !== null &&
  (!isFinite(projectionVal) || Number.isNaN(projectionVal))
) {
  return unavailableMarket(key, label)
}

After computing edge add:

if (!isFinite(edge) || Number.isNaN(edge)) {
  return unavailableMarket(key, label)
}


--------------------------------
TRACK 5 — Targeted Memoization
--------------------------------

File:
server/ncaabEngine.ts

Inside runNCAABEngine()

Add:

const oddsCache = new Map<number, number>()

Wrap implied probability calculation:

function getImpliedProb(odds: number) {
  if (oddsCache.has(odds)) {
    return oddsCache.get(odds)!
  }

  const prob = americanToImpliedPct(odds)
  oddsCache.set(odds, prob)
  return prob
}

Replace all direct americanToImpliedPct calls with:

getImpliedProb(odds)

IMPORTANT:
- clampProb must NOT be memoized
- cache must exist only inside runNCAABEngine
- cache must NOT persist across engine runs


--------------------------------
FINAL OUTPUT
--------------------------------

Return a summary including:

- files modified
- runtime validator implementation
- debugMarkets enrichment
- buildTopPlaysFeed helper
- stability guard additions
- memoization implementation

Stop after implementation.
Do not modify UI code.

Execute Task #19 exactly as defined in `.local/tasks/task-18.md`.

This task implements:

MLB Phase A: Admin Tab Access + Roster Service.

Do not redesign the spec. Follow it precisely.

-----------------------------------------------------

IMPLEMENT THE 5 DELIVERABLES

1. client/src/pages/dashboard.tsx
2. server/mlb/rosterService.ts
3. server/routes.ts (add POST /api/mlb/sync-rosters)
4. client/src/components/mlb-admin-tab.tsx
5. server/mlb/markets.ts (hydration + validation)

-----------------------------------------------------

REQUIREMENTS

Implement the 6 approved edits exactly:

Edit 1 — MLB_SEASON constant
Edit 2 — Admin tester payload matches MLBPropInput exactly
Edit 3 — Add team field to tester form
Edit 4 — Hydrate batterHand, team, playerName from rosterService
Edit 5 — Analytics tab is explicitly out of scope
Edit 6 — Add getPlayerByName helper

Do not introduce any new architecture.

-----------------------------------------------------

ROSTER SERVICE

Create server/mlb/rosterService.ts with:

Interfaces:
MLBPlayer
MLBStartingLineup
MLBPitcher

In-memory stores:
playerPool
teamRosters
startingLineups
startingPitchers

Sync functions:
updatePlayerPool()
updateTeamRosters()
updateStartingLineups(gameId)
updateStartingPitchers(gameId)

Read functions:
getPlayer()
getPlayerByName()
getTeamRoster()
getStartingLineup()
getStartingPitcher()
getPlayerPoolCount()
getTeamCount()

Use MLB Stats API with MLB_SEASON constant.

-----------------------------------------------------

SYNC ROUTE

Add route in server/routes.ts:

POST /api/mlb/sync-rosters

Admin only.

Route executes:

await updatePlayerPool()
await updateTeamRosters()

Return:

{
  playersLoaded,
  teamsLoaded
}

-----------------------------------------------------

ADMIN MLB PANEL

Create client/src/components/mlb-admin-tab.tsx.

Include 3 sections:

1. Sync MLB Rosters
2. Prop Edge Tester
3. Diagnostics Viewer

Follow structure used in ncaab-admin-tab.tsx.

-----------------------------------------------------

MARKETS INTEGRATION

Modify calculateMLBPropEdge in server/mlb/markets.ts.

Before dispatching to market calculators:

const rosterPlayer =
  getPlayer(input.playerId) ??
  getPlayerByName(input.playerName);

let resolvedInput = input;

if (rosterPlayer) {
  resolvedInput = {
    ...resolvedInput,
    batterHand: resolvedInput.batterHand ?? rosterPlayer.bats,
    team: resolvedInput.team || rosterPlayer.team,
    playerName: resolvedInput.playerName || rosterPlayer.playerName,
  };
}

Then validate required fields and append warnings to MLBPropOutput.warnings.

Pass resolvedInput to all market calculators.

Do not mutate original input.

-----------------------------------------------------

FRONTEND MLB TAB

Update dashboard.tsx:

Admin users can access MLB tab.

Non-admin users still see locked MLB tab.

Render MLBAdminTab when:

activeTab === "mlb" && user?.isAdmin

-----------------------------------------------------

STRICT DO-NOT-MODIFY LIST

Do not modify:

NBA engine
NCAAB engine
server/mlb/calibration.ts
server/mlb/probability.ts
server/mlb/projections.ts
server/mlb/featureEngineering.ts
server/mlb/diagnostics.ts
server/mlb/dataSources.ts
server/mlb/backtestHarness.ts
Analytics tab
Existing /api/sync-rosters route

-----------------------------------------------------

FINAL REQUIREMENT

Run:

npm run check

and ensure:

0 TypeScript errors.

Return implementation summary and file diffs.

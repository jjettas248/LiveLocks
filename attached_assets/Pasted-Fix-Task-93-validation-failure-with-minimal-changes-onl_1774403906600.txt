Fix Task #93 validation failure with minimal changes only.

Do NOT rewrite logic. Apply only these corrections:

-------------------------------------
1. FIX INVALID quickViewRendered FLAG
-------------------------------------

In dashboard.tsx:

REMOVE any POST that sets:
quickViewRendered: true
immediately after fetch or data load.

This is invalid.

INSTEAD:

Add a useEffect that runs AFTER Quick View render data is finalized:

useEffect(() => {
  if (!renderedRows || renderedRows.length === 0) return;

  console.log("[QUICK_VIEW_RENDER_ASSERT]", {
    rows: renderedRows.length
  });

  fetch("/api/halftime-verify", {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({
      quickViewRendered: true
    }),
  });
}, [renderedRows]);

Only send true if rows actually exist.

-------------------------------------
2. KEEP clientReceived VALID
-------------------------------------

This is OK:

clientReceived = data.length > 0

But DO NOT send quickViewRendered in the same POST.

-------------------------------------
3. FIX GLOBAL STATE LEAK
-------------------------------------

In server/routes.ts:

Replace module/global verification object with request-scoped map.

Use:

const verificationMap = new Map();

In POST /api/halftime-verify:

Use request identifier (simple approach):

const key = req.ip;

const existing = verificationMap.get(key) || {
  clientReceived: false,
  quickViewRendered: false
};

Update only provided fields and store back.

-------------------------------------
4. UPDATE SUCCESS GATE
-------------------------------------

When evaluating:

const verification = verificationMap.get(req.ip) || {
  clientReceived: false,
  quickViewRendered: false
};

Use this instead of global state.

-------------------------------------
5. DO NOT AUTO-FLIP FLAGS
-------------------------------------

Both must remain false unless explicitly set by client:

- clientReceived
- quickViewRendered

-------------------------------------
SUCCESS CRITERIA
-------------------------------------

- quickViewRendered only true AFTER real render
- no early POST sets it
- verification is per-request (not global)
- success gate cannot be falsely triggered

-------------------------------------
OUTPUT
-------------------------------------

Return only:
- files changed
- exact lines modified
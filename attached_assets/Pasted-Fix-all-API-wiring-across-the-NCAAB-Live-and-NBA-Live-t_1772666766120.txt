Fix all API wiring across the NCAAB Live and NBA Live tabs.
Do NOT assume or hardcode any API sources.

---

STEP 1 — AUDIT FIRST

Before writing any code, scan the entire codebase and identify:

1. Every environment variable already set in Replit Secrets
   (process.env.* references throughout all files)

2. Every API endpoint already being called anywhere in the project
   (fetch(), axios, or any HTTP calls in any file)

3. Every data file or service already imported and used
   (local JSON, external SDKs, utility functions)

4. Every place where "--", null, 0%, or placeholder data 
   is being returned instead of real values

Print a summary of findings as a code comment at the top 
of your response before writing any fixes:

  // AUDIT RESULTS
  // Env vars found: [list]
  // APIs already wired: [list]
  // Data files in use: [list]
  // Fields returning placeholder data: [list]

---

STEP 2 — FIX USING ONLY WHAT EXISTS

Using ONLY the APIs, env vars, and data sources found 
in Step 1:

Fix every field that is returning placeholder data.

For each broken field:
  1. Trace where its data should come from 
     based on existing API calls in the codebase
  2. Extract the correct value from the existing 
     API response using the correct field path
  3. Wire it to the correct UI element
  4. Add a "--" fallback if the field is missing 
     from the response — never break the card layout

Do NOT introduce any new APIs.
Do NOT introduce any new env vars.
Do NOT install any new packages.
Use only what is already in the project.

---

STEP 3 — FIX CARD INTERACTIVITY

Every game row in the NCAAB Live tab must be clickable.

Audit every game row component and confirm:
  - onClick handler exists on the outermost container div
  - cursor: pointer is applied
  - hover state lifts border to zinc-600
  - Clicking expands the inline game card

If any row is missing onClick:
  Add: onClick={() => handleRowClick(game.id)}
  to the outermost div, not a child element.

Do NOT add onClick to child elements like team name spans —
it must be on the full row container so the entire 
row area is tappable.

---

STEP 4 — FIX MARKET BUTTON WIRING

The Over, Under, and Spread market buttons must:
  1. Display the correct line from existing odds data
  2. Display the correct engine probability percentage
  3. Update the radial gauge when clicked
  4. Highlight as selected when active

Trace the existing odds data object in the codebase.
Map each button to the correct field from that object.
Do not hardcode any values.

---

STEP 5 — FIX GAUGE WIRING

The radial gauge must:
  1. Default to dominant side probability on card load
  2. React to market button clicks
  3. Always show full game probability — never H1 data
  4. Display a real number, never 0% unless the 
     actual calculated probability is genuinely 0

Trace where gauge value is being set in the codebase.
Find why it is defaulting to 0 or incorrect values.
Fix the source of that bug — do not patch the display.

---

STEP 6 — FIX VERDICT ROW WIRING

The Strong Over/Under EV row must show:
  - Correct engine probability vs book implied probability
  - Correct edge gap calculation
  - Correct directional label (Over vs Under)

Trace the existing edgeGap and bookImplied calculations.
If they exist but are receiving wrong inputs, fix the inputs.
If they do not exist, write them using only 
data already available in the game object.

---

STEP 7 — FIX SCORE + CLOCK AUTO-UPDATE

Live game scores must update automatically every 3 minutes.

Check if a polling interval already exists in the codebase:
  - If yes: verify it is actually triggering a re-fetch 
    and updating state correctly
  - If no: add a useEffect interval using the existing 
    fetch function — do not create a new fetch function

  useEffect(() => {
    if (game.status !== "live") return
    const interval = setInterval(() => {
      // call the existing fetch function already in codebase
    }, 180000)
    return () => clearInterval(interval)
  }, [game.id, game.status])

---

CONSTRAINTS

Do NOT:
  - Add any new APIs
  - Add any new environment variables
  - Install any new packages
  - Rewrite components from scratch
  - Change component structure or layout
  - Touch any styling that is already working

Only fix:
  - Data not flowing from existing sources to UI fields
  - onClick handlers missing from game rows
  - Gauge receiving wrong or zero values
  - Polling interval missing or broken
  - Market buttons not updating gauge on click
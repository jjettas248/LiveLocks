Implement MLB Live so it matches the NBA Live experience across BOTH web and mobile, while also upgrading the MLB probability model without breaking the existing LiveLocks engine architecture.

The MLB product must support two parallel modes:

1) AUTOMATIC SIGNALS (existing system)
2) MANUAL PLAYER PROBABILITY CALCULATOR (NBA-style workflow)

The default MLB experience must remain automatic-signals-first.
The manual calculator is a drill-down layer, not the primary product surface.

The system philosophy must remain:

Game State → Opportunity Projection → Outcome Probability → Edge vs Market

Do NOT replace the existing engine logic.
The new probability inputs must act only as modifiers to the base hit probability.
The orchestrator, edge cache, and signal pipeline must remain unchanged.

==================================================
PART 1 — ENGINE / PRODUCT PHILOSOPHY RULES
==================================================

The following systems must remain unchanged:

- liveGameOrchestrator
- edgeCache
- mlbEdgeCache
- triggerEngine
- liveGameRegistry
- signal pipeline
- live signals API architecture

Signals must still come from:
GET /api/mlb/live-signals/:gameId

Manual calculations must call:
POST /api/mlb/props

The frontend must NOT calculate:
- remainingPA
- remainingAB
- model probability
- edge

Frontend only sends raw game/player inputs.
Backend calculates all probabilities and projections.

MLB Live must preserve the automatic-first identity:
- default view = automatic signals + live box score
- manual calculator = secondary drill-down mode when a player is selected

==================================================
PART 2 — WEB + MOBILE PARITY REQUIREMENT
==================================================

The MLB Live experience must behave the same on:
- desktop
- tablet
- mobile

The interaction model must match NBA Live:

Tap game
→ view box score and signals

Tap player
→ open matchup details

Select sportsbook line
→ calculate probability

View prediction panel

Tap back
→ return to automatic signals

Use the same UI hierarchy and component style as NBA Live wherever possible.

==================================================
PART 3 — MLB LIVE PAGE STRUCTURE
==================================================

Refactor:
client/src/pages/mlb-live.tsx

Final structure:

MlbLivePage

GameStrip
InningTabs

IF selectedPlayer == null

    CollapsibleBoxScore
    EdgeCards

ELSE

    MatchupDetailsPanel
    LiveLineSelector
    CalculateProbabilityButton
    PredictionResultPanel

==================================================
PART 4 — GAME STRIP
==================================================

Use existing endpoint:
GET /api/mlb/live-games

Auto-refresh every 30 seconds.

On first load:
- auto-select the first live game
- if none, auto-select the first preview game
- if none, show empty state "No MLB games today"

Clicking a game overrides current selection.

==================================================
PART 5 — COLLAPSIBLE BOX SCORE
==================================================

Add state:
const [boxExpanded, setBoxExpanded] = useState(true)

Header must show:
▾ Box Score

Tapping toggles collapse.

When expanded, render table columns:

Player
AB
H
TB
R
RBI
BB
SB
K

Rows must be clickable.

Each player row:
onClick={() => setSelectedPlayer(player)}

Add state:
const [selectedPlayer, setSelectedPlayer] = useState(null)

Row coloring rule:
Use the player’s strongest available signal across all MLB markets.
Strongest = signal with largest Math.abs(edge).

Signals continue polling in the background even when selectedPlayer !== null.
Do not unmount signal queries.

==================================================
PART 6 — MATCHUP DETAILS PANEL
==================================================

When selectedPlayer !== null, show:

← Back to Game

This clears selectedPlayer.

Render MatchupDetailsPanel with the same visual structure as NBA matchup panel.

Fields:

Player
Team
Opponent
Batting Order Slot
Current Inning

Current stats:
AB
H
TB
BB
K
SB

Use same card style / spacing / layout system as NBA Live.

==================================================
PART 7 — LIVE SPORTSBOOK LINE SELECTOR
==================================================

Add endpoint:
GET /api/mlb/odds

Use sport key:
baseball_mlb

Return same structure as NBA odds endpoint:

{
  bookmaker: {
    line,
    overOdds,
    underOdds
  }
}

Books shown:
- FanDuel
- DraftKings
- Hard Rock

LiveLineSelector:
- displays the three books
- highlights selected row
- includes market selector:
  Hits
  Total Bases
  Strikeouts

Do NOT fabricate lines.
Do NOT fabricate book implied probability.

==================================================
PART 8 — CALCULATE PROBABILITY
==================================================

Button:
Calculate Probability

Disabled until a line is selected.

Calls:
POST /api/mlb/props

Payload must send only raw inputs:

- playerId
- market
- line
- currentStats
- currentInning
- battingOrderSlot
- team
- opponent

No frontend probability math of any kind.

==================================================
PART 9 — MLB PROBABILITY MODEL UPGRADE
==================================================

Implement an improved MLB probability model while preserving the existing LiveLocks engine architecture.

The engine flow MUST remain:

Game State
↓
Remaining Plate Appearance Projection
↓
Adjusted Hit Probability
↓
Binomial Probability Calculation
↓
Expected Outcome
↓
Edge vs Sportsbook

Do NOT replace the engine.
These additions must be modular modifiers layered into the existing probability pipeline.

------------------------------
STEP 1 — REMAINING PLATE APPEARANCE MODEL
------------------------------

Estimate remaining plate appearances using inning and batting order.

remainingPA = (9 - currentInning) * 0.44

Apply batting order adjustments:

slot 1–2 → +0.45
slot 3–5 → +0.30
slot 6–7 → +0.10
slot 8–9 → -0.10

remainingPA += slotAdjustment

Apply game pace factor:

paceFactor = currentRuns / leagueAvgRuns

remainingPA *= paceFactor

Clamp remainingPA between 1.0 and 3.5.

------------------------------
STEP 2 — BASE PLAYER HIT RATE
------------------------------

playerHitRate = playerHits / playerAB

Blend with league average to avoid small sample noise.

leagueHitRate = 0.243

blendedHitRate =
(0.60 * playerHitRate)
+
(0.40 * leagueHitRate)

------------------------------
STEP 3 — PITCHER MATCHUP ADJUSTMENT
------------------------------

Retrieve pitcher metrics:

- pitcherKRate
- pitcherBABIP

Calculate pitcher interaction modifier:

pitcherModifier =
1
- (pitcherKRate * 0.30)
+ ((pitcherBABIP - leagueBABIP) * 0.50)

adjustedHitRate = blendedHitRate * pitcherModifier

------------------------------
STEP 4 — BALLPARK FACTOR
------------------------------

Apply park factor multiplier.

Examples:
- Coors Field = 1.15
- Fenway Park = 1.05
- Petco Park = 0.94

adjustedHitRate *= parkFactor

------------------------------
STEP 5 — BULLPEN QUALITY ADJUSTMENT
------------------------------

Use opponent bullpen ERA.

bullpenModifier =
leagueBullpenERA / opponentBullpenERA

adjustedHitRate *= bullpenModifier

------------------------------
STEP 6 — WEATHER ADJUSTMENT
------------------------------

Use temperature and wind.

weatherModifier =
1
+ (windOut * 0.02)
+ ((temperature - 70) * 0.003)

adjustedHitRate *= weatherModifier

------------------------------
STEP 7 — PROBABILITY CALCULATION
------------------------------

Using binomial distribution:

P(hit ≥ 1) =
1 - (1 - adjustedHitRate) ^ remainingPA

------------------------------
STEP 8 — EXPECTED HITS
------------------------------

expectedHits =
remainingPA * adjustedHitRate

------------------------------
STEP 9 — EDGE VS MARKET
------------------------------

Convert American odds to implied probability.

impliedProbability =
americanOddsToProbability(bookOdds)

edge =
modelProbability - impliedProbability

------------------------------
STEP 10 — SIGNAL THRESHOLD
------------------------------

Only surface signals when:
edge ≥ 0.05 (5%)

Strong signals when:
edge ≥ 0.15 (15%)

==================================================
PART 10 — IMPLEMENTATION RULES FOR MODEL
==================================================

These modifiers must be implemented as modular functions so the engine remains extensible and transparent.

Example structure:

baseProbability()
applyPitcherModifier()
applyParkModifier()
applyBullpenModifier()
applyWeatherModifier()

This prevents a black-box model and preserves engine ethos.

The improved model must produce:

- modelProbability
- expectedHits
- remainingPA
- adjustedHitRate
- edge

while maintaining the existing signal detection architecture.

==================================================
PART 11 — PREDICTION RESULT PANEL
==================================================

Render result using the same UI primitives as NBA Live.

Use:
- ProbabilityRing
- EV card
- projection cards
- market comparison card
- CLV/implied comparison display

Show:

Over %
Under %
Expected Hits
Remaining PA
Adjusted Hit Rate
Edge %
Book Implied Probability

==================================================
PART 12 — EDGE SIGNAL CARDS
==================================================

When selectedPlayer == null, continue showing EdgeCards.

Signals still come from:
GET /api/mlb/live-signals/:gameId

Each card displays:

Player
Market
Line
Engine %
Book %
Edge %
Add to Parlay

Color tiers:
- ≥85% OVER → green
- 70–84% → yellow
- 60–69% → teal
- ≥85% UNDER → red

Signals route must remain cache-only.
No engine recomputation on request.

==================================================
PART 13 — MOBILE UX REQUIREMENTS
==================================================

Ensure:
- box score collapses cleanly
- player tap opens calculator mode
- calculator opens full width on mobile
- back button returns to automatic signals view
- cards stack vertically
- no horizontal scroll

==================================================
PART 14 — NBA PARITY REQUIREMENT
==================================================

The MLB calculator view must reuse the same interaction model and visual hierarchy as NBA Live:

- same matchup card pattern
- same result card pattern
- same selected-player flow
- same visual density and spacing rules
- same web and mobile interaction quality

MLB should feel like the same product as NBA Live, not a separate tool.

==================================================
PART 15 — OUT OF SCOPE
==================================================

Do NOT modify:
- orchestrator logic
- triggerEngine
- edgeCache behavior
- live signals API architecture
- NBA engine
- NCAAB engine
- Parlay deep linking logic

Parlay button may remain a stub for now.

==================================================
EXPECTED RESULT
==================================================

The MLB Live tab must function exactly like NBA Live:

open MLB tab
select game
view signals and box score
tap player
open matchup calculator
select sportsbook line
calculate probability
view projection and edge analysis
tap back
return to signals

And the MLB probability model must be improved using:
- remaining PA projection
- pitcher matchup adjustment
- park factor
- bullpen quality
- weather

without breaking the existing LiveLocks engine philosophy.
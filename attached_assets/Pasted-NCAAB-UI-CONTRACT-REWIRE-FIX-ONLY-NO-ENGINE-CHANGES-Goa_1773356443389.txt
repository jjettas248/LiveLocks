NCAAB UI CONTRACT REWIRE — FIX ONLY, NO ENGINE CHANGES

Goal:
Fix the NCAAB UI so each tab renders only the correct market-specific engine data and never mixes full-game, H1, H2, or legacy play fields.

Do not change engine math.
Do not add new features.
Do not run npm scripts automatically.
Return patch diff only.

Requirements

1. Enforce market-specific verdict sourcing

For each tab:
- Full Game tab must read only full-game verdicts
- 1st Half tab must read only H1 verdicts
- 2nd Half tab must read only H2 verdicts

If a verdict for the selected tab/market does not exist:
- render null / "No line" / "No edge"
- do NOT fall back to full-game values
- do NOT fall back to hardcoded 50%

2. Remove invalid probability fallbacks

Fix:
- H1 under currently falls back to 50
- H1/H2 spread prob currently falls back to full-game spread prob
- 2H plays section currently reads full-game spread verdict

Replace all of these with market-specific verdict lookup only.

3. Stop mixing legacy play fields with engineOutput

Audit and replace UI reads from:
- play.spread
- play.overProb
- play.h2SpreadLine
- play.h2EngineOverProb
- displayOverProb
- displayUnderProb
- projected fallback lines

Use only:
- engineOutput.marketVerdicts
- engineOutput.displayOutput
- engineOutput.calibrated* numeric fields for logic

4. Enforce display contract

User-facing labels must come from engineOutput.displayOutput where available.

Numeric logic may use calibrated numeric fields, but the UI must not build its own percentage strings if a displayOutput value exists.

5. Distinguish sportsbook line vs engine estimate

If there is no real sportsbook line:
- do not render projectedTotal as if it were the live line
- instead render a clearly labeled fallback like:
  "Engine fair total: X"
  or
  "Book line unavailable"

6. Normalize spread sign fallback consistency

If UI fallback is ever needed for spreadTeam/favorite comparison, match server behavior:
- use case-insensitive exact comparison
- never use heuristics
- never default to HOME

7. Output expected from this task

Return:
- list of all replaced legacy play.* fields
- list of market-specific verdict lookup helpers created
- confirmation that H1/H2 no longer use full-game spread probabilities
- confirmation that projected lines no longer masquerade as sportsbook lines

Files to modify:
- client/src/components/ncaab-admin-tab.tsx

Do not modify server engine files unless absolutely required for a missing field reference.
Stop after implementing the UI contract fix.
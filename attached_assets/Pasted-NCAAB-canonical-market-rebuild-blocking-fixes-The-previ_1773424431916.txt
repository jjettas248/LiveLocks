NCAAB canonical market rebuild — blocking fixes

The previous implementation was rejected because the UI still reads legacy fields and the engine does not correctly derive markets. This task must COMPLETE the canonical-market contract and remove all legacy fallbacks.

This is not a patch. The canonical architecture must be enforced.

DO NOT introduce new legacy fields, compatibility shims, or fallback behavior.

--------------------------------------------------

ENGINE CONTRACT FIXES

File:
server/ncaabEngine.ts

1. markets MUST be constructed first.

runNCAABEngine must build:

engineOutput.markets

as the single source of truth.

marketVerdicts must be derived from markets AFTER markets are complete.

Correct flow:

markets → marketVerdicts

NOT

marketVerdicts → markets

--------------------------------------------------

2. enforce sportsbook odds validation

Guardrail requirement:

Odds must be within [-2000, +2000].

Invalid odds include:

0
null
undefined
NaN
abs(odds) > 2000

If odds are invalid:

market.available = false
bookImpliedProb = null
edge = null

NO probability or edge computation is allowed.

Current behavior incorrectly calculates:

edge = modelProb - 50

This must be removed.

--------------------------------------------------

3. probability safety

Before assigning modelProb:

modelProb = clamp(modelProb, 1, 99)

If modelProb is:

NaN
Infinity
undefined

Then:

market.available = false

--------------------------------------------------

4. canonical market object

Every market must conform to:

interface NCAABMarket {

  available: boolean
  marketKey:
    "full_total" |
    "full_spread" |
    "h1_total" |
    "h1_spread" |
    "h2_total" |
    "h2_spread"

  label: string
  sportsbook: string | null
  bookLine: number | null
  projection: number | null
  modelProb: number | null
  bookImpliedProb: number | null
  edge: number | null
  side: "OVER" | "UNDER" | "HOME" | "AWAY" | null
  confidenceTier: "ELITE" | "STRONG" | "VALUE" | "NONE"
}

--------------------------------------------------

5. marketVerdicts compatibility layer

After markets is constructed:

marketVerdicts = Object.values(markets)
  .filter(m => m.available === true)
  .map(convertMarketToVerdict)

marketVerdicts must NOT be used to build markets.

--------------------------------------------------

UI CONTRACT FIXES

File:
client/src/components/ncaab-admin-tab.tsx

The UI must render ONLY from:

play.engineOutput.markets

--------------------------------------------------

6. remove ALL legacy field reads

Delete all UI render usage of:

play.overProb
play.spreadProb
play.h2EngineOverProb
play.h2SpreadLine
calibratedOverProb
calibratedUnderProb
calibratedSpreadProb
over2HProb

These fields may remain in the data model but must NEVER drive UI rendering.

--------------------------------------------------

7. remove all .find() calls on marketVerdicts

Delete every instance of:

marketVerdicts.find()

UI must not query the verdict array.

Markets must be accessed as:

play.engineOutput.markets.full_total
play.engineOutput.markets.full_spread
play.engineOutput.markets.h1_total
play.engineOutput.markets.h1_spread
play.engineOutput.markets.h2_total
play.engineOutput.markets.h2_spread

--------------------------------------------------

8. enforce unavailable rendering

If:

market.available === false

UI must render:

"Unavailable"

NO fallback values.

The following are forbidden:

?? 50
?? overProb
play.spread
play.h2SpreadLine
full-game substitutions
projection substitution

--------------------------------------------------

9. remove all type bypasses

Delete every instance of:

(mv: any)

Strict typing must be used.

If typing fails, fix the type definitions instead of bypassing them.

--------------------------------------------------

10. verify removal of legacy render paths

Run project grep checks:

grep -R "play.overProb"
grep -R "play.spreadProb"
grep -R "play.h2EngineOverProb"
grep -R "play.h2SpreadLine"
grep -R "marketVerdicts.find"

None of these may appear in UI render logic.

--------------------------------------------------

EXPECTED RESULT

After the rebuild:

Engine flow:

sportsbook lines → projection → modelProb → markets

UI flow:

markets → render

No inference
No fallback
No legacy field reads

--------------------------------------------------

STOP CONDITIONS

Do not run build loops or long validation cycles.

After code changes:

return a verification summary showing:

1. markets construction code
2. marketVerdicts derivation code
3. confirmation legacy fields removed
4. grep results proving removal
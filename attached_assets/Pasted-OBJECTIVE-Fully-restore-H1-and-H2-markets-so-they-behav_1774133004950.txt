OBJECTIVE
Fully restore H1 and H2 markets so they behave exactly like Full Game:
- real sportsbook lines when available
- -110 fallback when odds missing
- no projection-only fallback when real lines exist

---

Phase A — Verify extraction (no code yet)

For one live game:
Log:
- h1TotalLine
- h1SpreadLine
- h2TotalLine
- h2SpreadLine
- all detected sportsbook market keys

Confirm:
- Are H1/H2 keys present but not matched?
- Or never present at all?

STOP after logging.

---

Phase B — Fix extraction layer

1. Remove ANY premature break in extractLines bookmaker loop

2. Add H2 key matchers (mirror H1):

isH2TotalKey(key):
- includes "2h" OR "secondhalf" OR "2ndhalf"
- AND includes "total" OR "ou" OR "overunder"

isH2SpreadKey(key):
- includes "2h" OR "secondhalf" OR "2ndhalf"
- AND includes "spread" OR "line" OR "handicap"

3. Use normalizeMarketKey()

---

Phase C — Harden outcome parsing

When parsing outcomes:
- detect over / under
- detect home / away
- fallback to team names
- support BOTH:
  outcome.price
  outcome.odds

---

Phase D — Apply odds fallback (CRITICAL)

In computeNCAABPlays BEFORE runNCAABEngine:

Add:

if (h1TotalLine != null && h1OverOddsAmerican == null)
  h1OverOddsAmerican = -110;

if (h1SpreadLine != null && h1SpreadOddsAmerican == null)
  h1SpreadOddsAmerican = -110;

if (h2TotalLine != null && h2OverOddsAmerican == null)
  h2OverOddsAmerican = -110;

if (h2SpreadLine != null && h2SpreadOddsAmerican == null)
  h2SpreadOddsAmerican = -110;

DO NOT overwrite real odds.

---

Phase E — Add diagnostics

Log:
- H2 market keys detected but not matched
- H2 line present but odds missing
- final engineInput H1/H2 values

---

Phase F — Verify

For one live game confirm:

H1 tab:
- line visible
- model %
- book implied %

H2 tab:
- line visible (NOT projected)
- model %
- book implied %

---

Constraints

- Do NOT add synthetic lines
- Do NOT modify engine math
- Do NOT touch NBA/MLB
- Do NOT rely on ESPN for H1/H2

---

SUCCESS

H1/H2 behave exactly like Full Game:
- real lines when available
- no false "Unavailable"
- no projection-only when real data exists
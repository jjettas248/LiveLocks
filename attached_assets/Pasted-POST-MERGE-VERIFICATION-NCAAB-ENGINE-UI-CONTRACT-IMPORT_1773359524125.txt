POST-MERGE VERIFICATION — NCAAB ENGINE ↔ UI CONTRACT

IMPORTANT
- Do NOT modify any code
- Do NOT run npm commands
- Do NOT rebuild or redeploy
- This is a read-only diagnostic

Goal
Verify that the NCAAB UI is now rendering strictly from engineOutput.marketVerdicts and that the engine is actually producing the period-specific verdicts required by the new UI contract.

--------------------------------------------------

STEP 1 — Inspect engine output

Locate computeNCAABPlays() and identify one live or halftime game.

Print the following values for that play:

gameId
homeTeam
awayTeam
score

engineOutput.projectedTotal
engineOutput.calibratedOverProb
engineOutput.calibratedUnderProb
engineOutput.calibratedSpreadProb

engineOutput.displayOutput.displayProbability

engineOutput.marketVerdicts

Return the raw marketVerdicts array exactly as produced by the engine.

--------------------------------------------------

STEP 2 — Validate period-specific markets

Confirm whether the following verdict keys exist inside marketVerdicts:

total
spread
h1_total
h2_total
h1_spread
h2_spread

Return a table:

market | exists | example value

Example format:

total       | true  | line: 138.5 prob: 64
h1_total    | true  | line: 65.5 prob: 61
h2_total    | true  | line: 72.5 prob: 59
spread      | true  | line: -4.5 prob: 57
h1_spread   | false | -
h2_spread   | true  | line: -2.5 prob: 54

--------------------------------------------------

STEP 3 — Verify UI rendering contract

Open:

client/src/components/ncaab-admin-tab.tsx

Confirm the UI now reads verdicts through:

resolveVerdictForMarket(engineOutput, market)

Check the following locations:

H1 tab probability display
H2 tab probability display
Spread button logic
2H plays card

Return the exact code snippets showing the verdict lookup.

--------------------------------------------------

STEP 4 — Verify legacy fields are not used for rendering

Search the file for:

play.overProb
play.spreadProb
play.h2EngineOverProb
play.spreadEdge
play.totalEdge

Return each occurrence and classify it as:

USED FOR DISPLAY
USED FOR SORTING ONLY
NOT USED

--------------------------------------------------

STEP 5 — Verify line display behavior

Check logic for:

effectiveFGLine
effective1HLine
effective2HLine

Confirm that when a sportsbook line is missing the UI renders:

"Engine est: X.X"

Return the exact code snippet.

--------------------------------------------------

STEP 6 — Verify spread sign logic

Confirm spread sign resolution uses:

play.spreadTeam
or case-insensitive comparison of play.favorite vs team names.

Return the snippet implementing this.

--------------------------------------------------

FINAL OUTPUT FORMAT

ENGINE VERDICTS
(raw marketVerdicts)

VERDICT COVERAGE
(table of markets present)

UI LOOKUP LOGIC
(code snippets)

LEGACY FIELD USAGE
(table)

LINE DISPLAY BEHAVIOR
(snippet)

SPREAD SIGN LOGIC
(snippet)

Do NOT suggest changes.
Return verification results only.
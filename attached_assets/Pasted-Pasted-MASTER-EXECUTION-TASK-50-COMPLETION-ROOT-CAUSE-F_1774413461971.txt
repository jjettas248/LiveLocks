Pasted--MASTER-EXECUTION-TASK-50-COMPLETION-ROOT-CAUSE-FIX-SYS_1774399936485.txt
# MASTER EXECUTION: TASK #50 COMPLETION + ROOT CAUSE FIX

## SYSTEM:
Core Engine + UX Layer

## CONTEXT:
Task #50 (NBA Halftime Directional Bias — Forensic Fix) is already:
- drafted
- approved
- aligned with requirements

The issue is NOT planning.  
The issue is that Task #50 has NOT been fully executed and resolved in the active pipeline.

---

## OBJECTIVE:
Complete Task #50 end-to-end and resolve the halftime UNDER bias by:

1. Verifying the ACTIVE live path (not assumed path)
2. Reconciling engine → response → client → render
3. Identifying EXACT root cause (A/B/C/D/E)
4. Applying ONE minimal fix in the correct layer
5. Verifying post-fix alignment

---

## EXECUTION MODE (MANDATORY):

You are NOT allowed to:
- restate Task #50
- summarize the plan
- compare audits
- ask for approval
- stop after identifying files

You MUST:
- execute
- log
- classify
- fix
- verify

---

## PHASE 1 — ACTIVE PATH VERIFICATION

### Step 1: Identify LIVE client endpoint
Find EXACT:
- client file calling halftime data
- endpoint URL
- hook/function used

---

### Step 2: Trace to ACTIVE server handler
Locate:
- exact route serving that endpoint
- exact function building plays

CONFIRM:
This is the path used by the UI (not a dead or alternate route)

---

### Step 3: Add REQUIRED server log (ACTIVE path only)

```ts
console.log("[HT_RESPONSE_FINAL]", {
  count: plays.length,
  overs: plays.filter(p => p.direction === "OVER").length,
  unders: plays.filter(p => p.direction === "UNDER").length,
  sample: plays.slice(0, 10).map(p => ({
    playerName: p.playerName,
    market: p.market,
    direction: p.direction,
    probability: p.probability,
    edge: p.edge,
    projection: p.projection,
    line: p.line
  }))
});
PHASE 2 — CLIENT PIPELINE TRACE
Step 4: Log fetched data
console.log("[HT_CLIENT_FETCHED]", {
  count: data?.length ?? 0,
  overs: (data ?? []).filter(p => p.direction === "OVER").length,
  unders: (data ?? []).filter(p => p.direction === "UNDER").length
});
Step 5: Log rendered data (AFTER ALL TRANSFORMS)
console.log("[HT_CLIENT_RENDERED]", {
  count: renderedPlays?.length ?? 0,
  overs: (renderedPlays ?? []).filter(p => p.direction === "OVER").length,
  unders: (renderedPlays ?? []).filter(p => p.direction === "UNDER").length
});
Step 6: Log ALL transform stages

For EVERY stage:

filter
sort
slice
dedupe
map

Log:

console.log("[HT_STAGE_X]", {
  input: prev.length,
  output: next.length,
  overs: next.filter(p => p.direction === "OVER").length,
  unders: next.filter(p => p.direction === "UNDER").length
});
PHASE 3 — SORT / RANK DIAGNOSTIC
Step 7: Add top-edge server log
const sorted = [...plays].sort((a, b) => (Number(b.edge) || 0) - (Number(a.edge) || 0));

console.log("[HT_SORTED_TOP20]", sorted.slice(0, 20).map(p => ({
  playerName: p.playerName,
  market: p.market,
  direction: p.direction,
  edge: p.edge,
  probability: p.probability,
  projection: p.projection,
  line: p.line
})));
PHASE 4 — CLASSIFY ROOT CAUSE (MANDATORY)

You MUST classify into exactly ONE:

A = Engine output biased
B = Wrong endpoint / path
C = UI filtering/slicing/dedupe suppression
D = Client re-deriving direction/probability
E = Sorting/top-play skew

PHASE 5 — APPLY SINGLE MINIMAL FIX
RULE:

Fix ONLY the confirmed layer.

If A (engine bias):
Patch ONLY proven line (projection suppression OR mapping)
Do NOT rewrite engine
Do NOT change edge globally
If B (wrong path):
Repoint UI to correct endpoint
If C (UI suppression):
Remove filters, slicing, dedupe
Keep only structural validation
If D (re-derivation):

Delete ALL client logic like:

probability < 50 ? "UNDER" : "OVER"
100 - probability
If E (sorting skew):
Adjust selection logic ONLY
Do NOT modify engine
PHASE 6 — VALIDATION

Run a live slate and confirm:

Server:
[HT_RESPONSE_FINAL]
Client fetch:
[HT_CLIENT_FETCHED]
Client render:
[HT_CLIENT_RENDERED]

All counts must reconcile logically.

HARD RULES:
DO NOT rerun Task #50 planning
DO NOT touch MLB or NCAAB
DO NOT introduce artificial balancing
DO NOT modify multiple layers
DO NOT patch dead code paths
DO NOT stop early
SUCCESS CRITERIA:
Root cause identified and classified
Only ONE layer modified
Engine → UI contract preserved
Halftime signals reflect true model output
No unexplained UNDER bias remains
OUTPUT (REQUIRED):

Return:

Client file + endpoint
Server route
[HT_RESPONSE_FINAL] output
[HT_CLIENT_FETCHED] output
[HT_CLIENT_RENDERED] output
Transform stage logs
Root cause classification (A/B/C/D/E)
EXACT code diff applied
Before vs after distribution comparison
EXECUTION COMMAND:

BEGIN TASK #50 EXECUTION NOW


---

# Why this is the actual 10/10 prompt

- **Forces execution (not planning)**
- **Locks to Task #50 (no drift)**
- **Guarantees logs + proof**
- **Forces classification before fix**
- **Prevents engine overcorrection**
- **Produces a code diff (not theory)**

---

# What happens next

You will get a response that clearly shows:

- where the bias is coming from  
- exactly which layer is wrong  
- exactly ...
TASK: Repair MLB Live UI architecture so the probability engine runs correctly and the UI matches NBA Live behavior.

Current problems:
• Game matchups show "@" with no teams
• Box score renders at bottom of page
• Edge cards render for every player before selecting a game
• Every player shows the same fallback probability (18%)
• Calculator does not auto-fill context
• Engine endpoint is never called

The MLB tab must follow the exact interaction model used by NBA Live.

Do NOT modify the MLB engine code.
Only fix UI architecture and API invocation.

--------------------------------------------------

STEP 1 — Fix Game Matchup Rendering

File:
client/src/components/mlb-live-tab.tsx

The ESPN MLB API stores teams inside the competitions array.

Replace current team rendering with:

const competition = game.competitions?.[0]

const home = competition?.competitors?.find(
  c => c.homeAway === "home"
)

const away = competition?.competitors?.find(
  c => c.homeAway === "away"
)

Render matchup as:

{away?.team?.displayName} @ {home?.team?.displayName}

Also display:

score
inning
game status

--------------------------------------------------

STEP 2 — Add Selected Game State

At top of mlb-live-tab.tsx add:

const [selectedGame, setSelectedGame] = useState(null)

Default screen should show ONLY:

Today's Games

When a user clicks a game:

setSelectedGame(game)

--------------------------------------------------

STEP 3 — Render Game Details Only After Selection

Below the game list add:

if (!selectedGame) {
  return <GameList />
}

When selectedGame exists render:

Game Header
Box Score
Edge Signals

--------------------------------------------------

STEP 4 — Move Box Score Above Edge Signals

Correct layout order must be:

Game Header
Box Score
Edge Signals

Remove any layout where box score appears at the bottom.

--------------------------------------------------

STEP 5 — Scope Edge Signals To Selected Game

Currently edges render globally.

Filter edges using:

edges.filter(edge => edge.gameId === selectedGame.id)

Only display edges belonging to the selected game.

--------------------------------------------------

STEP 6 — Remove Fallback Probability

Search frontend for any of these patterns:

const probability = 18
probability ?? 18
probability || 18

Delete all fallback probability constants.

Probability must come only from the server response.

--------------------------------------------------

STEP 7 — Ensure Engine Endpoint Is Called

Inside edge-card.tsx ensure the Calculate button calls:

POST /api/mlb/calculate

Example:

await fetch("/api/mlb/calculate", {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify({
    playerId,
    statType,
    currentHits,
    currentAB,
    inning,
    liveLine
  })
})

Use response to set:

probability
expectedHits
remainingPA

--------------------------------------------------

STEP 8 — Auto Populate Calculator Fields

When opening the edge card populate:

playerName
team
opponent
battingOrderSlot
currentHits
currentAB
inning

User should only select:

statType
line

--------------------------------------------------

STEP 9 — Restore Odds API

Edge card must request sportsbook lines:

GET /api/odds?sport=mlb

Display lines from returned books.

If none exist show:

"No sportsbook line available"

Do not fabricate lines.

--------------------------------------------------

STEP 10 — Match NBA Interaction Model

Final MLB user flow must be:

Game List
↓
User selects game
↓
Box Score loads
↓
User clicks player
↓
Edge card opens
↓
Calculator auto-fills context
↓
User clicks Calculate
↓
Engine returns probability

No global player edges.
No placeholder probabilities.

--------------------------------------------------

After implementation verify:

• Game matchups show correctly
• Box score appears directly under selected game
• Edge signals appear only after selecting a game
• Probabilities differ per player
• Calculator auto-fills game context
• Calculate button calls /api/mlb/calculate
• Odds lines display correctly
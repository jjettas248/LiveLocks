TASK: Stripe Authority + Tier Normalization + Gating Integrity

SYSTEM:

Conversion + Access Control (CRITICAL PATH)

OBJECTIVE:
Eliminate ALL gating inconsistencies
Make Stripe the ONLY source of truth
Ensure MLB unlocks 100% reliably
Remove frontend hacks (localTier dependency)
🔧 IMPLEMENTATION
1. 🔥 CREATE SINGLE SOURCE OF TRUTH (MANDATORY)
server/billing/planMap.ts
export const PLAN_MAP = {
  [process.env.STRIPE_PRO_PRICE_ID]: 'pro_nba',
  [process.env.STRIPE_ALL_SPORTS_PRICE_ID]: 'pro_all',
};

export function getPlanFromPrice(priceId: string) {
  return PLAN_MAP[priceId] || 'free';
}
2. 🔥 FIX WEBHOOK (MAKE IT AUTHORITATIVE)
MUST handle ALL of these:
const relevantEvents = [
  'checkout.session.completed',
  'invoice.payment_succeeded',
  'customer.subscription.created',
  'customer.subscription.updated',
  'customer.subscription.deleted',
];
Replace logic with:
const sub = await stripe.subscriptions.retrieve(subscriptionId);

const priceId = sub.items.data[0].price.id;

const plan = getPlanFromPrice(priceId);

await db.users.update({
  where: { stripeCustomerId: sub.customer },
  data: {
    plan,
    subscriptionStatus: sub.status,
  },
});
3. 🔥 REMOVE localTier FROM GATING (IMPORTANT)
❌ DELETE THIS:
|| localTier === "elite"
✅ REPLACE WITH STRICT BACKEND-DRIVEN GATING:
const hasMLBAccess = user.plan === 'pro_all';
4. 🔥 FORCE USER REFETCH AFTER PURCHASE

In success flow:

await queryClient.invalidateQueries(['/api/auth/me']);
await refetchUser();
5. 🔥 HARDEN /api/auth/me FALLBACK

Your current version is good — upgrade it:

if (user.stripeCustomerId) {
  const subs = await stripe.subscriptions.list({
    customer: user.stripeCustomerId,
    status: 'active',
  });

  if (subs.data.length > 0) {
    const sub = subs.data[0];
    const priceId = sub.items.data[0].price.id;

    const plan = getPlanFromPrice(priceId);

    if (user.plan !== plan) {
      await db.users.update({
        where: { id: user.id },
        data: { plan },
      });

      console.log('[STRIPE SYNC FIX]', user.id, plan);
    }
  }
}
6. 🔥 ADD LOGGING (YOU NEED VISIBILITY)

Add this everywhere:

console.log('[STRIPE EVENT]', {
  event: event.type,
  customer: sub.customer,
  priceId,
  plan,
});
7. 🔥 ADD ADMIN DEBUG ENDPOINT (HIGH LEVERAGE)
GET /api/admin/debug-user/:id

Returns:

{
  "plan": "pro_all",
  "stripeCustomerId": "...",
  "subscriptionStatus": "active",
  "priceId": "..."
}
🎯 SUCCESS CRITERIA (REAL)
User pays → MLB instantly unlocked
Refresh → still unlocked
New device → still unlocked
No reliance on frontend hacks
No plan mismatch EVER
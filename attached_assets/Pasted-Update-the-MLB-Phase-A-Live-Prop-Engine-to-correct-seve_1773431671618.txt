Update the MLB Phase A Live Prop Engine to correct several structural issues discovered during review.

These updates must NOT break the existing NBA/NCAAB engines.

Only modify the MLB module and route registration where specified.

-----------------------------------------------------

CHANGE 1 — ADMIN-ONLY GATING (MLB IS STILL PRIVATE)

MLB Phase A is a private testing engine.

Replace the existing tier gate on the props route.

In server/routes.ts change:

app.post("/api/mlb/props", requireTier("all", "elite"), async (req, res) => {

TO:

app.post("/api/mlb/props", requireAdmin, async (req, res) => {

Do NOT expose MLB to subscribers yet.

The diagnostics route remains:

GET /api/mlb/diagnostics → requireAdmin

-----------------------------------------------------

CHANGE 2 — ADD MISSING SHARP BETTOR CONTEXT FIELDS

Extend MLBPropInput in server/mlb/types.ts to include context signals used in real MLB handicapping.

Add fields:

batterHand: "L" | "R" | "S"
pitcherThrows: "L" | "R"

parkHistoryFactor: number | null
bvpPlateAppearances: number | null
bvpOpsLikeFactor: number | null

pitcherVsHandednessFactor: number | null

lineupPocketWeakness: number | null

These values will be provided manually or by Phase B scrapers.

-----------------------------------------------------

CHANGE 3 — MARKET-SPECIFIC EDGE GAP THRESHOLDS

Replace the global EDGE_MIN_GAP constant.

Remove:

EDGE_MIN_GAP = 0.15

Create:

MLB_MARKET_MIN_GAP = {
  hits: 0.25,
  total_bases: 0.35,
  batter_k: 0.30,
  pitcher_k: 0.75,
  hits_allowed: 0.80,
  home_runs: 0.10,
  hrr: 0.60
}

Modify determineMLBSide() in markets.ts so the gap requirement uses:

MLB_MARKET_MIN_GAP[input.market]

instead of a global threshold.

-----------------------------------------------------

CHANGE 4 — ADD NEW FEATURE ENGINEERING FUNCTIONS

Extend server/mlb/featureEngineering.ts with four new functions:

computeParkHistoryAdjustment(input: MLBPropInput): number
  - uses parkHistoryFactor
  - capped to [-0.15, +0.15]

computeHandednessMatchupScore(input: MLBPropInput): number
  - uses pitcherVsHandednessFactor
  - capped to [-0.20, +0.20]

computeBvpAdjustment(input: MLBPropInput): number
  - if bvpPlateAppearances >= 10 apply scaled adjustment
  - capped to [-0.15, +0.15]

computeLineupPocketWeaknessScore(input: MLBPropInput): number
  - uses lineupPocketWeakness
  - capped to [-0.20, +0.20]

These scores should be incorporated into projection modifiers using the same capped-modifier system already implemented.

-----------------------------------------------------

CHANGE 5 — CLARIFY CONTACT QUALITY DATA SOURCES

Rename these fields in MLBPropInput:

hardHitRate → hardHitRateSeason
barrelRateProxy → barrelRateProxySeason

Update computeLiveContactQualityScore() to clearly separate:

season baseline contact ability
vs
today's live AB signals from priorAbResults

-----------------------------------------------------

CHANGE 6 — ADD SETTLEMENT SUPPORT TO DIAGNOSTICS

Extend MLBDiagnosticRecord in server/mlb/diagnostics.ts.

Add optional fields:

settled?: boolean
finalResult?: number | null
projectionError?: number | null

These allow future projection error tracking once results are known.

Projection error formula:

projectionError = projection − finalResult

Only compute this if settled === true.

-----------------------------------------------------

CHANGE 7 — IMPROVE HR MARKET GUARDRAILS

Strengthen the experimental HR logic in markets.ts.

The HR edge calculator must only fire if ALL conditions pass:

exitVelocity >= 98
launchAngle between 10 and 35
hitDistance >= 360
weatherParkScore > 0
pitcherContextScore > -0.2

If these conditions are not met, return NO_EDGE immediately.

-----------------------------------------------------

CHANGE 8 — EXPERIMENTAL MARKET CONFIDENCE CEILING

Home Runs and HRR are experimental markets.

They cannot return HIGH confidence.

Modify confidence logic so:

if market === "home_runs" OR market === "hrr"

maximum allowed confidenceTier = MEDIUM.

-----------------------------------------------------

CHANGE 9 — CONDITIONAL BULLPEN MODIFIER

Modify bullpen logic in projections.ts.

Current formula:

bullpenMod = (bullpenERA / 4.5 - 1) × 0.10

Replace with conditional logic.

Bullpen modifier should only apply when:

timesThruOrder >= 3
OR
pitcherPitchCount > 90

Otherwise bullpenMod = 0.

-----------------------------------------------------

CHANGE 10 — ENSURE TYPESCRIPT CLEAN BUILD

After all updates:

npm run check must return:

0 TypeScript errors.

Do not modify:

NBA engine
NCAAB engine
shared/schema.ts
authentication logic

MLB Phase A remains admin-only.

Yes — plan the next task as a focused Phase 2: Derived Signal Calibration + Confidence Hardening.

Objective:
Now that Task #89 guarantees pipeline reliability, the next priority is improving signal quality and preventing misleading edges from derived totals.

Scope (strict — no UI changes, no engine rewrite):

1. Derived Total Calibration (server/ncaabService.ts)
- Replace naive pace extrapolation with a regressed projection:
  - Blend projectedTotalRaw with a league baseline (~135)
  - Weight increases with elapsed time (early game = more regression)
  - Clamp final result to [100, 200]
- This reduces early-game volatility and prevents inflated projections

2. Derived Edge Suppression (server/ncaabEngine.ts or post-processing layer)
- When liveTotalSource === "derived":
  - Reduce edge magnitude (~60–70% multiplier)
  - Pull probability toward 50 using same factor
- Ensures derived plays are lower confidence than sportsbook-backed plays

3. Output Flagging (non-breaking)
- Add isDerived?: boolean to market output
- Set true when liveTotalSource === "derived"
- No UI changes — this is for future filtering, analytics, and gating

4. Early-Game Guardrail
- Suppress derived full_total markets if elapsedSeconds < ~300 (first ~5 minutes)
- Prevents noise-driven projections before sufficient data exists

5. Sanity Enforcement (if not already guaranteed)
- Ensure recommendedSide always matches projection vs line:
  - OVER only if projection > line
  - UNDER only if projection < line
- Prevent logical contradictions

Out of scope:
- No UI changes
- No changes to buildMarket()
- No sportsbook logic changes
- No new market types

Success criteria:
- Derived totals no longer produce extreme early-game projections
- Derived plays show lower probability/edge vs book-based plays
- No increase in zero-market failures
- Logs clearly differentiate book vs derived behavior

After this, we will evaluate:
- Derived vs book ROI separately
- Whether to extend this system to NBA
You are a senior quantitative sports engineer and debugging specialist.

I need you to audit and repair the LiveLocks NCAAB live betting engine.

Current problem:
- The NCAAB engine logic is off
- The displayed math is not matching the actual projections
- The picks being shown are sometimes incorrect or inconsistent with the math
- The UI/output layer may not be correctly reflecting the underlying calculations

Your task is to debug, refactor, and rebuild the NCAAB engine so that:
1. the math is correct
2. the probabilities are calibrated
3. the displayed pick direction is correct
4. the UI output always matches the actual engine output

==================================================
GOAL
==================================================

Fix the NCAAB engine end-to-end.

This includes:
- projection logic
- probability logic
- edge logic
- over/under pick selection
- displayed math formatting
- output consistency checks

I want this rebuilt in a modular, production-safe way.

==================================================
PART 1 — AUDIT THE CURRENT NCAAB ENGINE
==================================================

First, inspect the current NCAAB code and identify:

1. where the projection is calculated
2. where probability is calculated
3. where the over/under pick is selected
4. where the output text is generated
5. where the displayed math is formatted
6. where final displayed values can drift from internal values

Output an audit report showing:

- projection function name(s)
- probability function name(s)
- pick-selection function name(s)
- formatting/output function name(s)
- likely bug locations
- possible causes of mismatch

==================================================
PART 2 — NORMALIZE THE DATA MODEL
==================================================

Create or refactor a clean NCAAB input/output structure.

Define:

NCAABPropInput
- gameId
- sport = "NCAAB"
- playerId
- playerName
- team
- opponent
- marketType
- currentStat
- liveLine
- liveOddsOver
- liveOddsUnder
- firstHalfStat
- minutesPlayed
- fouls
- currentUsageProxy
- shotAttempts
- rebounds
- assists
- points
- scoreDiff
- gamePace
- opponentPace
- impliedPossessionsRemaining
- injuryContext
- rotationContext
- source timestamps

NCAABPropOutput
- playerName
- marketType
- line
- currentStat
- expectedFinal
- rawProbabilityOver
- rawProbabilityUnder
- calibratedProbabilityOver
- calibratedProbabilityUnder
- impliedProbOver
- impliedProbUnder
- edgePctOver
- edgePctUnder
- recommendedSide
- confidenceTier
- explanationBullets
- displayProjection
- displayProbability
- displayPick
- warnings

==================================================
PART 3 — REBUILD THE PROJECTION LAYER
==================================================

Create a pure function:

calculateNCAABProjection(input: NCAABPropInput): ProjectionResult

This function should estimate:
- expected remaining minutes
- expected remaining possessions
- expected stat accumulation rate
- expected final stat total

Projection should be driven primarily by:
- current stat total
- minutes played and expected remaining minutes
- foul risk
- current role / usage
- game pace
- opponent pace
- score differential / blowout risk
- rotation stability
- shot volume / rebound chances / assist opportunities depending on market
- injury / lineup context if available

Do NOT allow vague smart factors to freely stack.
All modifiers must be bounded.

Projection formula should be explicit and debuggable.

The output must include:
- baseProjection
- paceAdjustment
- foulAdjustment
- minutesAdjustment
- roleAdjustment
- finalProjection

==================================================
PART 4 — REBUILD THE PROBABILITY LAYER
==================================================

Create:

calculateRawProbabilities(input, projectionResult)

The current issue likely includes inflated or incorrect probability mapping.

Do NOT use a simplistic broken conversion.

Use:
- a reasonable stat distribution for NCAAB player props
- normal approximation if needed for points/rebounds/assists style props
- historical variance estimates by market type if available

Return:
- rawProbabilityOver
- rawProbabilityUnder

Then create:

calibrateNCAABProbability(rawProbability, marketType, context)

This should map theoretical probabilities to empirically realistic probabilities using historical results.

I want fewer fake 80–90% edges.

==================================================
PART 5 — FIX PICK DIRECTION LOGIC
==================================================

Create a dedicated function:

determineRecommendedSide(output)

This function must:
- compare calibrated over vs under probability
- compare model projection vs sportsbook line
- ensure recommended side is mathematically consistent
- never display Over if projection is clearly below line unless justified by probability logic
- never display Under if projection is clearly above line unless justified by probability logic

Return:
- recommendedSide = "OVER" | "UNDER" | "NO_EDGE"

Also create safeguards so impossible contradictions are caught.

Example contradiction to detect:
- projection = 27.1
- line = 32.5
- displayed pick = OVER

If contradiction exists, throw warning.

==================================================
PART 6 — FIX DISPLAY / OUTPUT MISMATCHES
==================================================

Create a function:

buildNCAABDisplayOutput(engineOutput)

This function should produce exactly what the UI renders.

Rules:
- displayed projection must equal rounded expectedFinal
- displayed probability must equal the probability of the recommended side
- displayed pick must match recommendedSide
- displayed line must match input liveLine
- displayed edge must match actual calculated edge

Add validation:

validateDisplayConsistency(input, projectionResult, probabilities, displayOutput)

This must detect:
- projection mismatch
- wrong displayed side
- probability-side mismatch
- rounded-value mismatch causing wrong pick display
- stale state mismatch

Return warnings if any inconsistency is found.

==================================================
PART 7 — ADD EXPLANATION LAYER
==================================================

For each NCAAB pick, generate concise explanation bullets like:
- "Projection remains below the live line due to reduced remaining-minute expectation"
- "Foul risk lowers second-half minute ceiling"
- "Pace environment boosts remaining stat opportunities"
- "Rotation context supports extended second-half run"
- "Current usage and shot volume support the over"

These explanations must be generated from actual model drivers, not generic filler.

==================================================
PART 8 — ADD DIAGNOSTIC LOGGING
==================================================

Create a diagnostics module that logs for every settled play:
- line
- final stat
- projection
- projection error
- displayed side
- actual result
- displayed probability
- calibrated probability
- whether display matched actual engine output
- whether pick direction logic was internally consistent

Add functions:
- analyzeProjectionError()
- analyzeCalibrationByBucket()
- analyzeDisplayMismatchRate()
- analyzeWrongSideRate()

==================================================
PART 9 — MARKET SUPPORT
==================================================

Make sure the NCAAB engine properly supports:
- points
- rebounds
- assists
- PRA
- PA
- PR
- RA
- threes if already in engine
- blocks/steals only if already supported, otherwise leave TODO

Each market should use the correct stat accumulation logic.

Do NOT use one generic projection formula for every market.

==================================================
PART 10 — OUTPUT TEST CASES
==================================================

Create test cases that explicitly verify:

CASE 1
- projection below line
- under probability higher
- displayed pick must be UNDER

CASE 2
- projection above line
- over probability higher
- displayed pick must be OVER

CASE 3
- very small edge
- recommendedSide must be NO_EDGE

CASE 4
- display rounding could cause mismatch
- validation must still preserve correct side

CASE 5
- stale line or stale probability
- output should throw warning

==================================================
PART 11 — IMPLEMENTATION RULES
==================================================

Requirements:
- TypeScript
- pure functions where possible
- modular file organization
- strongly typed
- readable debug logs
- no silent contradictions
- no UI display values that differ from engine values

Return:
1. file tree
2. audit findings
3. corrected types
4. corrected projection logic
5. corrected probability logic
6. corrected pick-selection logic
7. corrected display builder
8. validation module
9. test cases
10. summary of what was wrong and what was fixed

Start by auditing the current NCAAB engine and identifying the mismatch points before rewriting the logic.
You are updating the existing MLB Phase A Live Prop Engine implementation in the LiveLocks codebase.

The Phase A engine is already implemented with:

- 7 markets
- calculateMLBPropEdge dispatcher
- ProjectionLog modifier tracking
- suppression rules
- Two-AB rule with early explosive-contact mode
- calibration layer
- backtest harness
- diagnostics module

Now implement additional improvements for better model validation and debugging before MLB is ungated publicly.

These changes MUST NOT break the existing NBA engine.

-----------------------------------------------------

CHANGE 1 — MARKET VALIDATION REPORT

Extend diagnostics.ts to generate market-level performance reports.

Create a new function:

getMLBMarketReport()

Return structure:

{
  market: MLBMarket,
  sampleSize: number,
  hitRate: number,
  roi: number,
  avgProjectionError: number,
  avgEdge: number,
  avgCalibratedProbability: number,
  suppressionCount: number,
  suppressionFalseNegativeRate: number
}

Return reports for all markets:

- Hits
- Total Bases
- Batter Strikeouts
- Pitcher Strikeouts
- Hits Allowed
- Home Runs
- HRR

Add a route:

GET /api/mlb/market-report

Admin only.

-----------------------------------------------------

CHANGE 2 — RAW VS CALIBRATED PROBABILITY TRACKING

Modify MLBPropOutput to include both probabilities.

Add fields:

rawProbabilityOver
rawProbabilityUnder
calibratedProbabilityOver
calibratedProbabilityUnder

Ensure ProjectionLog stores:

rawProbability
calibratedProbability

This allows diagnostics to compare pre-calibration vs post-calibration behavior.

-----------------------------------------------------

CHANGE 3 — SUPPRESSION AUDIT TRACKING

Extend suppression logic to track suppressed plays.

Add fields to diagnostics record:

suppressed: boolean
suppressionReason: string
wouldHaveWon: boolean | null

If suppression occurs:

- projection is still computed
- edge is set to 0
- recommendedSide = NO_EDGE

Diagnostics should store the projection and final outcome so we can evaluate suppression false negatives.

-----------------------------------------------------

CHANGE 4 — SUPPRESSION FALSE NEGATIVE METRIC

Add logic to diagnostics.ts:

suppressionFalseNegativeRate =
suppressedWins / totalSuppressedPlays

Where:

suppressedWins = suppressed plays where projection side would have won.

-----------------------------------------------------

CHANGE 5 — MODIFIER CONTRIBUTION DISTRIBUTION

Extend ProjectionLog analysis.

Add function:

getModifierContributionSummary()

Return:

{
  modifier: string,
  avgContribution: number,
  contributionVariance: number,
  positiveContributionRate: number,
  negativeContributionRate: number
}

Modifiers to analyze:

- liveFormAdjustment
- pitcherAdjustment
- pitchTypeAdjustment
- weatherParkAdjustment
- lineupAdjustment
- bullpenAdjustment

This identifies which signals are dominating projections.

-----------------------------------------------------

CHANGE 6 — STRONG CONTEXT SCORE (IMPROVEMENT)

Replace simple "2 of 5 signals" logic with a weighted score.

Implement function:

computeStrongContextScore()

Score inputs:

- contactQualityScore
- lineupContextScore
- pitcherContextScore
- weatherParkScore
- handednessMatchupScore

Normalize each to 0–1.

Compute:

strongContextScore =
0.30 * contactQualityScore +
0.20 * pitcherContextScore +
0.20 * lineupContextScore +
0.15 * handednessMatchupScore +
0.15 * weatherParkScore

Early explosive-contact mode activates only if:

strongContextScore >= 0.65

Still require:

AB_today == 1
AND EV >= eliteEVThreshold
AND launchAngle in idealBand
AND distance >= eliteDistanceThreshold

-----------------------------------------------------

CHANGE 7 — PROJECTIONLOG EXTENSIONS

Extend ProjectionLog fields to ensure full debugging capability.

Required fields:

baseProjection
liveFormAdjustment
pitcherAdjustment
pitchTypeAdjustment
weatherParkAdjustment
lineupAdjustment
bullpenAdjustment
finalCappedAdjustment
rawProbability
calibratedProbability
confidenceTier
modeUsed (STANDARD | EARLY_EXPLOSIVE)

-----------------------------------------------------

CHANGE 8 — CONFIDENCE CEILING PER MARKET

Implement market-level confidence ceilings.

CORE MARKETS:

Hits
Total Bases
Batter Strikeouts
Pitcher Strikeouts
Hits Allowed

Max calibrated probability allowed:

0.82

EXPERIMENTAL MARKETS:

Home Runs
HRR

Max calibrated probability allowed:

0.70

Apply ceiling AFTER calibration.

-----------------------------------------------------

CHANGE 9 — BACKTEST HARNESS EXTENSION

Extend server/mlb/backtestHarness.ts.

Allow batch input:

POST /api/mlb/backtest

Accept:

{
  inputs: MLBPropInput[]
}

Return:

{
  results: MLBPropOutput[],
  diagnosticsSummary,
  marketReport
}

Use this route for shadow testing.

-----------------------------------------------------

CHANGE 10 — EXPLANATION LAYER

Add explanationBullets to MLBPropOutput.

Return 3–5 short drivers explaining the edge.

Examples:

- "Elite contact quality today (102 EV / ideal launch band)"
- "Pitch count suggests starter fatigue risk"
- "Lineup pocket vulnerability detected"
- "Weather and park increase carry for extra-base hits"
- "Bullpen downgrade expected next inning"

-----------------------------------------------------

DO NOT CHANGE

- existing NBA engine
- shared schema
- NCAAB engine
- user auth logic

MLB remains admin-only.

-----------------------------------------------------

RETURN

1. updated diagnostics.ts
2. updated probability.ts
3. updated projections.ts
4. updated backtestHarness.ts
5. updated MLBPropOutput type
6. new market report endpoint
7. suppression audit logic

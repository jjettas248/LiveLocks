## Packages
(none needed beyond base stack)

## Notes
The application assumes dark mode by default for a premium sports analytics aesthetic.
Backend provides `/api/players`, `/api/teams`, and `/api/calculate` endpoints.
Ensure Zod coercion handles number inputs from the frontend forms.

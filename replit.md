# LiveLocks by PropPulse — NBA/MLB/NCAAB Live Lines

## Overview
LiveLocks is a full-stack Progressive Web Application (PWA) designed for betting analytics in NBA, MLB, and NCAAB. It provides real-time probabilities for player prop bets, analyzes NCAAB live spread/total/team-total probabilities, and enables users to construct correlation-adjusted parlays. The platform aims to enhance sports bettors' decision-making with data-driven insights and sophisticated analytical tools by integrating live game data and offering subscription-based access, notifications, and an administrative panel.

## Landing Page & Growth Features
- **Dual-sport positioning**: Landing page highlights both NBA + MLB engines with proof images, feature bullets, and updated pricing table
- **3-day free trial**: All new subscriptions include a 3-day free trial, configured both at Stripe checkout code level (`trial_period_days: 3`) and baked into Stripe price objects. Trialing users get full tier access.
- **Guided onboarding**: `react-joyride` tour runs on first login, targeting live board, MLB tab, and edge values. Completion tracked via `hasCompletedOnboarding` user column.
- **Sport personalization**: Post-signup sport picker modal (`SportPicker` component) lets users choose NBA, MLB, or both. Stored in `sportFocus` user column, defaults dashboard active tab accordingly.
- **Stripe price IDs**: Pro = `price_1TJJ4M2ceUNmv10tYSsYXA6T` ($40/mo), All Sports = `price_1TJJ4M2ceUNmv10tB8JCzPYe` ($65/mo). Legacy archived IDs mapped in `server/billing/planMap.ts` for backward compatibility.
- **Stripe product IDs**: Pro = `prod_U4rP9VYoK6FC5I`, All Sports = `prod_U4rPSI6tQ4CGz7`

## User Preferences
I prefer clear and concise explanations. When implementing new features or making significant changes, please propose the high-level plan first and wait for my approval before proceeding with detailed implementation. For UI/UX, I prefer modern, clean designs with intuitive navigation. I am open to iterative development and feedback loops.

## System Architecture

### Frontend
- **Frameworks**: React with Vite, Tailwind CSS, and `shadcn/ui`.
- **Data Fetching**: TanStack Query.
- **Routing**: `wouter`.
- **UI/UX**: Features a top tab bar for sports with sub-tabs for specific analyses. Subscription tiers control feature access. Includes book filtering, probability gauges, EV verdicts, CLV indicators, and H2H matchup history. Daily slate reset system. Mobile-optimized components like a Parlay Slip bottom sheet and pull-to-refresh. PWA updates via service worker with `skipWaiting` and network-first strategy. Engine-calibrated probabilities are displayed directly from the server.

### Backend
- **Framework**: Express.js with TypeScript and `tsx`.
- **Database**: PostgreSQL with Drizzle ORM.
- **Shared Components**: `shared/schema.ts` and `shared/routes.ts` for type safety.
- **Authentication**: JWT-based authentication with role-based access control tied to subscription tiers (Free, Pro, All Sports).
- **Notifications**: Twilio for SMS alerts, Resend for transactional emails.
- **NBA Probability Engine (v2)**: Uses distribution-based probability with normal CDF, variance models, and archetype-based calibration. Implements market family grouping, derivative suppression, and directional bias tracking. Includes detailed diagnostics for each play.
- **NCAAB Engine**: Provides projections, probabilities (normal CDF model), and pick directions with deterministic edge rules. Features a diagnostics module and a `qualifiedEdge`/`fallback` architecture for market surfacing, applying weighted scoring for Top Plays and color-coded confidence tiers (ELITE, STRONG, LEAN). Halftime cards show live 2H context. **Dynamic Engine Contract**: `/api/ncaab/live` prepares full canonical payloads with `selectedMarket` (best market auto-selected) and `periodMarkets` (pre-computed selectedMarket objects for full/h1/h2 tabs). UI performs zero calculations — all coverProbability, edge, confidenceLabel, engineProbability, bookProbability, signalTag, and signalDirection are server-resolved. Big ring = selected market cover confidence; small rings = full game total over/under probabilities. Cards follow strict hierarchy: header → big ring → small rings → source pill → EV card → CLV card → market tabs.
- **MLB Prop Engine**: Processes various MLB player prop markets (Hits, Total Bases, Batter K, Pitcher K, Hits Allowed, HR) via a modular pipeline. Features a form indicator system, HR qualifying factors, scout-report translation, and a `SignalScore` system for confidence tiers (ELITE, STRONG, SOLID, WATCHLIST, NO_SIGNAL) based on a probability-first approach. Signals are ranked and styled by tier on the frontend.
- **MLB Signal Engine v2 (SSS Model)**: Rebuilt from edge-detector to signal-surfacing model. Root cause: edge-based qualification killed valid batter OVER signals (HR OVER was -83.4% ROI; without HR OVER, MLB OVER was +22.7% ROI). **Architecture split by market family**: `getMarketFamily()` classifies signals as `batter_over`, `under`, or `hr_radar`. **Batter OVER path**: Uses Situational Strength Score (SSS) via `scoreBatterOverSignal()` — weighted composite of form(15%) + matchup(20%) + park/weather(10%) + LEI(25%) + opportunity(10%) + event boost(10%) + prob(5%) + proj(5%). Prob floor lowered from 60→40; min score 42 (not 55); edge>0 removed from `isActionable`; projection tolerance relaxed per market; `checkSuppression` has 50% relaxed minGap and skips edge minimum/composite gates. **UNDER/pitcher path**: Unchanged — keeps edge model, standard suppression, `computeSignalScore()`. **HR Radar**: Parallel `scoreHRRadar()` scoring can surface HR_WATCH even when batter-over score misses gate. **Signal modes**: `watch`/`heating_up`/`lean`/`strong`/`elite` for batter overs (+ `hr_watch`/`hr_heating_up`/`hr_strong`/`hr_elite` for HR radar). **LEI integration**: `confidenceBoost` applied directly to `calibratedProbabilityOver` for batter markets (capped +8pts). **Family suppression**: Ranks by `signalScore` instead of edge for batter overs. **Live opportunity**: `computeLiveOpportunityScore` skips edge multiplier for batter_over family. **Persistence**: Watch-mode batter OVER signals persisted for calibration with `opportunityScore`/`liveScore`/`eventBoost` enrichment. Key files: `signalScore.ts`, `markets.ts`, `liveGameOrchestrator.ts`, `marketFamily.ts`.
- **HR Radar Detection Immutability**: `detectedInning`/`detectedHalf`/`detectedLabel`/`detectedAt` on `hr_radar_alerts` are write-once at CREATE and never mutated by UPDATE. CREATE backfills these fields from the engine's earliest in-memory detection (`HRAlertSnapshot.detectedInning/detectedHalf/detectedAtMs`) when strictly earlier than the current persistence tick — so the persisted "Detected" inning reflects when we FIRST noticed the player, not the inning where score later climbed. Diagnostic markers: `[HR_RADAR_PERSIST_GAP]` (CREATE backfill happened), `[HR_RADAR_DETECTION_LOCKED]` (UPDATE attempted to mutate frozen field — defensive), `[HR_RADAR_LATE_SIGNAL_LEAK]` (matcher credited called_hit despite signal being post-HR). UI contract documented in `client/src/lib/mlbUiMappers.ts` (`formatDetectedLabel` vs `formatScoreIncreaseLabel`): "Detected" label is frozen; "Score climbed in TX" rendered separately. Files: `server/mlb/hrAlertEngine.ts`, `server/storage.ts:createOrUpdateHrRadarAlert`, `server/mlb/liveGameOrchestrator.ts` (both call sites pass `firstDetectedInning/Half/AtMs`).
- **HR Signal Builder**: `server/mlb/HRSignalBuilder.ts` augments HR signals with a 0-10 build score computed from in-game contact quality (avg/max EV, launch angle, barrels, hard hits, deep flyouts), pitcher fatigue, park/wind, and platoon advantage. Produces `hrBuildScore`, `hrIntensity` ("weak"|"watch"|"strong"|"imminent"), and an edge boost (0-4%) applied to the HR market edge. HR signals gain intensity-based visual treatment (border glow, color escalation, pulse animation for "imminent"). Contact events are persisted to `contact_events` table for learning.
- **MLB Signal Pipeline**: Employs a clean ENGINE → NORMALIZER → API → UI CARD pipeline with a single source of truth (`MLBSignal` interface). `normalizeMLBSignal()` transforms engine output into a flat `MLBSignal` with sided probability selection, market alias normalization, current stat computation, `alreadyHit` detection, pitch mix fallback, smart tag generation, and primary reason generation. Includes a **signal→projection link** that adjusts pitcher market projections based on active pitcher signals (DOMINANT +8%, K_STREAK +6%, COMMAND_LOCKED +4%, FATIGUE_RISK -5%, VELOCITY_DROP -4%, HARD_CONTACT -6%).
- **Top Live Opportunities**: Ranking system using `liveScore = (signalScore/100) × clamp(edge/100,0,1) × (opportunityScore/100)` with event-driven boost component. Frontend displays top-5 by liveScore with ELITE/STRONG/SOLID/WATCH tiers. SpikeAlertBanner triggers at liveScore ≥ 0.10. Pitcher signal badges (DOM/K RUN/CMD/VELO↓/TIRED/HARD HIT) shown on signal cards. New DB columns: signal_score, opportunity_score, live_score, event_boost in persisted_plays.
- **MLB HR Market Fallback**: `resolveBookLine()` in `liveGameOrchestrator.ts` falls back to a line of 0.5 for `home_runs` when no real line is available, aligning with the industry standard for HR O/U.
- **MLB Live Monitoring Stability**: All polling queries (live-games, edge-feed, HR radar, alerts, box score, NBA games/signals) use `placeholderData: (prev) => prev` to prevent UI flashing during refetch cycles. All data freshness gates (edge-feed, HR radar, per-game signals) unified to 10-minute threshold to prevent multi-game orchestrator cycle timing from causing false stale-data dropouts. Per-game signal endpoint uses a 3-minute `isDegraded` soft flag (still serves data) vs 10-minute hard cutoff. Orchestrator heartbeat bumps `updatedAt` unconditionally for live games (not just when signals exist) to prevent stale-data false positives.
- **Simplified MLB Card UX**: `MlbSignalCard.tsx` redesigned for quick decision-making, offering a collapsed view with key signal info and an expanded view for detailed metrics, contact quality, driver breakdown, pitcher arsenal, BvP history, and risk flags. Smart tags and primary reasons are server-computed.
- **MLB Intelligence Layer**: Implements an archetype-driven intelligence pipeline with 8 batter and 6 pitcher archetypes, including variance multipliers, fragility thresholds, thesis generation, and market family suppression with penalty factors for derivatives. Features directional bias tracking with drift detection and archetype-specific safety ceilings.
- **MLB Probability Engine (v2.1)**: Pure probability computation module with no access to book lines. Computes model probability from projection, threshold, and market variance using market-specific distribution methods (normal CDF, negative binomial, binomial). Calibration and safety ceiling application are integrated.
- **MLB Distribution-First Engine Upgrade**: Core MLB markets (hits, total_bases, pitcher_strikeouts, home_runs) rewritten from heuristic math to market-specific outcome-distribution math. New modules for event rates, PA/BF distributions, and outcome distributions. Incorporates calibration shrinkage, market probability ceilings, and edge vs book implied calculations. Includes trust gating for projection quality.
- **MLB Phase 2 Engine Rebuild**: Features a spec-compliant feature layer with 11 normalized scores (e.g., contactQuality, batSpeedPower, handednessMatchup). Market engines in `markets.ts` use weighted formulas. A badge system populates canonical `badges[]`/`riskFlags[]`. An integrity firewall enforces probability ceilings and qualification floors. Confidence score uses a 5-component weighted formula. Event-driven recalculation triggers for high-impact game events.
- **Live Event Interpretation (LEI) Layer**: `server/mlb/liveEventInterpretation.ts` computes 5 sub-scores from in-game contact data: `contactScore` (hard-hit density, quality trends), `nearHrScore` (near-HR events and HR-shaped streaks), `momentumScore` (recency-weighted quality trajectory), `pitcherFatigueScore` (pitch count, TTO, collapsing state), `veloDropScore` (fastball velocity drop vs season avg from `PitcherContext.seasonAvgVelocity`). Produces `confidenceBoost` and contextual tags ("Strong Contact Trend", "Near HR Contact Detected", "Pitcher Fatigue Rising", "Velocity Drop Detected"). Wired into all 3 `MLBPropInput` construction sites (HR radar, batter markets, pitcher markets) in `liveGameOrchestrator.ts`. **Projection integration**: New `liveEvent` modifier bucket in `projections.ts` with market-aware weighting — batter markets get full 5-factor blend, pitcher markets only get fatigue/velo-drop as negative modifiers. Capped at `MODIFIER_CAPS.liveEvent = 0.15`. **HR Signal Builder**: LEI `nearHrScore`, `momentumScore`, and `veloDropScore` feed into `buildHRSignal()` to strengthen HR build scores. **HR Alert Escalation**: `evaluateHRAlert.ts` has a new `LEI_ESCALATION` path that promotes WATCH→ALERT when LEI signals are strong (nearHrScore ≥ 0.08 or confidenceBoost ≥ 0.05). **Signal Tags**: 4 new `SignalTag` values surfaced through `deriveSignalTags()`.
- **Statcast xBA Engine (`server/mlb/statcastXBA.ts`)**: Local EV+LA→xBA lookup grid computes per-batted-ball expected batting average, xSLG, contact grade (barrel/solid/flare/under/topped/weak/popup), and HR probability instantly without API calls. `classifyContact()` produces `StatcastContactClass` per event. `computeGameContactProfile()` aggregates to game-level avgXBA/maxXBA/barrelCount/contactQualityScore. `PlayerContactData` extended with `gameAvgXBA`, `gameMaxXBA`, `gameBarrelCount`, `gameContactQuality`. Per-AB results enriched with `perABxBA`, `contactGrade`, `hrProbability`. **Savant game-level CSV feed** (`fetchSavantGameFeed` in `dataSources.ts`) polls `baseballsavant.mlb.com/statcast_search/csv?game_pk=XXX&type=details` every 90s per active game, overlaying official `estimated_ba_using_speedangle` xBA values when available. Local xBA used as instant fallback. xBA integrated into: (1) `buildContactProfile` in engine uses xBA for contact quality grading, (2) `computeSpecHotColdForm` blends xBA with raw EV trends (50/50 weight), (3) `buildHRSignal` adds score bonuses for high maxXBA (≥.800: +1.5) and avgXBA (≥.500: +1.0). Frontend displays per-AB xBA badges (green ≥.700, blue ≥.400) in HR Radar analyze modal and box score AB log.
- **Real-Time Data Pipeline (AB-to-AB)**: MLB orchestrator polls game state every 10s, with event-driven engine recalculation on every at-bat. High-impact triggers (`new_ab`, `ab_completed`, `out_recorded`, `score_change`, `inning_change`, `pitcher_change`, `tto_shift`, `lineup_substitution`) use 5s dedup for near-instant re-evaluation. `GameStateCache` tracks `homeScore`, `awayScore`, `totalPlays` for AB completion detection. `PlayerContactData` stores both max EV (`exitVelocity`) and latest AB's EV (`latestExitVelocity`/`latestLaunchAngle`). `completedAB` per batter uses actual box score AB count. Server cache TTLs: live games 15s, boxscore stats 15s, signals 30s. Signal staleness gate: 10min (FEED_FRESHNESS_MS). Frontend polls: games list 15s, edge feed 20s, HR radar 20s, boxscore 15s, signal counts 20s. Live pulse indicator shows data freshness on the game strip and boxscore headers.
- **Signal Pipeline Resilience**: Cache preservation prevents empty engine runs (when no markets are evaluated due to data unavailability) from overwriting valid cached signals, bounded by a 10-minute max age. Heartbeat mechanism refreshes `updatedAt` on cache entries with either qualified or watch signals when no state change is detected. Edge cache `entries()` filters TTL-expired entries consistently with `get()`.
- **Early Signal Mode**: Removed hard `playerAB < 1` gate and `pitchCount < 10` gate that blocked ALL signal generation in early innings. Engine now evaluates every batter and pitcher from first pitch. Pre-AB signals are tagged as `isEarlySignal: true` with `WATCHLIST` confidence tier and "Pre-Game Edge" smart tag. Once a batter completes their first AB, signals upgrade to full qualification. This ensures the UI always has content from game start.
- **HR Engine v2 (Conversion-Based Alert Model)**: Complete rebuild of `HRSignalBuilder.ts` and `evaluateHRAlert.ts` targeting 20-25% alert hit rate. **Event Classification**: Every batted ball classified as noiseContact/powerContact/hrShapedContact/missedHrContact/eliteHrContact using EV/LA/distance thresholds (e.g., hrShaped: EV≥98+LA 20-38°+dist≥360, eliteHr: EV≥102+LA 23-34°+dist≥390). **Three Alert Paths**: PATH_A (2+ HR-shaped events, qualified EV mean ≥99, max dist ≥375, remaining PA ≥1.3; relaxed gate requires qualEVMean≥96 + hrBuildScore≥3.5), PATH_B (1 missed/elite HR + pitcher fatigue or env favorable), PATH_C (late-game inning 5+ with HR-shaped contact + favorable pitcher). **Hard Caps**: No official alert from single barrel/hardHit/deepFly/103EV alone. No alert when remaining PA <1.0 without elite evidence. **Negative Suppression**: Veto-based system checks remaining PA, repeat confirmation, LA consistency, headwind, same-side matchup, cold temperature. Hard vetoes block alerts entirely; soft vetoes reduce confidence. **Alert Tiers**: officialAlert/prepare/watch with full diagnostics (alertPath, positiveFactors, suppressionFlags, pitcherFatigueState, environmentContext, contactClasses). All context (remainingPA, pitchCount, timesThrough, parkFactor, wind, temperature, handedness) passed from orchestrator. 10-minute per-player cooldown preserved. **Memory Management**: `clearGameCooldowns()` and `KNOWN_HR_COUNTS` pruning triggered on game-final to prevent unbounded growth. **Double-classification eliminated**: `evaluateHRAlert` reuses `factors.contactClasses` from builder. **currentRuns default**: Uses null-check (not falsy) to preserve 0-0 game scores. **Reconciliation**: Uses last known inning from box score events rather than hardcoded 9. **OnlyHomers boosts**: `hotHitterBoost`/`bvpHrBoost` properly typed on `MLBPropInput`.
- **Dynamic HR Alert State Machine**: `server/mlb/hrAlertEngine.ts` implements a per-batter in-memory state machine (WATCH→PREPARE→BET_NOW→COOLED_OFF→CLOSED) that recomputes on every orchestrator tick. Keyed by `gameId_playerId`. Features: signal decay (12min/3PA half-life anchored to `detectedAtMs`), pitcher HR vulnerability scoring (0-100 from pitch count, TTO, ERA, deterioration, reliever stats), remaining PA expectation, calibrated HR conversion probability with pitcher-change penalty (15% reduction). State transitions: threshold-based (BET_NOW≥14%, PREPARE≥8%, WATCH≥5%), 3 consecutive declines→COOLED_OFF (sticky until re-escalation), hard veto→COOLED_OFF, game final→CLOSED. Snapshot includes `hrReadinessScore`, `hrConversionProbabilityRaw/Calibrated`, `remainingPAExpectation`, `positiveDrivers[]`, `negativeSuppressors[]`, `cooldownReason`, `pitcherHrVulnerability`, `decayFactor`, `peakScore/peakState`, `tickCount`, `dataFreshnessMs`. Wired into orchestrator alongside existing `evaluateHRAlert()` — passes precomputed alert result to avoid double evaluation. Snapshot attached to signal output as `hrAlertSnapshot` and serialized to `/api/mlb/signals` and `/api/mlb/hr-radar` endpoints. `clearGameHrStates()` called on game-final alongside `clearGameCooldowns()`. Shared type `hrAlert` on `MLBSignal` in `shared/mlbSignal.ts`. Frontend: `HrRadarCardUi` extended with 15 dynamic fields; `RadarCard` renders dynamic state badges (color-coded BET_NOW/PREPARE/WATCHING/COOLED_OFF/CLOSED), conversion probability, remaining PA, pitcher vulnerability, decay %, cooldown reason. Expanded view shows drivers/suppressors badges. Sort order prioritizes dynamic state then calibrated conversion probability. BET_NOW cards get red glow border + pulse animation.
- **HR Radar System v2 (Canonical Model)**: HR radar endpoint combines two data sources: (1) edge cache signals tagged `hr_radar` (when available), and (2) DB-persisted alerts from `hr_radar_alerts` table (primary source since `home_runs` UNDER signals are suppressed and hrRadarOnly mode rarely produces OVER signals). Active DB alerts with `signalState=actionable` or `confidenceTier=strong` populate bettable; others populate watchlist. Dedicated `hr_radar_alerts` table with unique constraint on (session_date, game_id, player_id) for hard dedup — one outcome per player/game/session. `hr_radar_analytics` table for durable performance history. Storage CRUD: `createOrUpdateHrRadarAlert` (upsert with score tracking + increase detection), `resolveHrRadarAlertAsHit`, `resolveHrRadarAlertAsMiss`, `reconcileHrRadarAlertsForGame` (bulk hit/miss resolution → dedup → archive), `collapseDuplicateHrRadarOutcomes`, `archiveDailyHrRadarOutcomesToAnalytics`. API routes: `GET /api/mlb/hr-radar-board` (today's board), `GET /api/mlb/hr-radar-analyze/:playerId/:gameId` (AB-by-AB analyze modal with full contact data), `GET /api/admin/hr-radar-analytics` (admin analytics with filters), `GET /api/mlb/hr-radar-grading-history` (per-day W/L summaries for last 14 days), `GET /api/mlb/hr-radar-grading/:sessionDate` (historical day's graded outcomes). Client: `HrRadarCardUi` extended with `detectedLabel`, `scoreIncreased`, `scoreIncreaseLabel`, `peakScore`, `alertPath`, `conversionPct`. WATCH cards surface Conv% + alertPath when no edge/enginePct available. Expanded resolved cards show diagnostic summary (alertPath, conversionPct, peak score, detectedLabel). Missed cards enriched with alertPath + conversionPct badges. `HrRadarAnalyzeViewModel` type for analyze modal. Analyze button opens full batter detail modal with score trajectory, AB log with BRL/HH badges, trigger tags, and engine factors. Admin analytics surface in analytics-tab.tsx with summary stats, filtering, and graded history table. **Grading accuracy fix**: Auto-graded hits (HRs without prior alerts) use `detectedLabel: null` and `triggerTags: ["auto_graded"]` — excluded from W/L record but still shown as amber "Uncalled HR" cards. **Date chip history**: Scrollable chip strip in grading panel lets users browse past days' results with W/L counts per day.
- **MLB UI Architecture (NBA-mirrored rebuild)**: Dashboard with sub-tabs [Games | Live Feed | HR Radar]. **Games tab** features a horizontal game chip strip and a two-panel layout for selected games, displaying game context and active signals via `MlbSignalCard`. **Live Feed tab** shows `TopPlays` and `LiveBoard` (tier-grouped). **HR Radar tab** focuses on HR edges and activity. **Unified `MlbSignalCard`** is used across all views, collapsing by default and expanding to show detailed info. Includes a signal state machine and floating bet slip.
- **MLB Canonical Normalizers (`mlbNormalizers.ts`)**: Centralized display helpers for MLB UI. `formatMlbDisplayInning(game)` produces clean ▲/▼ inning text, `formatMlbDisplayStatus(game)` sanitizes all status strings (live/final/pregame/delayed/suspended), `normalizeMlbGameChip(game)` produces a canonical `MlbGameChipViewModel` consumed directly by GameChipStrip (no manual status branching). `deriveMlbQuickViewColorTier(signals, playerId)` maps signals to green/yellow/blue/neutral row accents (75%+/65%+/55%+ thresholds + WATCHLIST tier → blue when bestPct>0). `deriveBestPlay(signals, playerId)` returns the best play info with market, side, probability, and confidence tier. Used by MlbBoxScore for color-coded scanning rows. No NBA logic — MLB-only probability thresholds.
- **Inning Display Resilience**: Live-games API uses `competition.status.period` (direct numeric) as primary ESPN inning source, with `shortDetail` regex as fallback. `isTopInningFromEspn` checks for "Bot"/"Bottom" (→false) before "Top"/"Mid" (→true). `syncGameState` preserves previous cached inning/isTopInning when MLB Stats API returns null `currentInning` instead of defaulting to inning 1.
- **MLB Grading (Phase 5)**: End-to-end MLB play grading using MLB Stats API boxscores, resolving ESPN event IDs to MLB gamePk. Maps 14 markets to boxscore fields. Grading cron runs every 3 minutes, with admin endpoints for manual trigger and grading summaries.
- **Consolidated Analytics Pipeline**: All signal persistence routes through `trackPlay()` in `server/services/playTracker.ts` → `persisted_plays` table. MLB signals auto-persist from the engine cycle in `liveGameOrchestrator.ts` (no user request needed) with in-memory dedup guard (`mlbPersistGuard` Map keyed by canonical `playerId|market|direction|gameId|date` — allows passage when new signalScore exceeds previously persisted score for upsert). `recordPlay` in storage does upsert: updates line/prob/signalScore/confidenceTier when higher signalScore arrives for same canonical key. Analytics queries (`getGradedPlaysForCalibration`, `getAllSettledPlays`) apply in-memory canonical dedup keeping highest signalScore entry. `cleanDuplicatePlays` runs on startup and via admin endpoint to physically remove historical duplicates. Disabled MLB markets (`batter_strikeouts`, `hr_allowed`, `walks_allowed`) defined in `DISABLED_MLB_MARKETS` constant in `types.ts`, excluded from active market arrays, skipped during grading. Admin endpoint: `POST /api/admin/mlb/clean-duplicates`. Grading runs via `gradePersistedPlays` every 3 minutes.
- **Roster Sync**: API endpoint for synchronizing ESPN rosters and team abbreviations, handling cross-date listings.
- **MLB Self-Learning Engine**: `server/mlb/selfLearning.ts` provides multi-source calibration. Three sub-components: (1) Contact profile learning from 2,663+ BIP events (EV/LA thresholds, barrel/hard-hit rates), (2) Pitch vulnerability learning from HR outcomes (pitch-type HR distribution), (3) Market rate calibration from box score stats (actual vs expected rates per market). Refreshes every 30min. Learned rate adjustments feed into `seasonAvg` in the projection pipeline via `getLearnedRateAdjustment()`. Learned contact quality scores (`learnedHitLikelihood`, `learnedHrLikelihood`, `pitchTypeHrRisk`) enhance feature engineering scores. Diagnostic endpoint: `GET /api/mlb/self-learning-status`.
- **OnlyHomers.com Integration**: Self-learning HR data source scraper (`server/mlb/onlyHomersService.ts`). Scrapes daily HR outcomes with full Statcast data (EV, LA, distance, pitch type, pitcher, ballpark), hot hitter lists (7/14/30-day), and ballpark HR counts. Data stored in `hr_outcomes`, `hr_hot_hitters`, `hr_ballpark_factors` tables. Orchestrator runs scrape hourly and caches hot hitter/ballpark data in-memory (30min refresh). Hot hitters get score boosts in `buildHRSignal` (+0.8 for 7d, +0.5 for 14d, +0.3 for 30d) and `HOT_HITTER` badge. HR radar cards show hot hitter flame indicator and OnlyHomers verified badge. API routes: `GET /api/mlb/onlyhomers/stats`, `GET /api/mlb/onlyhomers/hot-hitters`, `GET /api/mlb/onlyhomers/batter/:name`, `GET /api/mlb/onlyhomers/bvp/:batter/:pitcher`, `POST /api/admin/onlyhomers/scrape`.

- **Unified Admin Analytics**: All admin analytics consolidated into a single "Analytics" tab on `/admin` page via `UnifiedAnalyticsPanel` component (`client/src/components/unified-analytics.tsx`). Contains 2 sub-tabs: Dashboard (unified KPI row with win rate/ROI/edge/prob/direction split, global sport+range+direction filters including NCAAB, probability buckets, NBA/NCAAB confidence buckets with archetype/flagship filters, single paginated play history table, settle/dedupe actions), HR Radar (tier breakdowns, peak score buckets, filtered history). No duplicate data views — one filter bar, one KPI row, one play table. Old `analytics-tab.tsx`, `analytics.tsx`, `performance.tsx` deleted as dead code. ROI Calculator and Calibration Dashboard remain as separate admin tabs.
- **Date Handling (Eastern Time)**: All server-side "today" date computations use `todayET()` from `server/utils/dateUtils.ts` which returns the current date in US Eastern time (America/New_York). This prevents the off-by-one-day bug where late-night ET games (past midnight UTC) would be recorded with the next day's date. Affects: playTracker, alertManager, storage, routes, auth, MLB orchestrator, dataPullService.

### Decision Engine Dashboard
- **Live Edge Feed**: Collapsible button for cross-sport edge cards.
- **Trust Track Record Panel**: Public-facing 7-day win rate and ROI.
- **User Status Rail**: Displays user's tier, plays used, live signal counts, and 7-day win rate.
- **Live Update Toast**: Notifies users of new ELITE edges across sports.
- **Shared Signal Components**: Reusable UI components for displaying signals and confidence across all sports.

### Admin Panel
- Manages users, subscription tiers, play counts, feedback, and daily slate reset times.

## External Dependencies

- **ESPN**: Live game data, scores, player statistics.
- **The Odds API**: Spread/total book lines, American odds. Supports multi-key rotation (`ODDS_API_KEY`, `ODDS_API_KEY_2`) — when one key's quota is exhausted, the system automatically rotates to the next available key and retries.
- **Sports Game Odds (SGO)**: 1H lines, team total lines.
- **Stripe**: Payment gateway for subscriptions.
- **Twilio**: SMS notifications.
- **Resend**: Transactional email service.
- **PostgreSQL**: Application data storage.

## Engine Isolation Architecture (v1.0)

### Overview
NBA and MLB engines are fully isolated into separate systems under `server/engines/`. Each sport has its own validation rules, confidence tiers, fallback modes, and diagnostics — zero shared calculation logic between sports.

### Structure
- `server/engines/nba/` — NBA engine wrapper (regression-based, edge threshold, low-frequency)
  - `index.ts` — `processNBAEngine()` entry point, maps candidates → NBAPlay[], applies strict/fallback filtering
  - `types.ts` — NBAPlay, NBAEngineOutput, NBAEngineDiagnostics, NBAValidationRules with strict/fallback constants
  - `validation.ts` — NBA-specific signal validation (edge minimum, probability bounds, projection alignment)
- `server/engines/mlb/` — MLB engine wrapper (contact-based, event-driven, high-frequency)
  - `index.ts` — `processMLBEngine()` entry point, maps candidates → MLBPlay[], confidence tiering instead of hard edge filter
  - `types.ts` — MLBPlay, MLBEngineOutput, MLBEngineDiagnostics, MLBContactProfile, MLBValidationRules
  - `validation.ts` — MLB-specific signal validation (confidence tier minimum, no regression)
- `docs/agents/nba-agent.md` — Locked NBA engine agent specification
- `docs/agents/mlb-agent.md` — Locked MLB engine agent specification

### Key Contracts
- NBA output: `{ plays, engine: "NBA", mode: "strict"|"fallback", confidence: "low"|"medium"|"high", diagnostics }`
- MLB output: `{ plays, engine: "MLB", mode: "strict"|"fallback", confidence: "developing"|"strong"|"elite", contactProfile, diagnostics }`

### Orchestration
- `routes.ts` imports `processNBAEngine` and `processMLBEngine` directly
- NBA live-signals path uses NBA engine wrapper (no shared `filterValidSignals`/`filterValidEngineOutputs`)
- MLB live-signals path uses MLB engine wrapper for diagnostics and engine tagging
- NCAAB uses shared services (not yet isolated); standalone `/ncaab` page at `client/src/pages/ncaab-live.tsx` with `/api/ncaab/live` endpoint
- Debug endpoint: `GET /api/debug/engine-isolation` shows isolation status and cross-contamination check

## NBA Engine Validation Harness

### Overview
A permanent validation framework that encodes `NBA_Model_Logic.md` into executable assertions, automatically detecting any drift in implementation, calibration, filtering, or output shape.

### Files
- `server/validation/nba/harness.ts` — Core harness with constant validation, archetype classification tests, fixture scenarios, calibration stability checks, drift reporting
- `server/validation/nba/fixtures.ts` — 10 deterministic test scenarios covering all 7 archetypes, combo props, under-side, halftime H1→H2 transitions
- `server/validation/nba/run.ts` — CLI runner (exit code 0 = pass, 1 = fail)

### What It Validates
- All 55 engine constants (variance multipliers, fragility multipliers, correlations, combo extras, safety ceilings) match documented values
- 8 archetype classification test cases
- 10 fixture scenarios testing directional integrity, probability bounds, output contracts, fragility reasons, ceiling enforcement
- Calibration stability (average confidence range, over/under balance)

### Running
- **CLI**: `npx tsx server/validation/nba/run.ts`
- **Admin endpoint**: `GET /api/debug/nba/validate` (requires admin auth)

### Drift Types Detected
ARCHETYPE_DRIFT, BLENDED_RATE_DRIFT, CALIBRATION_DRIFT, FRAGILITY_DRIFT, SAFETY_CEILING_DRIFT, DIRECTIONAL_INTEGRITY_FAILURE, PROBABILITY_BOUNDS_FAILURE, OUTPUT_CONTRACT_FAILURE, COVARIANCE_DRIFT, CONSTANT_DRIFT